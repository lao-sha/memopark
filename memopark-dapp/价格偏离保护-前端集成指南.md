# 价格偏离保护机制 - 前端集成指南

**创建时间**：2025-10-20  
**版本**：v1.0  
**状态**：✅ 核心功能已集成

---

## 📋 功能概述

本文档说明如何在前端集成价格偏离保护机制（±20%限制），为用户提供实时的价格预警和友好的错误提示。

---

## ✅ 已完成的前端集成

### 1. CreateOrderPage.tsx（OTC订单创建页）✅

**文件位置**：`/home/xiaodong/文档/memopark/memopark-dapp/src/features/otc/CreateOrderPage.tsx`

#### 1.1 数据结构更新

```typescript
// MarketMaker接口新增字段（第21行）
interface MarketMaker {
  mmId: number
  owner: string
  feeBps: number
  sellPremiumBps: number  // 🆕 2025-10-20：Sell溢价（OTC订单）
  minAmount: string
  publicCid: string
  deposit: string
}
```

#### 1.2 状态管理

```typescript
// 新增状态（第96-97行）
const [basePrice, setBasePrice] = React.useState<number>(0)  // 基准价格（USDT，精度10^6）
const [loadingPrice, setLoadingPrice] = React.useState<boolean>(true)
```

#### 1.3 基准价格查询

```typescript
// 查询pallet-pricing的市场加权均价（第104-125行）
React.useEffect(() => {
  const loadBasePrice = async () => {
    try {
      const api = await getApi()
      const price = await (api.query as any).pricing?.memoMarketPriceWeighted?.()
      if (price) {
        const priceValue = Number(price.toString())
        setBasePrice(priceValue)
        console.log('✅ 基准价格加载成功:', (priceValue / 1_000_000).toFixed(6), 'USDT/MEMO')
      }
    } catch (e: any) {
      console.error('加载基准价格失败:', e)
    } finally {
      setLoadingPrice(false)
    }
  }
  
  loadBasePrice()
  // 定时更新基准价格（每30秒）
  const interval = setInterval(loadBasePrice, 30000)
  return () => clearInterval(interval)
}, [])
```

#### 1.4 价格偏离计算函数

```typescript
// 计算价格偏离率（第346-366行）
const calculatePriceDeviation = (makerId: number): { 
  finalPrice: number; 
  deviationPercent: number; 
  isWarning: boolean; 
  isError: boolean 
} => {
  const maker = marketMakers.find(m => m.mmId === makerId)
  if (!maker || basePrice === 0) {
    return { finalPrice: 0, deviationPercent: 0, isWarning: false, isError: false }
  }
  
  // 应用sell溢价
  // final_price = base_price × (10000 + sell_premium_bps) / 10000
  const sellPremium = maker.sellPremiumBps
  const finalPrice = Math.floor(basePrice * (10000 + sellPremium) / 10000)
  
  // 计算偏离率（百分比）
  const deviationPercent = Math.abs((finalPrice - basePrice) / basePrice * 100)
  
  // 警告：偏离率 > 15%
  const isWarning = deviationPercent > 15 && deviationPercent <= 20
  // 错误：偏离率 > 20%
  const isError = deviationPercent > 20
  
  return { finalPrice, deviationPercent, isWarning, isError }
}
```

#### 1.5 解析做市商数据

```typescript
// 加载做市商时解析sellPremiumBps（第154行）
makers.push({
  mmId,
  owner: appData.owner || '',
  feeBps: appData.feeBps || 0,
  sellPremiumBps: appData.sellPremiumBps !== undefined ? Number(appData.sellPremiumBps) : 0,  // 🆕
  minAmount: appData.minAmount || '0',
  publicCid: appData.publicCid ? 
    (Array.isArray(appData.publicCid) ? 
      new TextDecoder().decode(new Uint8Array(appData.publicCid)) : 
      appData.publicCid) : '',
  deposit: appData.deposit || '0'
})
```

---

### 2. 建议的UI改进（待实施）

#### 2.1 价格信息显示

在选择挂单后显示价格信息面板：

```tsx
{selectedListing && basePrice > 0 && (
  <Card style={{ marginTop: 16, backgroundColor: '#f0f8ff' }}>
    <Title level={5}>💰 订单价格信息</Title>
    
    <Descriptions bordered size="small">
      <Descriptions.Item label="基准价格（pallet-pricing）">
        <Text strong>{(basePrice / 1_000_000).toFixed(6)} USDT/MEMO</Text>
      </Descriptions.Item>
      
      <Descriptions.Item label="做市商溢价">
        <Text type={selectedListing.makerInfo?.sellPremiumBps! > 0 ? 'danger' : 'success'}>
          {selectedListing.makerInfo?.sellPremiumBps! > 0 ? '+' : ''}
          {(selectedListing.makerInfo?.sellPremiumBps || 0) / 100}% 
          ({selectedListing.makerInfo?.sellPremiumBps || 0} bps)
        </Text>
      </Descriptions.Item>
      
      {(() => {
        if (!selectedListing.makerInfo) return null
        const { finalPrice, deviationPercent, isWarning, isError } = 
          calculatePriceDeviation(selectedListing.makerInfo.mmId)
        
        return (
          <>
            <Descriptions.Item label="最终订单价格">
              <Text strong>{(finalPrice / 1_000_000).toFixed(6)} USDT/MEMO</Text>
            </Descriptions.Item>
            
            <Descriptions.Item label="价格偏离">
              <Text type={isError ? 'danger' : isWarning ? 'warning' : 'success'}>
                {deviationPercent.toFixed(2)}%
                {isError && ' ⚠️ 超出20%限制'}
                {isWarning && ' ⚡ 接近20%限制'}
              </Text>
            </Descriptions.Item>
          </>
        )
      })()}
    </Descriptions>
    
    {(() => {
      if (!selectedListing.makerInfo) return null
      const { deviationPercent, isWarning, isError } = 
        calculatePriceDeviation(selectedListing.makerInfo.mmId)
      
      if (isError) {
        return (
          <Alert 
            type="error" 
            showIcon 
            style={{ marginTop: 12 }}
            message="⛔ 价格偏离过大"
            description={`该做市商的价格偏离基准价${deviationPercent.toFixed(1)}%，超过20%限制！链端将拒绝创建订单，请选择其他做市商。`}
          />
        )
      }
      
      if (isWarning) {
        return (
          <Alert 
            type="warning" 
            showIcon 
            style={{ marginTop: 12 }}
            message="⚠️ 价格偏离较大"
            description={`该做市商的价格偏离基准价${deviationPercent.toFixed(1)}%，接近20%限制。建议谨慎选择。`}
          />
        )
      }
      
      return (
        <Alert 
          type="success" 
          showIcon 
          style={{ marginTop: 12 }}
          message="✅ 价格合理"
          description={`价格偏离基准价${deviationPercent.toFixed(1)}%，在正常范围内。`}
        />
      )
    })()}
  </Card>
)}
```

#### 2.2 订单创建前验证

在`onCreate`函数开始添加前端验证：

```typescript
const onCreate = async (values: any) => {
  try {
    setCreating(true)
    
    // 🆕 2025-10-20：价格偏离前端检查
    if (selectedListing?.makerInfo) {
      const { deviationPercent, isError } = calculatePriceDeviation(selectedListing.makerInfo.mmId)
      
      if (isError) {
        message.error({
          content: `价格偏离过大（${deviationPercent.toFixed(1)}%），超过20%限制！请选择其他做市商。`,
          duration: 5
        })
        setCreating(false)
        return
      }
      
      if (deviationPercent > 15) {
        const confirmed = window.confirm(
          `价格偏离警告：\n\n` +
          `• 基准价格：${(basePrice / 1_000_000).toFixed(6)} USDT/MEMO\n` +
          `• 最终价格：${((basePrice * (10000 + selectedListing.makerInfo.sellPremiumBps) / 10000) / 1_000_000).toFixed(6)} USDT/MEMO\n` +
          `• 偏离率：${deviationPercent.toFixed(2)}%\n\n` +
          `价格偏离较大，是否继续创建订单？`
        )
        
        if (!confirmed) {
          setCreating(false)
          return
        }
      }
    }
    
    // ... 原有的订单创建逻辑
  } catch (e: any) {
    // ... 错误处理
  }
}
```

#### 2.3 链端错误处理

在错误处理中添加特定的价格偏离错误提示：

```typescript
try {
  // ... 订单创建
} catch (e: any) {
  console.error('订单创建失败:', e)
  
  let errorMsg = '订单创建失败'
  
  // 🆕 2025-10-20：处理价格偏离错误
  if (e.message?.includes('PriceDeviationTooLarge')) {
    errorMsg = '价格偏离过大：订单价格超出允许范围（±20%），请选择其他做市商或等待市场价格调整'
  } else if (e.message?.includes('InvalidBasePrice')) {
    errorMsg = '市场价格暂不可用，请稍后再试'
  } else {
    errorMsg = e.message || '订单创建失败'
  }
  
  message.error({
    content: errorMsg,
    duration: 8,
    key: 'create-order-error'
  })
  
  setCreating(false)
}
```

---

### 3. SimpleBridgePage.tsx（Bridge订单创建页）

**建议相同的集成方式**：

1. 添加`basePrice`状态
2. 查询`pallet-pricing::memoMarketPriceWeighted()`
3. 解析做市商的`buyPremiumBps`
4. 计算价格偏离（使用负溢价）
5. 显示价格信息和警告
6. 前端验证和错误处理

**价格计算差异**：
```typescript
// Bridge使用buy_premium_bps（通常为负数）
const finalPrice = Math.floor(basePrice * (10000 + buyPremiumBps) / 10000)
```

---

## 🎨 UI/UX 建议

### 颜色方案

| 状态 | 颜色 | 使用场景 |
|------|------|---------|
| **正常（0-15%）** | 绿色 `#52c41a` | 价格偏离在正常范围 |
| **警告（15-20%）** | 黄色 `#faad14` | 价格偏离较大，接近限制 |
| **错误（>20%）** | 红色 `#ff4d4f` | 价格偏离超限，无法创建 |

### 图标使用

- ✅ `<CheckCircleOutlined />` - 价格正常
- ⚠️ `<WarningOutlined />` - 价格警告
- ⛔ `<CloseCircleOutlined />` - 价格超限

---

## 📝 错误消息映射

### 链端错误 → 前端友好提示

| 链端错误 | 前端提示 |
|---------|---------|
| `PriceDeviationTooLarge` | 价格偏离过大：订单价格超出允许范围（±20%），请选择其他做市商 |
| `InvalidBasePrice` | 市场价格暂不可用，请稍后再试 |

---

## 🧪 测试场景

### 1. 正常价格测试

**条件**：
- 基准价格：0.01 USDT/MEMO
- 做市商溢价：+5% (+500 bps)
- 最终价格：0.0105 USDT/MEMO
- 偏离率：5%

**预期结果**：
- ✅ 显示绿色"价格合理"提示
- ✅ 允许创建订单
- ✅ 链端接受订单

### 2. 警告价格测试

**条件**：
- 基准价格：0.01 USDT/MEMO
- 做市商溢价：+18% (+1800 bps)
- 最终价格：0.0118 USDT/MEMO
- 偏离率：18%

**预期结果**：
- ⚠️ 显示黄色"价格偏离较大"警告
- ⚠️ 创建订单前弹出确认对话框
- ✅ 确认后允许创建
- ✅ 链端接受订单

### 3. 超限价格测试

**条件**：
- 基准价格：0.01 USDT/MEMO
- 做市商溢价：+50% (+5000 bps)
- 最终价格：0.015 USDT/MEMO
- 偏离率：50%

**预期结果**：
- ❌ 显示红色"价格偏离过大"错误
- ❌ 禁用"创建订单"按钮
- ❌ 前端阻止提交
- ❌ 如果提交到链端，链端返回`PriceDeviationTooLarge`错误

---

## 💻 完整示例代码

### 价格信息面板组件

```tsx
// PriceDeviationPanel.tsx
import React from 'react'
import { Card, Descriptions, Alert, Typography } from 'antd'
import { CheckCircleOutlined, WarningOutlined, CloseCircleOutlined } from '@ant-design/icons'

const { Title, Text } = Typography

interface PriceDeviationPanelProps {
  basePrice: number  // 精度10^6
  premium: number  // bps
  finalPrice: number  // 精度10^6
  deviationPercent: number  // 百分比
  type: 'buy' | 'sell'  // OTC=sell, Bridge=buy
}

export const PriceDeviationPanel: React.FC<PriceDeviationPanelProps> = ({
  basePrice,
  premium,
  finalPrice,
  deviationPercent,
  type
}) => {
  const isWarning = deviationPercent > 15 && deviationPercent <= 20
  const isError = deviationPercent > 20
  
  return (
    <Card style={{ marginTop: 16, backgroundColor: '#f0f8ff' }}>
      <Title level={5}>💰 价格信息</Title>
      
      <Descriptions bordered size="small" column={2}>
        <Descriptions.Item label="基准价格" span={2}>
          <Text strong>{(basePrice / 1_000_000).toFixed(6)} USDT/MEMO</Text>
        </Descriptions.Item>
        
        <Descriptions.Item label="溢价" span={2}>
          <Text type={premium > 0 ? 'danger' : 'success'}>
            {premium > 0 ? '+' : ''}{(premium / 100).toFixed(2)}% ({premium} bps)
          </Text>
        </Descriptions.Item>
        
        <Descriptions.Item label="最终价格" span={2}>
          <Text strong>{(finalPrice / 1_000_000).toFixed(6)} USDT/MEMO</Text>
        </Descriptions.Item>
        
        <Descriptions.Item label="价格偏离" span={2}>
          <Text type={isError ? 'danger' : isWarning ? 'warning' : 'success'}>
            {deviationPercent.toFixed(2)}%
            {isError && ' (超限)'}
            {isWarning && ' (警告)'}
          </Text>
        </Descriptions.Item>
      </Descriptions>
      
      {isError && (
        <Alert 
          type="error" 
          showIcon 
          icon={<CloseCircleOutlined />}
          style={{ marginTop: 12 }}
          message="⛔ 价格偏离过大"
          description={`价格偏离${deviationPercent.toFixed(1)}%，超过20%限制！链端将拒绝此订单。`}
        />
      )}
      
      {isWarning && !isError && (
        <Alert 
          type="warning" 
          showIcon 
          icon={<WarningOutlined />}
          style={{ marginTop: 12 }}
          message="⚠️ 价格偏离较大"
          description={`价格偏离${deviationPercent.toFixed(1)}%，接近20%限制，请谨慎选择。`}
        />
      )}
      
      {!isWarning && !isError && (
        <Alert 
          type="success" 
          showIcon 
          icon={<CheckCircleOutlined />}
          style={{ marginTop: 12 }}
          message="✅ 价格合理"
          description={`价格偏离${deviationPercent.toFixed(1)}%，在正常范围内。`}
        />
      )}
    </Card>
  )
}
```

---

## 🚀 部署建议

### 开发环境

1. **开启详细日志**：
   ```typescript
   console.log('[价格检查] 基准价格:', basePrice)
   console.log('[价格检查] 溢价:', premium, 'bps')
   console.log('[价格检查] 最终价格:', finalPrice)
   console.log('[价格检查] 偏离率:', deviationPercent, '%')
   ```

2. **降低限制用于测试**：
   ```typescript
   const isWarning = deviationPercent > 10  // 开发：10%
   const isError = deviationPercent > 15    // 开发：15%
   ```

### 生产环境

1. **使用正式限制**：
   ```typescript
   const isWarning = deviationPercent > 15  // 生产：15%
   const isError = deviationPercent > 20    // 生产：20%
   ```

2. **添加监控**：
   - 统计价格偏离分布
   - 监控警告和错误触发频率
   - 分析用户行为

---

## 📊 实施进度

| 项目 | 状态 | 完成度 |
|------|------|--------|
| CreateOrderPage 数据结构 | ✅ 完成 | 100% |
| CreateOrderPage 基准价格查询 | ✅ 完成 | 100% |
| CreateOrderPage 偏离计算 | ✅ 完成 | 100% |
| CreateOrderPage UI显示 | 🔄 建议 | 0% |
| SimpleBridgePage 集成 | 🔄 建议 | 0% |
| 错误处理优化 | 🔄 建议 | 0% |

**核心功能**：✅ 已完成  
**UI优化**：🔄 建议实施

---

## 🎯 后续工作

### 短期（1周内）

1. **完成UI显示**
   - 实现价格信息面板
   - 添加价格偏离警告
   - 优化用户体验

2. **SimpleBridgePage集成**
   - 复用CreateOrderPage的逻辑
   - 适配Bridge场景（负溢价）

3. **测试验证**
   - 开发环境测试
   - 各种价格场景测试

### 中期（1个月内）

1. **数据分析**
   - 统计用户行为
   - 优化警告阈值
   - 收集用户反馈

2. **性能优化**
   - 减少API调用
   - 优化渲染性能

---

**文档创建时间**：2025-10-20  
**最后更新**：2025-10-20  
**作者**：AI Assistant  
**状态**：✅ 核心功能已集成，UI优化建议待实施

