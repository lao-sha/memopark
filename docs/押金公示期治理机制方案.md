# 押金公示期治理机制方案

> **文档版本**: v1.0.0  
> **更新日期**: 2025-11-13  
> **适用范围**: 投诉、留言、修改等所有内容操作  
> **核心机制**: 押金担保 + 公示期保护 + 自动退回

---

## 📋 目录

1. [方案概述](#方案概述)
2. [核心机制设计](#核心机制设计)
3. [投诉押金机制](#投诉押金机制)
4. [留言押金机制](#留言押金机制)
5. [修改押金机制](#修改押金机制)
6. [公示期机制](#公示期机制)
7. [押金退回机制](#押金退回机制)
8. [完整流程设计](#完整流程设计)
9. [技术实现方案](#技术实现方案)
10. [合理性分析](#合理性分析)
11. [可行性分析](#可行性分析)
12. [风险评估与应对](#风险评估与应对)
13. [实施路线图](#实施路线图)

---

## 1. 方案概述

### 1.1 设计理念

**"押金担保 + 公示期保护 + 自动退回"** 三重机制：

1. **押金担保**：所有操作（投诉、留言、修改）都需要支付押金
2. **公示期保护**：操作后进入公示期，给予公众投诉机会
3. **自动退回**：公示期结束且无投诉，自动退回押金

### 1.2 设计目标

1. ✅ **防止恶意操作**：押金机制提高恶意操作成本
2. ✅ **保护内容安全**：公示期给予公众监督机会
3. ✅ **激励合规操作**：无投诉自动退回，鼓励合规
4. ✅ **透明公正**：所有流程链上可查，公开透明
5. ✅ **自动化处理**：公示期到期自动处理，减少人工干预

### 1.3 适用操作

- **投诉操作**：公众对内容进行投诉
- **留言操作**：用户在内容下留言
- **修改操作**：拥有者修改内容（墓地、逝者、作品等）

---

## 2. 核心机制设计

### 2.1 押金生命周期

```
用户发起操作（投诉/留言/修改）
    ↓
支付押金（冻结在托管账户）
    ↓
操作进入公示期
    ↓
公示期内（7-30天）
    ├─ 有投诉 → 进入投诉处理流程
    └─ 无投诉 → 公示期到期
    ↓
公示期到期
    ├─ 有投诉 → 押金冻结，等待处理结果
    └─ 无投诉 → 自动退回押金
    ↓
[如果有投诉]
    ↓
投诉处理完成
    ├─ 投诉成立 → 罚没押金
    └─ 投诉不成立 → 退回押金
```

### 2.2 押金标准

#### 2.2.1 投诉押金

| 投诉类型 | 押金标准 | 说明 |
|---------|---------|------|
| **普通投诉** | 10 DUST | 一般违规内容 |
| **紧急投诉** | 50 DUST | 严重违法内容（快速通道） |
| **批量投诉** | 20 DUST | 同一内容多人投诉 |

#### 2.2.2 留言押金

| 留言类型 | 押金标准 | 说明 |
|---------|---------|------|
| **普通留言** | 1 DUST | 一般留言 |
| **长留言** | 2 DUST | 超过500字符 |
| **带媒体留言** | 3 DUST | 包含图片/视频 |

#### 2.2.3 修改押金

| 修改类型 | 押金标准 | 说明 |
|---------|---------|------|
| **基础信息修改** | 5 DUST | 姓名、日期等 |
| **内容修改** | 10 DUST | 描述、文本等 |
| **媒体修改** | 15 DUST | 图片、视频等 |
| **重要信息修改** | 20 DUST | 主图、分类等 |

### 2.3 公示期标准

| 操作类型 | 公示期 | 说明 |
|---------|--------|------|
| **投诉** | 7天 | 投诉内容公示 |
| **留言** | 3天 | 留言内容公示 |
| **修改** | 7-30天 | 根据修改重要性调整 |

**公示期计算**：
- 公示期开始：操作提交成功时
- 公示期结束：`公示期开始 + 公示期天数`
- 自动检查：每100个块检查一次到期操作

---

## 3. 投诉押金机制

### 3.1 投诉流程

```
用户发现违规内容
    ↓
提交投诉（支付押金 10-50 DUST）
    ↓
投诉进入公示期（7天）
    ↓
公示期内
    ├─ 有其他投诉 → 合并处理
    └─ 无其他投诉 → 继续公示
    ↓
公示期到期
    ├─ 有投诉 → 进入委员会审核
    └─ 无投诉 → 自动退回押金
    ↓
[如果有投诉]
    ↓
委员会审核（2/3 多数投票）
    ├─ 批准 → 罚没押金，执行处理
    └─ 驳回 → 退回押金
```

### 3.2 押金处理

**公示期无投诉**：
- ✅ 自动退回押金（100%）
- ✅ 操作生效（投诉记录保留）

**公示期有投诉**：
- ⏸️ 押金冻结，等待审核结果
- ✅ 审核通过：押金不退，进入罚没流程
- ✅ 审核驳回：退回押金（100%）

### 3.3 投诉押金标准

```rust
// 投诉押金计算
fn calculate_complaint_deposit(
    complaint_type: ComplaintType,
) -> BalanceOf<T> {
    match complaint_type {
        ComplaintType::Normal => 10 * UNIT,      // 10 DUST
        ComplaintType::Emergency => 50 * UNIT,   // 50 DUST
        ComplaintType::Batch => 20 * UNIT,       // 20 DUST
    }
}
```

---

## 4. 留言押金机制

### 4.1 留言流程

```
用户查看内容
    ↓
提交留言（支付押金 1-3 DUST）
    ↓
留言进入公示期（3天）
    ↓
公示期内
    ├─ 有投诉 → 进入投诉处理
    └─ 无投诉 → 继续公示
    ↓
公示期到期
    ├─ 有投诉 → 押金冻结，等待处理
    └─ 无投诉 → 自动退回押金，留言生效
```

### 4.2 押金处理

**公示期无投诉**：
- ✅ 自动退回押金（100%）
- ✅ 留言生效（公开显示）

**公示期有投诉**：
- ⏸️ 押金冻结，留言隐藏
- ✅ 投诉不成立：退回押金，留言恢复显示
- ✅ 投诉成立：罚没押金，留言删除

### 4.3 留言押金标准

```rust
// 留言押金计算
fn calculate_comment_deposit(
    content_length: u32,
    has_media: bool,
) -> BalanceOf<T> {
    let base = 1 * UNIT;  // 1 DUST 基础押金
    
    // 长留言额外押金
    let length_extra = if content_length > 500 {
        1 * UNIT  // +1 DUST
    } else {
        BalanceOf::<T>::zero()
    };
    
    // 带媒体额外押金
    let media_extra = if has_media {
        1 * UNIT  // +1 DUST
    } else {
        BalanceOf::<T>::zero()
    };
    
    base + length_extra + media_extra
}
```

---

## 5. 修改押金机制

### 5.1 修改流程

```
拥有者修改内容
    ↓
提交修改（支付押金 5-20 DUST）
    ↓
修改进入公示期（7-30天）
    ↓
公示期内
    ├─ 有投诉 → 进入投诉处理
    └─ 无投诉 → 继续公示
    ↓
公示期到期
    ├─ 有投诉 → 押金冻结，修改暂停
    └─ 无投诉 → 自动退回押金，修改生效
```

### 5.2 押金处理

**公示期无投诉**：
- ✅ 自动退回押金（100%）
- ✅ 修改生效（内容更新）

**公示期有投诉**：
- ⏸️ 押金冻结，修改暂停
- ✅ 投诉不成立：退回押金，修改生效
- ✅ 投诉成立：罚没押金，修改回滚

### 5.3 修改押金标准

```rust
// 修改押金计算
fn calculate_update_deposit(
    update_type: UpdateType,
) -> BalanceOf<T> {
    match update_type {
        UpdateType::BasicInfo => 5 * UNIT,      // 5 DUST
        UpdateType::Content => 10 * UNIT,       // 10 DUST
        UpdateType::Media => 15 * UNIT,         // 15 DUST
        UpdateType::Important => 20 * UNIT,     // 20 DUST
    }
}
```

### 5.4 修改公示期

| 修改类型 | 公示期 | 说明 |
|---------|--------|------|
| **基础信息** | 7天 | 姓名、日期等 |
| **内容修改** | 14天 | 描述、文本等 |
| **媒体修改** | 21天 | 图片、视频等 |
| **重要信息** | 30天 | 主图、分类等 |

---

## 6. 公示期机制

### 6.1 公示期定义

**公示期**：操作提交后到生效前的一段时间，用于：
- 给予公众监督机会
- 发现和举报违规操作
- 保护内容安全

### 6.2 公示期状态

| 状态 | 说明 | 操作效果 |
|------|------|---------|
| **公示中** | 公示期内 | 操作未生效，可被投诉 |
| **公示到期（无投诉）** | 公示期结束且无投诉 | 自动生效，退回押金 |
| **公示到期（有投诉）** | 公示期结束但有投诉 | 暂停生效，等待处理 |
| **投诉处理中** | 有投诉，等待审核 | 押金冻结，操作暂停 |
| **投诉处理完成** | 审核完成 | 根据结果决定是否生效 |

### 6.3 公示期检查

**自动检查机制**：
- 每100个块检查一次到期操作
- 检查公示期是否到期
- 检查是否有投诉
- 自动处理（退回押金或冻结）

**检查逻辑**：
```rust
fn check_notice_period_expired(
    operation_id: u64,
    now: BlockNumber,
) -> Option<NoticeExpiredResult> {
    let operation = Operations::<T>::get(operation_id)?;
    let notice_end = operation.notice_start + operation.notice_period;
    
    if now >= notice_end {
        // 检查是否有投诉
        let has_complaint = ComplaintsByOperation::<T>::contains_key(operation_id);
        
        if has_complaint {
            Some(NoticeExpiredResult::HasComplaint)
        } else {
            Some(NoticeExpiredResult::NoComplaint)
        }
    } else {
        None
    }
}
```

---

## 7. 押金退回机制

### 7.1 退回条件

#### 7.1.1 自动退回（公示期无投诉）

**条件**：
- 公示期已到期
- 公示期内无任何投诉
- 操作状态为"公示中"

**处理**：
- ✅ 自动释放押金（Holds API）
- ✅ 操作生效
- ✅ 发送退回事件

#### 7.1.2 审核后退回（投诉不成立）

**条件**：
- 公示期内有投诉
- 投诉已进入审核流程
- 委员会审核驳回投诉

**处理**：
- ✅ 释放押金（100%退回）
- ✅ 操作生效
- ✅ 发送退回事件

### 7.2 退回实现

```rust
/// 自动退回押金（公示期无投诉）
fn auto_refund_deposit(
    operation_id: u64,
) -> DispatchResult {
    let operation = Operations::<T>::get(operation_id)
        .ok_or(Error::<T>::OperationNotFound)?;
    
    // 检查公示期是否到期
    let now = <frame_system::Pallet<T>>::block_number();
    let notice_end = operation.notice_start + operation.notice_period;
    ensure!(now >= notice_end, Error::<T>::NoticePeriodNotExpired);
    
    // 检查是否有投诉
    ensure!(
        !ComplaintsByOperation::<T>::contains_key(operation_id),
        Error::<T>::HasComplaint
    );
    
    // 释放押金
    T::Fungible::release(
        &HoldReason::OperationDeposit.into(),
        &operation.operator,
        operation.deposit_amount,
        Precision::Exact,
        Fortitude::Polite,
    )?;
    
    // 更新操作状态
    Operations::<T>::mutate(operation_id, |op| {
        if let Some(op) = op {
            op.status = OperationStatus::Completed;
            op.refunded_at = Some(now);
        }
    });
    
    // 发送事件
    Self::deposit_event(Event::DepositRefunded {
        operation_id,
        operator: operation.operator,
        amount: operation.deposit_amount,
        reason: RefundReason::NoComplaint,
    });
    
    Ok(())
}
```

### 7.3 退回时间

| 场景 | 退回时间 | 说明 |
|------|---------|------|
| **公示期无投诉** | 公示期到期后立即 | 自动退回 |
| **投诉不成立** | 审核驳回后立即 | 审核后退回 |
| **投诉成立** | 不退回 | 押金罚没 |

---

## 8. 完整流程设计

### 8.1 投诉操作流程

```
用户提交投诉
    ↓
支付押金（10-50 DUST）
    ↓
冻结押金（Holds API）
    ↓
投诉进入公示期（7天）
    ↓
公示期内
    ├─ 有其他投诉 → 合并处理
    └─ 无其他投诉 → 继续公示
    ↓
公示期到期
    ├─ 有投诉 → 进入委员会审核
    │   ├─ 批准 → 罚没押金，执行处理
    │   └─ 驳回 → 退回押金（100%）
    └─ 无投诉 → 自动退回押金（100%），投诉记录保留
```

### 8.2 留言操作流程

```
用户提交留言
    ↓
支付押金（1-3 DUST）
    ↓
冻结押金（Holds API）
    ↓
留言进入公示期（3天）
    ↓
公示期内
    ├─ 有投诉 → 留言隐藏，押金冻结
    └─ 无投诉 → 继续公示
    ↓
公示期到期
    ├─ 有投诉 → 等待审核结果
    │   ├─ 投诉不成立 → 退回押金，留言恢复显示
    │   └─ 投诉成立 → 罚没押金，留言删除
    └─ 无投诉 → 自动退回押金（100%），留言生效
```

### 8.3 修改操作流程

```
拥有者提交修改
    ↓
支付押金（5-20 DUST）
    ↓
冻结押金（Holds API）
    ↓
修改进入公示期（7-30天）
    ↓
公示期内
    ├─ 有投诉 → 修改暂停，押金冻结
    └─ 无投诉 → 继续公示
    ↓
公示期到期
    ├─ 有投诉 → 等待审核结果
    │   ├─ 投诉不成立 → 退回押金，修改生效
    │   └─ 投诉成立 → 罚没押金，修改回滚
    └─ 无投诉 → 自动退回押金（100%），修改生效
```

---

## 9. 技术实现方案

### 9.1 存储设计

#### 9.1.1 操作记录存储

```rust
/// 操作记录（投诉/留言/修改）
#[pallet::storage]
pub type Operations<T: Config> = StorageMap<
    _,
    Blake2_128Concat,
    u64,  // operation_id
    OperationRecord<T::AccountId, BalanceOf<T>, BlockNumberFor<T>>,
    OptionQuery,
>;

/// 操作记录结构
#[derive(Encode, Decode, Clone, PartialEq, Eq, TypeInfo, MaxEncodedLen)]
pub struct OperationRecord<AccountId, Balance, BlockNumber> {
    pub operator: AccountId,           // 操作者
    pub operation_type: OperationType, // 操作类型（投诉/留言/修改）
    pub target_domain: u8,             // 目标域（1=grave, 2=deceased等）
    pub target_id: u64,                // 目标ID
    pub deposit_amount: Balance,       // 押金金额
    pub notice_start: BlockNumber,     // 公示期开始
    pub notice_period: BlockNumber,    // 公示期时长
    pub status: OperationStatus,        // 操作状态
    pub refunded_at: Option<BlockNumber>, // 退回时间
}
```

#### 9.1.2 投诉索引

```rust
/// 按操作索引的投诉（用于检查是否有投诉）
#[pallet::storage]
pub type ComplaintsByOperation<T: Config> = StorageMap<
    _,
    Blake2_128Concat,
    u64,  // operation_id
    BoundedVec<u64, ConstU32<100>>,  // complaint_ids
    ValueQuery,
>;
```

#### 9.1.3 到期操作队列

```rust
/// 到期操作队列（按区块索引）
#[pallet::storage]
pub type ExpiredOperations<T: Config> = StorageMap<
    _,
    Blake2_128Concat,
    BlockNumberFor<T>,
    BoundedVec<u64, ConstU32<100>>,  // operation_ids
    ValueQuery,
>;
```

### 9.2 接口设计

#### 9.2.1 提交投诉（带押金和公示期）

```rust
/// 提交投诉（带押金和公示期）
#[pallet::call_index(10)]
pub fn submit_complaint_with_deposit(
    origin: OriginFor<T>,
    domain: u8,
    target: u64,
    action: u8,
    evidence_cid: Vec<u8>,
    complaint_type: ComplaintType,
) -> DispatchResult {
    let who = ensure_signed(origin)?;
    
    // 1. 计算押金
    let deposit = Self::calculate_complaint_deposit(complaint_type);
    
    // 2. 检查余额
    let balance = T::Fungible::balance(&who);
    ensure!(balance >= deposit, Error::<T>::InsufficientBalance);
    
    // 3. 冻结押金
    T::Fungible::hold(
        &HoldReason::ComplaintDeposit.into(),
        &who,
        deposit,
    )?;
    
    // 4. 创建操作记录
    let operation_id = NextOperationId::<T>::mutate(|n| {
        let id = *n;
        *n = id.saturating_add(1);
        id
    });
    
    let now = <frame_system::Pallet<T>>::block_number();
    let notice_period = 7 * DAYS;  // 7天公示期
    
    let operation = OperationRecord {
        operator: who.clone(),
        operation_type: OperationType::Complaint,
        target_domain: domain,
        target_id: target,
        deposit_amount: deposit,
        notice_start: now,
        notice_period,
        status: OperationStatus::InNotice,
        refunded_at: None,
    };
    
    Operations::<T>::insert(operation_id, operation);
    
    // 5. 添加到到期队列
    let notice_end = now + notice_period;
    ExpiredOperations::<T>::try_mutate(notice_end, |ops| {
        ops.try_push(operation_id).map_err(|_| Error::<T>::TooMany)
    })?;
    
    // 6. 提交投诉（调用现有接口）
    Self::do_submit_complaint(
        who,
        domain,
        target,
        action,
        evidence_cid,
        Some(operation_id),  // 关联操作ID
    )?;
    
    // 7. 发送事件
    Self::deposit_event(Event::ComplaintSubmittedWithDeposit {
        operation_id,
        who,
        deposit,
        notice_end,
    });
    
    Ok(())
}
```

#### 9.2.2 提交留言（带押金和公示期）

```rust
/// 提交留言（带押金和公示期）
#[pallet::call_index(11)]
pub fn submit_comment_with_deposit(
    origin: OriginFor<T>,
    domain: u8,
    target: u64,
    content: Vec<u8>,
    media_cid: Option<Vec<u8>>,
) -> DispatchResult {
    let who = ensure_signed(origin)?;
    
    // 1. 计算押金
    let content_length = content.len() as u32;
    let has_media = media_cid.is_some();
    let deposit = Self::calculate_comment_deposit(content_length, has_media);
    
    // 2. 冻结押金
    T::Fungible::hold(
        &HoldReason::CommentDeposit.into(),
        &who,
        deposit,
    )?;
    
    // 3. 创建操作记录
    let operation_id = NextOperationId::<T>::mutate(|n| {
        let id = *n;
        *n = id.saturating_add(1);
        id
    });
    
    let now = <frame_system::Pallet<T>>::block_number();
    let notice_period = 3 * DAYS;  // 3天公示期
    
    let operation = OperationRecord {
        operator: who.clone(),
        operation_type: OperationType::Comment,
        target_domain: domain,
        target_id: target,
        deposit_amount: deposit,
        notice_start: now,
        notice_period,
        status: OperationStatus::InNotice,
        refunded_at: None,
    };
    
    Operations::<T>::insert(operation_id, operation);
    
    // 4. 添加到到期队列
    let notice_end = now + notice_period;
    ExpiredOperations::<T>::try_mutate(notice_end, |ops| {
        ops.try_push(operation_id).map_err(|_| Error::<T>::TooMany)
    })?;
    
    // 5. 保存留言（隐藏状态，公示期后生效）
    Comments::<T>::insert(operation_id, CommentRecord {
        operator: who.clone(),
        domain,
        target,
        content,
        media_cid,
        status: CommentStatus::InNotice,
        created_at: now,
    });
    
    // 6. 发送事件
    Self::deposit_event(Event::CommentSubmittedWithDeposit {
        operation_id,
        who,
        deposit,
        notice_end,
    });
    
    Ok(())
}
```

#### 9.2.3 提交修改（带押金和公示期）

```rust
/// 提交修改（带押金和公示期）
#[pallet::call_index(12)]
pub fn submit_update_with_deposit(
    origin: OriginFor<T>,
    domain: u8,
    target: u64,
    update_data: Vec<u8>,
    update_type: UpdateType,
) -> DispatchResult {
    let who = ensure_signed(origin)?;
    
    // 1. 权限检查（必须是拥有者）
    Self::ensure_owner(&who, domain, target)?;
    
    // 2. 计算押金和公示期
    let deposit = Self::calculate_update_deposit(update_type);
    let notice_period = Self::get_notice_period_for_update(update_type);
    
    // 3. 冻结押金
    T::Fungible::hold(
        &HoldReason::UpdateDeposit.into(),
        &who,
        deposit,
    )?;
    
    // 4. 创建操作记录
    let operation_id = NextOperationId::<T>::mutate(|n| {
        let id = *n;
        *n = id.saturating_add(1);
        id
    });
    
    let now = <frame_system::Pallet<T>>::block_number();
    
    let operation = OperationRecord {
        operator: who.clone(),
        operation_type: OperationType::Update,
        target_domain: domain,
        target_id: target,
        deposit_amount: deposit,
        notice_start: now,
        notice_period,
        status: OperationStatus::InNotice,
        refunded_at: None,
    };
    
    Operations::<T>::insert(operation_id, operation);
    
    // 5. 保存修改（待生效状态）
    PendingUpdates::<T>::insert(operation_id, UpdateRecord {
        operator: who.clone(),
        domain,
        target,
        update_data,
        update_type,
        status: UpdateStatus::InNotice,
        created_at: now,
    });
    
    // 6. 添加到到期队列
    let notice_end = now + notice_period;
    ExpiredOperations::<T>::try_mutate(notice_end, |ops| {
        ops.try_push(operation_id).map_err(|_| Error::<T>::TooMany)
    })?;
    
    // 7. 发送事件
    Self::deposit_event(Event::UpdateSubmittedWithDeposit {
        operation_id,
        who,
        deposit,
        notice_end,
    });
    
    Ok(())
}
```

### 9.3 自动处理机制

#### 9.3.1 公示期到期检查（Hook）

```rust
#[pallet::hooks]
impl<T: Config> Hooks<BlockNumberFor<T>> for Pallet<T> {
    fn on_finalize(now: BlockNumberFor<T>) {
        // 每100个块检查一次（避免频繁检查）
        if now % 100u32.into() != 0u32.into() {
            return;
        }
        
        // 获取到期的操作列表
        if let Some(operation_ids) = ExpiredOperations::<T>::get(now) {
            for operation_id in operation_ids.iter() {
                // 处理到期操作
                let _ = Self::process_expired_operation(*operation_id, now);
            }
            
            // 清理已处理的到期队列
            ExpiredOperations::<T>::remove(now);
        }
    }
}

/// 处理到期操作
fn process_expired_operation(
    operation_id: u64,
    now: BlockNumberFor<T>,
) -> DispatchResult {
    let operation = Operations::<T>::get(operation_id)
        .ok_or(Error::<T>::OperationNotFound)?;
    
    // 检查是否有投诉
    let has_complaint = ComplaintsByOperation::<T>::contains_key(operation_id);
    
    if has_complaint {
        // 有投诉：冻结押金，等待处理
        Operations::<T>::mutate(operation_id, |op| {
            if let Some(op) = op {
                op.status = OperationStatus::PendingComplaint;
            }
        });
        
        Self::deposit_event(Event::OperationPendingComplaint {
            operation_id,
            reason: b"Has complaint, waiting for review".to_vec(),
        });
    } else {
        // 无投诉：自动退回押金，操作生效
        Self::auto_refund_and_apply(operation_id)?;
    }
    
    Ok(())
}
```

#### 9.3.2 自动退回和应用

```rust
/// 自动退回押金并应用操作
fn auto_refund_and_apply(
    operation_id: u64,
) -> DispatchResult {
    let operation = Operations::<T>::get(operation_id)
        .ok_or(Error::<T>::OperationNotFound)?;
    
    // 1. 释放押金
    T::Fungible::release(
        &HoldReason::OperationDeposit.into(),
        &operation.operator,
        operation.deposit_amount,
        Precision::Exact,
        Fortitude::Polite,
    )?;
    
    // 2. 根据操作类型应用操作
    match operation.operation_type {
        OperationType::Complaint => {
            // 投诉：记录保留，但不执行处理
            // （投诉需要委员会审核才能执行）
        },
        OperationType::Comment => {
            // 留言：生效（公开显示）
            Comments::<T>::mutate(operation_id, |comment| {
                if let Some(c) = comment {
                    c.status = CommentStatus::Active;
                }
            });
        },
        OperationType::Update => {
            // 修改：生效（应用修改）
            Self::apply_update(operation_id)?;
        },
    }
    
    // 3. 更新操作状态
    let now = <frame_system::Pallet<T>>::block_number();
    Operations::<T>::mutate(operation_id, |op| {
        if let Some(op) = op {
            op.status = OperationStatus::Completed;
            op.refunded_at = Some(now);
        }
    });
    
    // 4. 发送事件
    Self::deposit_event(Event::DepositRefunded {
        operation_id,
        operator: operation.operator,
        amount: operation.deposit_amount,
        reason: RefundReason::NoComplaint,
    });
    
    Ok(())
}
```

### 9.4 投诉关联机制

```rust
/// 投诉时关联操作ID
fn submit_complaint_against_operation(
    origin: OriginFor<T>,
    operation_id: u64,
    evidence_cid: Vec<u8>,
) -> DispatchResult {
    let who = ensure_signed(origin)?;
    
    // 1. 检查操作是否存在
    let operation = Operations::<T>::get(operation_id)
        .ok_or(Error::<T>::OperationNotFound)?;
    
    // 2. 检查操作是否在公示期内
    let now = <frame_system::Pallet<T>>::block_number();
    let notice_end = operation.notice_start + operation.notice_period;
    ensure!(now <= notice_end, Error::<T>::NoticePeriodExpired);
    
    // 3. 提交投诉（调用现有投诉接口）
    let complaint_id = Self::do_submit_complaint(
        who.clone(),
        operation.target_domain,
        operation.target_id,
        0,  // action（针对操作本身的投诉）
        evidence_cid,
        Some(operation_id),  // 关联操作ID
    )?;
    
    // 4. 关联投诉到操作
    ComplaintsByOperation::<T>::try_mutate(operation_id, |complaints| {
        complaints.try_push(complaint_id).map_err(|_| Error::<T>::TooMany)
    })?;
    
    // 5. 更新操作状态
    Operations::<T>::mutate(operation_id, |op| {
        if let Some(op) = op {
            op.status = OperationStatus::HasComplaint;
        }
    });
    
    // 6. 发送事件
    Self::deposit_event(Event::ComplaintAgainstOperation {
        operation_id,
        complaint_id,
        who,
    });
    
    Ok(())
}
```

---

## 10. 合理性分析

### 10.1 押金机制合理性

#### 10.1.1 押金标准合理性

**投诉押金（10-50 DUST）**：
- ✅ **合理**：防止恶意投诉，提高投诉质量
- ✅ **分级合理**：紧急投诉更高押金，防止滥用
- ✅ **成本适中**：10 DUST 对普通用户可接受

**留言押金（1-3 DUST）**：
- ✅ **合理**：防止垃圾留言，提高留言质量
- ✅ **分级合理**：长留言和带媒体留言更高押金
- ✅ **成本低**：1 DUST 门槛低，鼓励正常留言

**修改押金（5-20 DUST）**：
- ✅ **合理**：防止恶意修改，保护内容安全
- ✅ **分级合理**：重要修改更高押金
- ✅ **成本适中**：5 DUST 对拥有者可接受

#### 10.1.2 押金作用合理性

**预防作用**：
- ✅ 提高恶意操作成本
- ✅ 筛选真实用户
- ✅ 减少垃圾内容

**激励作用**：
- ✅ 鼓励合规操作（无投诉自动退回）
- ✅ 提高操作质量
- ✅ 增强用户责任感

### 10.2 公示期机制合理性

#### 10.2.1 公示期时长合理性

**投诉公示期（7天）**：
- ✅ **合理**：给予足够时间发现和举报
- ✅ **效率平衡**：不会过长影响用户体验
- ✅ **标准统一**：与其他平台类似

**留言公示期（3天）**：
- ✅ **合理**：留言影响较小，3天足够
- ✅ **快速响应**：不影响正常交流
- ✅ **成本低**：押金低，公示期短

**修改公示期（7-30天）**：
- ✅ **合理**：根据重要性调整公示期
- ✅ **保护充分**：重要修改30天公示期
- ✅ **灵活适应**：不同修改类型不同公示期

#### 10.2.2 公示期作用合理性

**监督作用**：
- ✅ 给予公众监督机会
- ✅ 发现违规操作
- ✅ 保护内容安全

**保护作用**：
- ✅ 保护内容不被恶意修改
- ✅ 给予投诉时间
- ✅ 提高内容质量

### 10.3 自动退回机制合理性

#### 10.3.1 退回条件合理性

**公示期无投诉自动退回**：
- ✅ **合理**：鼓励合规操作
- ✅ **公平**：无违规不应罚没押金
- ✅ **激励**：提高用户合规意愿

**投诉不成立退回**：
- ✅ **合理**：保护用户权益
- ✅ **公平**：不应因恶意投诉损失押金
- ✅ **平衡**：平衡投诉者和被投诉者权益

#### 10.3.2 退回时机合理性

**立即退回**：
- ✅ **合理**：公示期到期立即退回，不拖延
- ✅ **效率**：自动化处理，无需人工干预
- ✅ **透明**：链上可查，公开透明

---

## 11. 可行性分析

### 11.1 技术可行性

#### 11.1.1 现有基础设施

**优势**：
- ✅ 已有 Holds API（押金管理）
- ✅ 已有申诉系统（投诉处理）
- ✅ 已有公示期机制（`NoticeDefaultBlocks`）
- ✅ 已有自动执行机制（Hook）

**需要开发**：
- ⚠️ 操作记录存储（1周）
- ⚠️ 公示期检查机制（3天）
- ⚠️ 自动退回机制（3天）
- ⚠️ 投诉关联机制（2天）

**技术难度**：**中** ⭐⭐⭐

**实现时间**：**2-3 周**

#### 11.1.2 技术实现难点

**难点1：公示期检查性能**
- **问题**：大量操作需要定期检查
- **解决**：使用队列机制，按区块索引
- **优化**：每100个块检查一次，批量处理

**难点2：投诉关联**
- **问题**：需要关联投诉到操作
- **解决**：使用索引存储关联关系
- **优化**：双向索引，快速查询

**难点3：自动退回**
- **问题**：需要自动释放押金
- **解决**：使用 Holds API 的 release
- **优化**：批量处理，减少Gas费用

### 11.2 经济可行性

#### 11.2.1 成本分析

**开发成本**：
- 开发时间：2-3 周
- 测试时间：1 周
- 总计：3-4 周

**运营成本**：
- 链上存储：低（仅存储元数据）
- Gas 费用：由用户支付押金覆盖
- 维护成本：低（自动化流程）

#### 11.2.2 收益分析

**直接收益**：
- 押金资金池（可用于其他用途）
- 减少恶意操作，降低维护成本
- 提高内容质量

**间接收益**：
- 提高用户信任
- 增强平台合规性
- 减少人工审核成本

**结论**：**经济可行** ✅

### 11.3 社会可行性

#### 11.3.1 用户接受度

**优势**：
- 押金机制提高内容质量
- 公示期保护内容安全
- 无投诉自动退回，鼓励合规

**挑战**：
- 押金门槛可能影响用户参与
- 公示期可能影响操作速度

**应对**：
- 设置合理的押金标准（1-50 DUST）
- 公示期根据操作类型调整（3-30天）
- 完善前端 UI/UX，清晰说明机制

**结论**：**社会可行** ✅

---

## 12. 风险评估与应对

### 12.1 技术风险

#### 12.1.1 公示期检查性能风险

**风险**：大量操作导致检查性能下降

**应对**：
- 使用队列机制，按区块索引
- 每100个块检查一次，避免频繁检查
- 批量处理，限制单次处理数量

#### 12.1.2 押金退回失败风险

**风险**：自动退回失败导致用户损失

**应对**：
- 失败重试机制（最多3次）
- 手动退回接口（治理权限）
- 事件记录和监控

### 12.2 经济风险

#### 12.2.1 押金门槛过高风险

**风险**：押金过高影响用户参与

**应对**：
- 设置合理的押金标准
- 支持动态调整（治理提案）
- 提供押金减免机制（信用等级）

#### 12.2.2 恶意投诉风险

**风险**：恶意投诉导致押金被冻结

**应对**：
- 投诉需要委员会审核
- 恶意投诉罚没投诉者押金
- 黑名单机制

### 12.3 治理风险

#### 12.3.1 公示期滥用风险

**风险**：恶意延长公示期

**应对**：
- 公示期固定，不可修改
- 治理可调整公示期标准（全民投票）
- 透明记录，社区监督

---

## 13. 实施路线图

### 13.1 Phase 1：基础机制（1周）

**目标**：实现押金和公示期基础机制

**任务**：
1. ✅ 设计操作记录存储结构
2. ✅ 实现押金冻结（Holds API）
3. ✅ 实现公示期检查机制
4. ✅ 实现到期队列

**交付物**：
- 操作记录存储
- 押金管理接口
- 公示期检查Hook

### 13.2 Phase 2：自动退回（3天）

**目标**：实现自动退回机制

**任务**：
1. ✅ 实现公示期到期检查
2. ✅ 实现自动退回逻辑
3. ✅ 实现操作生效逻辑
4. ✅ 测试自动退回流程

**交付物**：
- 自动退回接口
- 操作生效逻辑
- 测试用例

### 13.3 Phase 3：投诉关联（2天）

**目标**：实现投诉关联机制

**任务**：
1. ✅ 实现投诉关联存储
2. ✅ 实现投诉关联接口
3. ✅ 实现投诉处理后的押金处理
4. ✅ 测试投诉关联流程

**交付物**：
- 投诉关联接口
- 押金处理逻辑
- 测试用例

### 13.4 Phase 4：接口集成（1周）

**目标**：集成到现有接口

**任务**：
1. ✅ 扩展投诉接口（带押金和公示期）
2. ✅ 扩展留言接口（带押金和公示期）
3. ✅ 扩展修改接口（带押金和公示期）
4. ✅ 前端UI集成

**交付物**：
- 扩展接口
- 前端页面
- 使用文档

### 13.5 Phase 5：测试和优化（1周）

**目标**：全面测试和性能优化

**任务**：
1. ✅ 单元测试和集成测试
2. ✅ 性能测试和优化
3. ✅ 安全审计
4. ✅ 文档完善

**交付物**：
- 测试报告
- 性能优化报告
- 用户使用指南

---

## 14. 总结

### 14.1 方案优势

1. ✅ **全面保护**：投诉、留言、修改都有押金和公示期
2. ✅ **自动处理**：公示期到期自动退回，无需人工干预
3. ✅ **透明公正**：所有流程链上可查，公开透明
4. ✅ **激励合规**：无投诉自动退回，鼓励合规操作
5. ✅ **灵活适应**：不同操作类型不同押金和公示期

### 14.2 核心机制

- **押金担保**：所有操作需要支付押金
- **公示期保护**：操作后进入公示期，给予监督机会
- **自动退回**：公示期无投诉自动退回押金
- **投诉关联**：公示期内投诉关联到操作

### 14.3 实施建议

1. **优先实施 Phase 1-2**：基础机制最重要
2. **逐步完善**：根据实际使用情况优化
3. **社区参与**：鼓励社区参与治理
4. **持续监控**：监控系统运行情况

### 14.4 预期效果

- **恶意操作减少**：押金机制提高恶意操作成本
- **内容质量提升**：公示期保护内容安全
- **用户信任增强**：透明公正的流程
- **维护成本降低**：自动化处理减少人工干预

---

## 15. 附录

### 15.1 押金标准参考

| 操作类型 | 押金范围 | 公示期 | 说明 |
|---------|---------|--------|------|
| 投诉 | 10-50 DUST | 7天 | 根据投诉类型调整 |
| 留言 | 1-3 DUST | 3天 | 根据留言长度和媒体调整 |
| 修改 | 5-20 DUST | 7-30天 | 根据修改重要性调整 |

### 15.2 相关文档

- [基于押金的内容治理方案](./基于押金的内容治理方案.md)
- [非法内容投诉治理方案](./非法内容投诉治理方案.md)
- [pallet-stardust-appeals README](../pallets/stardust-appeals/README.md)

### 15.3 联系方式

- **技术问题**：开发团队
- **治理问题**：内容委员会
- **紧急情况**：Root 账户

---

**文档维护**: Stardust 开发团队  
**最后更新**: 2025-11-13  
**版本**: v1.0.0

