# 🎴 重命名快速参考卡片

**📅 日期**: 2025-10-29  
**📊 当前进度**: 40% ✅ 链端完成，⏳ 前端待处理

---

## 🚀 三种执行方式

### 方式1️⃣: 一键自动执行（最快）
```bash
cd /home/xiaodong/文档/memopark
chmod +x docs/重命名脚本-前端项目.sh
./docs/重命名脚本-前端项目.sh
```
**耗时**: 15-20分钟  
**特点**: 全自动，带确认提示

---

### 方式2️⃣: 分步手动执行（安全）
```bash
# 步骤1: 备份
cd /home/xiaodong/文档/memopark
git add -A && git commit -m "Checkpoint"
git tag -a before-frontend-rename -m "备份"

# 步骤2: 重命名目录
mv memopark-dapp stardust-dapp
mv memopark-governance stardust-governance
mv memopark-gov stardust-gov
mv memopark-gov-scripts stardust-gov-scripts

# 步骤3: 更新配置（每个目录执行）
cd stardust-dapp
sed -i 's/"memopark-dapp"/"stardust-dapp"/g' package.json
sed -i 's/<title>Memopark<\/title>/<title>Stardust<\/title>/g' index.html

# 步骤4: 提交
cd /home/xiaodong/文档/memopark
git add -A && git commit -m "阶段6完成"
```
**耗时**: 30-40分钟  
**特点**: 可控，便于调试

---

### 方式3️⃣: 继续AI辅助（灵活）
告诉我："继续执行前端重命名"，我会自动完成所有步骤。

**耗时**: 20-30分钟  
**特点**: 自动化 + 实时反馈

---

## 📋 关键命令速查

### 备份和回滚
```bash
# 创建备份
git tag -a backup-$(date +%Y%m%d) -m "备份点"

# 查看所有备份点
git tag -l | grep -E "before-|backup-"

# 回滚到某个备份点
git reset --hard <tag-name>

# 回滚示例
git reset --hard before-frontend-rename
```

### 验证命令
```bash
# 检查重命名是否完成
ls -d stardust-* 2>/dev/null | wc -l  # 应该≥4

# 检查旧目录是否还存在
ls -d memopark-* 2>/dev/null | wc -l  # 应该=0

# 检查package.json
grep '"name":' stardust-dapp/package.json

# 检查index.html
grep '<title>' stardust-dapp/index.html
```

### 编译验证
```bash
# Runtime编译
cd /home/xiaodong/文档/memopark
cargo check -p stardust-runtime

# Node完整编译
cargo build --release -p stardust-node

# 前端编译
cd stardust-dapp && npm install && npm run build
```

### 启动测试
```bash
# 启动节点（终端1）
./target/release/stardust-node --dev --tmp

# 启动前端（终端2）
cd stardust-dapp && npm run dev
```

---

## 🎯 核心文件替换规则

### ✅ 必须替换
| 原文本 | 新文本 | 位置 |
|--------|--------|------|
| `"memopark-dapp"` | `"stardust-dapp"` | package.json |
| `<title>Memopark</title>` | `<title>Stardust</title>` | index.html |
| 目录名 `memopark-*` | `stardust-*` | 文件系统 |

### 🟡 建议替换
| 原文本 | 新文本 | 位置 |
|--------|--------|------|
| `纪念园` | `星尘宇宙` | UI中文 |
| `MEMO代币` | `DUST代币` | UI中文 |
| `Memopark` | `Stardust` | 文档、注释 |

### 🔴 建议保留
| 原文本 | 说明 | 原因 |
|--------|------|------|
| `memoAmount` | API变量名 | 避免破坏API兼容性 |
| `useMemo` | React Hook | React标准Hook名称 |
| API路径中的memo | 后端接口 | 保持前后端一致 |

---

## ⚠️ 重要提示

### 执行顺序
1. ✅ 链端（已完成）
2. ⏳ 前端（进行中）
3. ⏳ 文档
4. ⏳ 根目录（最后）

### 注意事项
- 📌 每步完成后立即验证
- 📌 定期提交Git更改
- 📌 保留完整的node_modules（第一次）
- 📌 最后再执行根目录重命名

### 风险点
- ⚠️ 批量替换可能误替换API变量
- ⚠️ 前端编译可能需要重新install
- ⚠️ 根目录重命名会改变路径

---

## 📊 进度追踪

```
阶段1: 准备工作          [████████████] 100% ✅
阶段2: Pallet重命名      [████████████] 100% ✅
阶段3: Workspace配置     [████████████] 100% ✅
阶段4: Runtime代码       [████████████] 100% ✅
阶段5: 全局代码替换      [██░░░░░░░░░░]  20% ⏳
阶段6: 前端项目          [░░░░░░░░░░░░]   0% ⏳ ← 当前
阶段7: 文档更新          [░░░░░░░░░░░░]   0% ⏳
阶段8: 根目录重命名      [░░░░░░░░░░░░]   0% ⏳
阶段9: 编译验证          [░░░░░░░░░░░░]   0% ⏳
阶段10: 功能测试         [░░░░░░░░░░░░]   0% ⏳
```

**总进度**: 40%  
**已用时**: ~1.5小时  
**预计剩余**: 2.5-3.5小时

---

## 🆘 紧急回滚

### 场景1: 前端重命名后悔了
```bash
cd /home/xiaodong/文档/memopark
git reset --hard before-frontend-rename
```

### 场景2: 全部重来
```bash
cd /home/xiaodong/文档/memopark
git reset --hard before-rename-to-stardust
```

### 场景3: 只回退某些文件
```bash
# 恢复单个目录
git restore stardust-dapp/

# 恢复package.json
git restore stardust-dapp/package.json
```

---

## 🎉 完成检查清单

完成后验证以下内容：

### 目录结构
- [ ] `stardust-dapp/` 存在
- [ ] `stardust-governance/` 存在
- [ ] `stardust-gov/` 存在
- [ ] `stardust-gov-scripts/` 存在
- [ ] `memopark-*` 目录全部不存在

### 配置文件
- [ ] package.json中的包名已更新
- [ ] index.html中的标题已更新
- [ ] README.md中的项目名已更新

### 编译验证
- [ ] Runtime编译通过
- [ ] Node编译通过
- [ ] 前端编译通过

### 功能测试
- [ ] 节点可以启动
- [ ] 前端可以连接
- [ ] 基础功能正常

---

## 📞 获取帮助

遇到问题？

**选项1**: 查看详细指南
```bash
cat docs/重命名手动操作指南.md
```

**选项2**: 查看脚本源码
```bash
cat docs/重命名脚本-前端项目.sh
```

**选项3**: 继续AI辅助
告诉我具体的错误信息，我会帮您解决。

---

**🚀 准备好了吗？选择一种方式开始吧！**

