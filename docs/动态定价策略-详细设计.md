# 动态定价策略 - 详细设计

## 🎯 设计目标

实现USD锚定的动态押金定价策略，确保押金价值稳定在$10 USD等值的MEMO。

### 核心需求

1. **美元锚定**：押金价值稳定为$10 USD
2. **实时定价**：根据MEMO/USD实时汇率动态计算
3. **风险差异化**：不同操作不同倍数（1.0× ~ 2.0×）
4. **回退机制**：定价失败时使用固定押金

---

## 🏗️ 架构设计

### 组件关系

```
┌─────────────────────────────────────────────┐
│  pallet-pricing                              │
│  ├─ OtcPriceAggregate                       │
│  └─ BridgePriceAggregate                    │
│      ↓ 提供MEMO/USD价格                      │
└─────────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────────┐
│  DepositPolicy trait                         │
│  ├─ calc_deposit()                          │
│  └─ USD锚定计算逻辑                          │
│      ↓ 计算押金金额                          │
└─────────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────────┐
│  pallet-deposits                             │
│  └─ reserve_deposit()                       │
│      ↓ 使用计算的押金金额                     │
└─────────────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────────┐
│  pallet-stardust-appeals                        │
│  └─ submit_appeal()                         │
│      ↓ 冻结押金                               │
└─────────────────────────────────────────────┘
```

---

## 📐 计算公式

### 基础公式

```
押金金额（DUST） = 基准金额（USD） / MEMO价格（USD） × 风险倍数
```

### 详细公式

```rust
// 1. 获取MEMO/USD价格
let memo_price_usd = get_memo_price_from_pricing();
// 返回值精度：10^6 (USDT精度)
// 例如：$2.5 USD = 2_500_000

// 2. 基准押金
let base_usd = 10_000_000u64;  // $10 USD (精度10^6)

// 3. 确定风险倍数
let multiplier = get_multiplier(domain, action);
// 返回值：100-200 (1.0× ~ 2.0×)

// 4. 计算MEMO数量
let memo_amount = (base_usd as u128)
    .saturating_mul(1_000_000u128)  // 转为MEMO精度(10^12)
    .saturating_mul(multiplier as u128)
    .saturating_div(memo_price_usd as u128)
    .saturating_div(100);

// 例子：
// 基准 = $10
// MEMO价格 = $2.5
// 倍数 = 2.0×
// 结果 = $10 / $2.5 × 2.0 = 8 DUST
```

---

## 🎚️ 风险倍数表

### Domain/Action倍数配置

```rust
// 倍数定义（100 = 1.0×, 150 = 1.5×, 200 = 2.0×）
fn get_multiplier(domain: u8, action: u8) -> u16 {
    match (domain, action) {
        // ===== Domain 1: grave (墓地) =====
        (1, 10) => 100,  // 清空封面: 1.0×
        (1, 11) => 200,  // 转让所有权: 2.0× (高风险)
        (1, 12) => 100,  // 设置限制: 1.0×
        (1, 13) => 150,  // 软删除: 1.5×
        (1, 14) => 100,  // 恢复: 1.0×
        
        // ===== Domain 2: deceased (逝者档案) =====
        (2, 1) => 100,   // 设置可见性: 1.0×
        (2, 2) => 150,   // 清空主图: 1.5×
        (2, 3) => 150,   // 设置主图: 1.5×
        (2, 4) => 200,   // 转让所有权: 2.0× (高风险)
        
        // ===== Domain 3: offerings (供奉品) =====
        (3, 1) => 100,   // 普通供奉品审核: 1.0×
        (3, 2) => 150,   // 特殊供奉品审核: 1.5×
        
        // ===== Domain 4: deceased-media (媒体) =====
        (4, 30) => 100,  // 隐藏媒体: 1.0×
        (4, 31) => 150,  // 替换URI: 1.5×
        (4, 32) => 150,  // 冻结视频集: 1.5×
        
        // ===== Domain 5: deceased-text (文本) =====
        (5, 20) => 150,  // 删除生平: 1.5×
        (5, 21) => 150,  // 删除悼词: 1.5×
        (5, 22) => 100,  // 编辑生平: 1.0×
        (5, 23) => 100,  // 编辑悼词: 1.0×
        
        // ===== 未来域 =====
        // (6, x) => pet-game
        // (7, x) => market
        
        // ===== 默认 =====
        _ => 100,  // 未定义的操作，默认1.0×
    }
}
```

### 倍数设计理念

| 风险等级 | 倍数 | 适用场景 | 说明 |
|---------|------|---------|------|
| **低风险** | 1.0× | 可逆操作 | 设置可见性、编辑文本等 |
| **中风险** | 1.5× | 部分可逆 | 删除内容、替换图片等 |
| **高风险** | 2.0× | 不可逆操作 | 转让所有权等 |

---

## 💻 代码实现

### 1. DepositPolicy Trait定义

```rust
// pallets/deposits/src/lib.rs

/// 动态押金策略Trait
pub trait DepositPolicy {
    type AccountId;
    type Balance;
    
    /// 计算押金金额
    ///
    /// 参数：
    /// - who: 押金提供者（可用于用户级差异化）
    /// - purpose: 押金用途
    ///
    /// 返回：
    /// - Some(amount): 计算的押金金额
    /// - None: 无法计算（回退到固定押金）
    fn calc_deposit(
        who: &Self::AccountId,
        purpose: &DepositPurpose,
    ) -> Option<Self::Balance>;
}
```

### 2. USD锚定策略实现

```rust
// runtime/src/configs/mod.rs

/// USD锚定动态押金策略
pub struct UsdAnchoredDepositPolicy;

impl pallet_deposits::DepositPolicy for UsdAnchoredDepositPolicy {
    type AccountId = AccountId;
    type Balance = Balance;
    
    fn calc_deposit(
        _who: &AccountId,
        purpose: &DepositPurpose,
    ) -> Option<Balance> {
        // 1. 获取MEMO/USD价格
        let memo_price_usd = Self::get_memo_price_usd()?;
        
        // 2. 基准押金：$10 USD
        let base_usd = 10_000_000u64;  // $10 with 6 decimals
        
        // 3. 根据用途确定倍数
        let multiplier = Self::get_multiplier_for_purpose(purpose);
        
        // 4. 计算MEMO数量
        let memo_amount = (base_usd as u128)
            .saturating_mul(1_000_000u128)  // 转为MEMO精度(10^12)
            .saturating_mul(multiplier as u128)
            .saturating_div(memo_price_usd as u128)
            .saturating_div(100);
        
        Some(memo_amount)
    }
}

impl UsdAnchoredDepositPolicy {
    /// 获取MEMO/USD价格
    fn get_memo_price_usd() -> Option<u64> {
        // 从pallet-pricing获取聚合价格
        let otc_price = pallet_pricing::Pallet::<Runtime>::otc_price_aggregate()?;
        let bridge_price = pallet_pricing::Pallet::<Runtime>::bridge_price_aggregate()?;
        
        // 取平均值
        let avg_price = (otc_price.price_usdt + bridge_price.price_usdt) / 2;
        
        // 价格有效性检查
        if avg_price > 0 && avg_price < u64::MAX {
            Some(avg_price)
        } else {
            None
        }
    }
    
    /// 根据用途获取倍数
    fn get_multiplier_for_purpose(purpose: &DepositPurpose) -> u16 {
        match purpose {
            DepositPurpose::Appeal { domain, action, .. } => {
                Self::get_multiplier(*domain, *action)
            },
            DepositPurpose::OfferingReview { kind_code, .. } => {
                match kind_code {
                    1 => 100,  // 普通供奉品: 1.0×
                    2 => 150,  // 特殊供奉品: 1.5×
                    _ => 100,
                }
            },
            DepositPurpose::TextComplaint { .. } => 100,
            DepositPurpose::MediaComplaint { .. } => 100,
            DepositPurpose::Custom { .. } => 100,
        }
    }
    
    /// 获取domain/action倍数
    fn get_multiplier(domain: u8, action: u8) -> u16 {
        match (domain, action) {
            // grave
            (1, 10) => 100,  // 清空封面: 1.0×
            (1, 11) => 200,  // 转让所有权: 2.0×
            (1, 12) => 100,  // 设置限制: 1.0×
            (1, 13) => 150,  // 软删除: 1.5×
            (1, 14) => 100,  // 恢复: 1.0×
            
            // deceased
            (2, 1) => 100,   // 设置可见性: 1.0×
            (2, 2) => 150,   // 清空主图: 1.5×
            (2, 3) => 150,   // 设置主图: 1.5×
            (2, 4) => 200,   // 转让所有权: 2.0×
            
            // deceased-media
            (4, 30) => 100,  // 隐藏媒体: 1.0×
            (4, 31) => 150,  // 替换URI: 1.5×
            (4, 32) => 150,  // 冻结视频集: 1.5×
            
            // deceased-text
            (5, 20) => 150,  // 删除生平: 1.5×
            (5, 21) => 150,  // 删除悼词: 1.5×
            (5, 22) => 100,  // 编辑生平: 1.0×
            (5, 23) => 100,  // 编辑悼词: 1.0×
            
            // 默认
            _ => 100,  // 1.0×
        }
    }
}
```

### 3. Runtime配置

```rust
// runtime/src/lib.rs 或 runtime/src/configs/mod.rs

impl pallet_deposits::Config for Runtime {
    type RuntimeEvent = RuntimeEvent;
    type Currency = Balances;
    type ReleaseOrigin = EnsureRoot<AccountId>;
    type SlashOrigin = EnsureRoot<AccountId>;
    type MaxDepositsPerAccount = ConstU32<100>;
    
    /// 使用USD锚定动态定价策略
    type DepositPolicy = UsdAnchoredDepositPolicy;
}
```

### 4. 使用动态定价

```rust
// pallet-stardust-appeals/src/lib.rs

#[pallet::call_index(0)]
pub fn submit_appeal(...) -> DispatchResult {
    let who = ensure_signed(origin)?;
    
    // 1. 创建押金用途
    let purpose = DepositPurpose::Appeal {
        appeal_id: id,
        domain,
        target,
        action,
    };
    
    // 2. 使用动态定价计算押金
    let deposit_amount = T::DepositPolicy::calc_deposit(&who, &purpose)
        .unwrap_or(T::FallbackDeposit::get());  // 回退到固定押金
    
    // 3. 冻结押金
    let deposit_id = T::DepositManager::reserve(
        &who,
        deposit_amount,
        purpose,
    )?;
    
    // 4. 创建申诉记录
    // ...
}
```

---

## 🧪 测试用例

### 单元测试

```rust
#[cfg(test)]
mod tests {
    use super::*;
    
    #[test]
    fn dynamic_pricing_works_basic() {
        new_test_ext().execute_with(|| {
            // Mock: MEMO价格 = $2 USD
            MockPricing::set_price(2_000_000);  // 精度10^6
            
            let alice = 1;
            let purpose = DepositPurpose::Appeal {
                appeal_id: 1,
                domain: 1,  // grave
                target: 123,
                action: 10,  // clear_cover, 倍数1.0×
            };
            
            // 计算：$10 / $2 × 1.0 = 5 DUST
            let deposit = UsdAnchoredDepositPolicy::calc_deposit(&alice, &purpose);
            assert_eq!(deposit, Some(5_000_000_000_000));  // 5 DUST (精度10^12)
        });
    }
    
    #[test]
    fn dynamic_pricing_with_multiplier() {
        new_test_ext().execute_with(|| {
            MockPricing::set_price(2_000_000);
            
            let alice = 1;
            let purpose = DepositPurpose::Appeal {
                appeal_id: 1,
                domain: 1,
                target: 123,
                action: 11,  // transfer_owner, 倍数2.0×
            };
            
            // 计算：$10 / $2 × 2.0 = 10 DUST
            let deposit = UsdAnchoredDepositPolicy::calc_deposit(&alice, &purpose);
            assert_eq!(deposit, Some(10_000_000_000_000));
        });
    }
    
    #[test]
    fn dynamic_pricing_fallback_on_error() {
        new_test_ext().execute_with(|| {
            // Mock: 价格获取失败
            MockPricing::set_price_error();
            
            let alice = 1;
            let purpose = DepositPurpose::Appeal {
                appeal_id: 1,
                domain: 1,
                target: 123,
                action: 10,
            };
            
            // 返回None，触发fallback
            let deposit = UsdAnchoredDepositPolicy::calc_deposit(&alice, &purpose);
            assert_eq!(deposit, None);
        });
    }
}
```

---

## 📊 价格示例

### 不同MEMO价格下的押金

| MEMO价格 | 1.0× | 1.5× | 2.0× |
|---------|------|------|------|
| $0.5 USD | 20 DUST | 30 DUST | 40 DUST |
| $1.0 USD | 10 DUST | 15 DUST | 20 DUST |
| $2.0 USD | 5 DUST | 7.5 DUST | 10 DUST |
| $5.0 USD | 2 DUST | 3 DUST | 4 DUST |
| $10.0 USD | 1 DUST | 1.5 DUST | 2 DUST |

**观察**：
- MEMO价格上涨 → 押金数量减少
- MEMO价格下跌 → 押金数量增加
- ✅ 押金价值始终稳定在$10-$20 USD

---

## 🔒 安全考虑

### 1. 价格异常处理

```rust
fn get_memo_price_usd() -> Option<u64> {
    let otc_price = pallet_pricing::Pallet::<Runtime>::otc_price_aggregate()?;
    let bridge_price = pallet_pricing::Pallet::<Runtime>::bridge_price_aggregate()?;
    
    // 有效性检查
    if otc_price.price_usdt == 0 || bridge_price.price_usdt == 0 {
        return None;  // 价格为0，回退
    }
    
    // 偏差检查（防止价格操纵）
    let diff = otc_price.price_usdt.abs_diff(bridge_price.price_usdt);
    let avg = (otc_price.price_usdt + bridge_price.price_usdt) / 2;
    
    // 如果两个价格偏差>50%，拒绝使用
    if diff > avg / 2 {
        return None;  // 价格偏差过大，回退
    }
    
    Some(avg)
}
```

### 2. 溢出保护

```rust
let memo_amount = (base_usd as u128)
    .saturating_mul(1_000_000u128)  // ✅ 使用saturating防止溢出
    .saturating_mul(multiplier as u128)
    .saturating_div(memo_price_usd as u128)
    .saturating_div(100);
```

### 3. Fallback机制

```rust
// 计算失败时使用固定押金
let deposit_amount = T::DepositPolicy::calc_deposit(&who, &purpose)
    .unwrap_or(T::FallbackDeposit::get());
```

---

## 📈 性能优化

### 缓存策略

```rust
// 缓存最近的价格（避免每次都查询）
#[pallet::storage]
pub type CachedMemoPrice<T: Config> = StorageValue<
    _,
    (u64, BlockNumberFor<T>),  // (price, block_number)
    OptionQuery,
>;

fn get_memo_price_usd() -> Option<u64> {
    let current_block = <frame_system::Pallet<T>>::block_number();
    
    // 如果缓存未过期（<10块），使用缓存
    if let Some((cached_price, cached_block)) = CachedMemoPrice::<T>::get() {
        if current_block - cached_block < 10u32.into() {
            return Some(cached_price);
        }
    }
    
    // 否则重新获取
    let price = /* ... */;
    CachedMemoPrice::<T>::put((price, current_block));
    Some(price)
}
```

---

## ✅ 实施清单

- [ ] 定义DepositPolicy trait
- [ ] 实现UsdAnchoredDepositPolicy
- [ ] 集成pallet-pricing
- [ ] 编写单元测试
- [ ] 性能测试
- [ ] 安全审查
- [ ] 文档完善

---

**预计完成时间**：Week 2 (3天)  
**复杂度**：⭐⭐⭐⭐（较高）

---

*更新时间：2025-10-25*

