# 基于押金的内容治理方案

> **文档版本**: v1.0.0  
> **更新日期**: 2025-11-13  
> **适用范围**: 墓地、陵园、逝者创建者上传的内容  
> **核心机制**: 创建押金 + 投诉罚没 + 押金不足锁定 + 公共治理

---

## 📋 目录

1. [方案概述](#方案概述)
2. [核心机制设计](#核心机制设计)
3. [押金管理机制](#押金管理机制)
4. [投诉罚没机制](#投诉罚没机制)
5. [押金不足锁定机制](#押金不足锁定机制)
6. [公共治理处理机制](#公共治理处理机制)
7. [完整流程设计](#完整流程设计)
8. [技术实现方案](#技术实现方案)
9. [可行性分析](#可行性分析)
10. [风险评估与应对](#风险评估与应对)
11. [实施路线图](#实施路线图)

---

## 1. 方案概述

### 1.1 核心设计理念

**"押金担保 + 投诉罚没 + 锁定保护"** 三重机制：

1. **创建押金**：内容创建者必须支付押金才能创建内容
2. **投诉罚没**：公众投诉成立后，罚没部分或全部押金
3. **押金不足锁定**：押金不足时，内容被锁定，拥有者无法修改
4. **公共治理**：押金不足时，只能通过公共治理处理

### 1.2 设计目标

1. ✅ **预防违规**：押金机制提高违规成本
2. ✅ **快速响应**：公众可便捷投诉，自动罚没押金
3. ✅ **保护内容**：押金不足时锁定，防止恶意修改
4. ✅ **治理兜底**：公共治理处理复杂情况
5. ✅ **透明公正**：所有流程链上可查

### 1.3 适用对象

- **墓地（Grave）**：创建墓地时支付押金
- **陵园（Park）**：创建陵园时支付押金
- **逝者档案（Deceased）**：创建逝者档案时支付押金
- **逝者媒体（Media）**：上传媒体内容时支付押金
- **逝者文本（Text）**：上传文本内容时支付押金

---

## 2. 核心机制设计

### 2.1 押金生命周期

```
内容创建
    ↓
支付押金（冻结在托管账户）
    ↓
内容正常使用
    ↓
[如果投诉成立]
    ↓
罚没押金（部分或全部）
    ↓
[如果押金不足]
    ↓
内容锁定（拥有者无法修改）
    ↓
[补齐押金]
    ↓
内容解锁（恢复正常）
```

### 2.2 押金状态定义

| 状态 | 押金余额 | 内容状态 | 拥有者权限 |
|------|---------|---------|-----------|
| **正常** | ≥ 最低押金 | 正常 | 可修改 |
| **警告** | < 最低押金，≥ 0 | 正常 | 可修改（警告提示） |
| **锁定** | = 0 | 锁定 | **不可修改** |
| **负值** | < 0（罚没超额） | 锁定 | **不可修改** |

### 2.3 押金标准

#### 2.3.1 最低押金要求

| 内容类型 | 最低押金 | 说明 |
|---------|---------|------|
| 墓地（Grave） | 100 DUST | 基础押金 |
| 陵园（Park） | 500 DUST | 大型内容，更高押金 |
| 逝者档案（Deceased） | 50 DUST | 基础押金 |
| 逝者媒体（Media） | 20 DUST | 单条媒体 |
| 逝者文本（Text） | 10 DUST | 单条文本 |

#### 2.3.2 动态押金调整

**调整因素**：
- 内容规模（媒体数量、文本长度）
- 历史违规记录（违规次数）
- 用户信用等级（信用分）

**计算公式**：
```rust
// 基础押金
let base_deposit = get_base_deposit(content_type);

// 规模系数（媒体数量、文本长度等）
let scale_factor = calculate_scale_factor(content);

// 违规系数（历史违规次数）
let violation_factor = get_violation_factor(owner);

// 信用系数（用户信用分）
let credit_factor = get_credit_factor(owner);

// 最终押金
let final_deposit = base_deposit 
    * scale_factor 
    * violation_factor 
    / credit_factor;
```

---

## 3. 押金管理机制

### 3.1 创建时押金支付

#### 3.1.1 支付流程

```
用户创建内容
    ↓
系统计算所需押金
    ↓
检查用户余额是否足够
    ↓
冻结押金到托管账户（Holds API）
    ↓
创建内容并关联押金记录
    ↓
内容可正常使用
```

#### 3.1.2 押金冻结

**使用 Holds API**：
```rust
// 冻结押金
T::Fungible::hold(
    &HoldReason::ContentDeposit.into(),
    &who,
    deposit_amount,
)?;

// 记录押金信息
ContentDeposits::<T>::insert(
    content_id,
    DepositInfo {
        owner: who.clone(),
        amount: deposit_amount,
        minimum: min_deposit,
        locked: false,
        created_at: now,
    },
);
```

### 3.2 押金查询与监控

#### 3.2.1 押金余额查询

**接口设计**：
```rust
/// 查询内容押金余额
pub fn get_content_deposit(content_id: u64) -> Option<BalanceOf<T>> {
    let deposit_info = ContentDeposits::<T>::get(content_id)?;
    let held = T::Fungible::balance_on_hold(
        &HoldReason::ContentDeposit.into(),
        &deposit_info.owner,
    );
    Some(held)
}

/// 检查押金是否充足
pub fn is_deposit_sufficient(content_id: u64) -> bool {
    if let Some(balance) = Self::get_content_deposit(content_id) {
        let min_deposit = Self::get_minimum_deposit(content_id);
        balance >= min_deposit
    } else {
        false
    }
}
```

#### 3.2.2 押金不足预警

**预警机制**：
- 押金余额 < 最低押金的 150% → 发送警告事件
- 押金余额 < 最低押金 → 发送严重警告事件
- 押金余额 = 0 → 自动锁定内容

---

## 4. 投诉罚没机制

### 4.1 投诉流程

```
公众发现违规内容
    ↓
提交投诉（提供证据 + 支付投诉押金）
    ↓
内容委员会审核（2/3 多数投票）
    ├─ 批准 → 进入罚没流程
    └─ 驳回 → 退还投诉押金
    ↓
计算罚没金额
    ├─ 轻微违规：10-30% 押金
    ├─ 一般违规：30-50% 押金
    ├─ 严重违规：50-100% 押金
    └─ 极端违规：100% 押金 + 额外罚款
    ↓
执行罚没（从内容押金中扣除）
    ↓
分配罚没资金
    ├─ 50% 给投诉者（奖励）
    ├─ 30% 给内容委员会（审核报酬）
    └─ 20% 给国库（系统维护）
```

### 4.2 罚没金额计算

#### 4.2.1 违规等级与罚没比例

| 违规等级 | 罚没比例 | 适用场景 |
|---------|---------|---------|
| **轻微** | 10-30% | 不当内容、轻微侵权 |
| **一般** | 30-50% | 虚假信息、一般侵权 |
| **严重** | 50-100% | 严重侵权、商业欺诈 |
| **极端** | 100% + 额外 | 严重违法、恐怖主义 |

#### 4.2.2 罚没计算逻辑

```rust
/// 计算罚没金额
fn calculate_slash_amount(
    content_id: u64,
    violation_level: ViolationLevel,
) -> Result<BalanceOf<T>, DispatchError> {
    let deposit_info = ContentDeposits::<T>::get(content_id)
        .ok_or(Error::<T>::ContentNotFound)?;
    
    let current_balance = Self::get_content_deposit(content_id)
        .unwrap_or_default();
    
    // 根据违规等级确定罚没比例
    let slash_percent = match violation_level {
        ViolationLevel::Minor => 20,      // 20%
        ViolationLevel::Moderate => 40,    // 40%
        ViolationLevel::Severe => 75,     // 75%
        ViolationLevel::Extreme => 100,   // 100%
    };
    
    // 计算罚没金额
    let slash_amount = current_balance * slash_percent / 100;
    
    // 极端违规：额外罚款（最低押金的 50%）
    let extra_fine = if violation_level == ViolationLevel::Extreme {
        deposit_info.minimum * 50 / 100
    } else {
        BalanceOf::<T>::zero()
    };
    
    Ok(slash_amount.saturating_add(extra_fine))
}
```

### 4.3 罚没执行

#### 4.3.1 罚没流程

```rust
/// 执行押金罚没
fn slash_content_deposit(
    content_id: u64,
    slash_amount: BalanceOf<T>,
) -> DispatchResult {
    let deposit_info = ContentDeposits::<T>::get(content_id)
        .ok_or(Error::<T>::ContentNotFound)?;
    
    // 从 Holds 中释放并转移到国库
    T::Fungible::release(
        &HoldReason::ContentDeposit.into(),
        &deposit_info.owner,
        slash_amount,
        Precision::Exact,
        Fortitude::Polite,
    )?;
    
    // 转移到国库
    T::Fungible::transfer(
        &deposit_info.owner,
        &T::TreasuryAccount::get(),
        slash_amount,
        ExistenceRequirement::KeepAlive,
    )?;
    
    // 更新押金记录
    let new_balance = Self::get_content_deposit(content_id)
        .unwrap_or_default();
    
    // 检查是否需要锁定
    if new_balance < deposit_info.minimum {
        Self::lock_content(content_id)?;
    }
    
    Ok(())
}
```

#### 4.3.2 罚没资金分配

```rust
/// 分配罚没资金
fn distribute_slashed_funds(
    slash_amount: BalanceOf<T>,
    reporter: &T::AccountId,
) -> DispatchResult {
    // 50% 给投诉者
    let reporter_reward = slash_amount * 50 / 100;
    T::Fungible::transfer(
        &T::TreasuryAccount::get(),
        reporter,
        reporter_reward,
        ExistenceRequirement::KeepAlive,
    )?;
    
    // 30% 给内容委员会（按投票权重分配）
    let committee_reward = slash_amount * 30 / 100;
    Self::distribute_committee_rewards(committee_reward)?;
    
    // 20% 留在国库
    // （已从用户账户转移到国库，无需额外操作）
    
    Ok(())
}
```

---

## 5. 押金不足锁定机制

### 5.1 锁定触发条件

#### 5.1.1 自动锁定

**触发条件**：
1. 押金余额 < 最低押金要求
2. 押金余额 = 0
3. 押金余额 < 0（罚没超额）

**锁定时机**：
- 罚没执行后立即检查
- 定期检查（每 100 个块检查一次）

#### 5.1.2 锁定状态

**锁定效果**：
- ✅ **内容可见**：内容仍然可见，但标记为"押金不足"
- ❌ **禁止修改**：拥有者无法修改任何内容
- ❌ **禁止删除**：拥有者无法删除内容
- ✅ **允许查看**：公众可以正常查看内容
- ✅ **允许投诉**：公众可以继续投诉

### 5.2 锁定实现

#### 5.2.1 锁定存储

```rust
/// 内容锁定状态
#[pallet::storage]
pub type ContentLocks<T: Config> = StorageMap<
    _,
    Blake2_128Concat,
    u64,  // content_id
    LockInfo<BlockNumberFor<T>>,
    OptionQuery,
>;

/// 锁定信息
#[derive(Encode, Decode, Clone, PartialEq, Eq, TypeInfo, MaxEncodedLen)]
pub struct LockInfo<BlockNumber> {
    pub locked_at: BlockNumber,
    pub reason: LockReason,
    pub required_deposit: Balance,
    pub current_deposit: Balance,
}
```

#### 5.2.2 锁定检查

```rust
/// 检查并锁定内容（如果需要）
fn check_and_lock_content(content_id: u64) -> DispatchResult {
    let deposit_info = ContentDeposits::<T>::get(content_id)
        .ok_or(Error::<T>::ContentNotFound)?;
    
    let current_balance = Self::get_content_deposit(content_id)
        .unwrap_or_default();
    
    // 检查是否需要锁定
    if current_balance < deposit_info.minimum {
        // 锁定内容
        let now = <frame_system::Pallet<T>>::block_number();
        let lock_info = LockInfo {
            locked_at: now,
            reason: LockReason::InsufficientDeposit,
            required_deposit: deposit_info.minimum,
            current_deposit: current_balance,
        };
        
        ContentLocks::<T>::insert(content_id, lock_info);
        
        // 发送锁定事件
        Self::deposit_event(Event::ContentLocked {
            content_id,
            reason: LockReason::InsufficientDeposit,
            required: deposit_info.minimum,
            current: current_balance,
        });
    }
    
    Ok(())
}
```

#### 5.2.3 权限检查

```rust
/// 检查内容是否被锁定
pub fn is_content_locked(content_id: u64) -> bool {
    ContentLocks::<T>::contains_key(content_id)
}

/// 检查拥有者是否有修改权限
pub fn can_owner_modify(
    content_id: u64,
    owner: &T::AccountId,
) -> Result<bool, DispatchError> {
    // 检查拥有者权限
    let actual_owner = Self::get_content_owner(content_id)?;
    if actual_owner != *owner {
        return Ok(false);
    }
    
    // 检查是否被锁定
    if Self::is_content_locked(content_id) {
        return Ok(false);
    }
    
    Ok(true)
}
```

---

## 6. 公共治理处理机制

### 6.1 治理触发条件

#### 6.1.1 需要治理的情况

**以下情况必须通过公共治理处理**：

1. **押金不足且无法补齐**
   - 拥有者账户余额不足
   - 拥有者账户已注销
   - 拥有者长期不活跃

2. **严重违规内容**
   - 极端违规（恐怖主义、严重违法）
   - 需要立即删除
   - 押金不足以覆盖罚没

3. **争议内容**
   - 投诉和答辩存在争议
   - 需要高级治理判断

4. **系统异常**
   - 押金计算错误
   - 锁定状态异常
   - 其他系统问题

### 6.2 治理提案流程

#### 6.2.1 提案类型

| 提案类型 | 适用场景 | 权限要求 |
|---------|---------|---------|
| **处理押金不足** | 押金不足且无法补齐 | 内容委员会 2/3 多数 |
| **强制删除内容** | 极端违规内容 | 内容委员会 2/3 多数 + Root |
| **解除锁定** | 误锁定或已补齐 | 内容委员会 2/3 多数 |
| **调整押金标准** | 修改最低押金要求 | 全民投票 |

#### 6.2.2 提案流程

```
发现问题
    ↓
提交治理提案（内容委员会成员）
    ↓
提案公示期（7天）
    ↓
内容委员会投票（2/3 多数通过）
    ├─ 通过 → 执行提案
    └─ 驳回 → 提案结束
    ↓
执行治理动作
    ├─ 强制删除内容
    ├─ 解除锁定
    ├─ 调整押金
    └─ 其他治理动作
```

### 6.3 治理动作实现

#### 6.3.1 强制删除内容

```rust
/// 治理强制删除内容（押金不足且无法补齐）
#[pallet::call_index(20)]
pub fn governance_force_delete(
    origin: OriginFor<T>,
    content_id: u64,
    reason_cid: Vec<u8>,
) -> DispatchResult {
    // 验证治理权限
    T::GovernanceOrigin::ensure_origin(origin)?;
    
    // 检查内容是否被锁定
    ensure!(
        Self::is_content_locked(content_id),
        Error::<T>::ContentNotLocked
    );
    
    // 释放剩余押金（如果有）
    if let Some(deposit_info) = ContentDeposits::<T>::get(content_id) {
        let remaining = Self::get_content_deposit(content_id)
            .unwrap_or_default();
        
        if !remaining.is_zero() {
            // 释放押金到国库
            T::Fungible::release(
                &HoldReason::ContentDeposit.into(),
                &deposit_info.owner,
                remaining,
                Precision::Exact,
                Fortitude::Polite,
            )?;
        }
    }
    
    // 删除内容（调用目标 Pallet 的治理接口）
    T::Router::execute_governance_action(
        content_id,
        GovernanceAction::ForceDelete,
    )?;
    
    // 清理锁定记录
    ContentLocks::<T>::remove(content_id);
    ContentDeposits::<T>::remove(content_id);
    
    // 发送事件
    Self::deposit_event(Event::ContentForceDeleted {
        content_id,
        reason_cid,
    });
    
    Ok(())
}
```

#### 6.3.2 解除锁定

```rust
/// 治理解除内容锁定
#[pallet::call_index(21)]
pub fn governance_unlock(
    origin: OriginFor<T>,
    content_id: u64,
    reason_cid: Vec<u8>,
) -> DispatchResult {
    // 验证治理权限
    T::GovernanceOrigin::ensure_origin(origin)?;
    
    // 检查内容是否被锁定
    ensure!(
        Self::is_content_locked(content_id),
        Error::<T>::ContentNotLocked
    );
    
    // 解除锁定
    ContentLocks::<T>::remove(content_id);
    
    // 发送事件
    Self::deposit_event(Event::ContentUnlocked {
        content_id,
        reason_cid,
    });
    
    Ok(())
}
```

#### 6.3.3 调整押金标准

```rust
/// 治理调整最低押金标准
#[pallet::call_index(22)]
pub fn governance_set_min_deposit(
    origin: OriginFor<T>,
    content_type: u8,
    new_min_deposit: BalanceOf<T>,
) -> DispatchResult {
    // 验证治理权限（需要全民投票）
    T::GovernanceOrigin::ensure_origin(origin)?;
    
    // 更新最低押金标准
    MinDeposits::<T>::insert(content_type, new_min_deposit);
    
    // 检查所有现有内容，如果押金不足则锁定
    Self::check_all_contents_deposit()?;
    
    // 发送事件
    Self::deposit_event(Event::MinDepositUpdated {
        content_type,
        new_min_deposit,
    });
    
    Ok(())
}
```

---

## 7. 完整流程设计

### 7.1 内容创建流程

```
用户创建内容（墓地/陵园/逝者）
    ↓
系统计算所需押金
    ├─ 基础押金
    ├─ 规模系数
    ├─ 违规系数
    └─ 信用系数
    ↓
检查用户余额是否足够
    ├─ 足够 → 冻结押金
    └─ 不足 → 拒绝创建
    ↓
冻结押金到托管账户（Holds API）
    ↓
创建内容并关联押金记录
    ↓
内容可正常使用
```

### 7.2 投诉罚没流程

```
公众发现违规内容
    ↓
提交投诉（提供证据 + 支付投诉押金 10 DUST）
    ↓
内容委员会审核（2/3 多数投票，7天内）
    ├─ 批准 → 进入罚没流程
    └─ 驳回 → 退还投诉押金，投诉者损失 30%
    ↓
计算罚没金额（根据违规等级）
    ├─ 轻微：10-30%
    ├─ 一般：30-50%
    ├─ 严重：50-100%
    └─ 极端：100% + 额外罚款
    ↓
执行罚没（从内容押金中扣除）
    ↓
分配罚没资金
    ├─ 50% 给投诉者
    ├─ 30% 给内容委员会
    └─ 20% 给国库
    ↓
检查押金余额
    ├─ 充足 → 内容正常
    └─ 不足 → 锁定内容
```

### 7.3 押金不足处理流程

```
押金余额 < 最低押金
    ↓
自动锁定内容
    ├─ 标记为"押金不足"
    ├─ 禁止拥有者修改
    └─ 允许公众查看和投诉
    ↓
通知拥有者（事件 + 前端提示）
    ↓
拥有者补齐押金
    ├─ 成功 → 自动解锁
    └─ 失败 → 进入治理流程
    ↓
[如果无法补齐]
    ↓
内容委员会提交治理提案
    ↓
委员会投票（2/3 多数）
    ├─ 通过 → 执行治理动作
    │   ├─ 强制删除内容
    │   ├─ 解除锁定（特殊情况）
    │   └─ 其他治理动作
    └─ 驳回 → 保持锁定状态
```

### 7.4 押金补齐流程

```
拥有者发现内容被锁定
    ↓
查看押金余额和最低要求
    ↓
计算需要补齐的金额
    ↓
支付押金（冻结到托管账户）
    ↓
系统检查押金是否充足
    ├─ 充足 → 自动解锁
    └─ 不足 → 继续锁定
    ↓
内容解锁，恢复正常使用
```

---

## 8. 技术实现方案

### 8.1 存储设计

#### 8.1.1 押金存储

```rust
/// 内容押金信息
#[pallet::storage]
pub type ContentDeposits<T: Config> = StorageMap<
    _,
    Blake2_128Concat,
    u64,  // content_id
    DepositInfo<T::AccountId, BalanceOf<T>>,
    OptionQuery,
>;

/// 押金信息
#[derive(Encode, Decode, Clone, PartialEq, Eq, TypeInfo, MaxEncodedLen)]
pub struct DepositInfo<AccountId, Balance> {
    pub owner: AccountId,
    pub initial_amount: Balance,      // 初始押金
    pub minimum: Balance,              // 最低押金要求
    pub current_balance: Balance,      // 当前余额（缓存）
    pub created_at: BlockNumber,
    pub violation_count: u32,          // 违规次数
}
```

#### 8.1.2 锁定存储

```rust
/// 内容锁定状态
#[pallet::storage]
pub type ContentLocks<T: Config> = StorageMap<
    _,
    Blake2_128Concat,
    u64,  // content_id
    LockInfo<BlockNumberFor<T>, BalanceOf<T>>,
    OptionQuery,
>;

/// 锁定信息
#[derive(Encode, Decode, Clone, PartialEq, Eq, TypeInfo, MaxEncodedLen)]
pub struct LockInfo<BlockNumber, Balance> {
    pub locked_at: BlockNumber,
    pub reason: LockReason,
    pub required_deposit: Balance,
    pub current_deposit: Balance,
    pub governance_proposal_id: Option<u64>,  // 关联的治理提案
}
```

#### 8.1.3 最低押金标准

```rust
/// 最低押金标准（按内容类型）
#[pallet::storage]
pub type MinDeposits<T: Config> = StorageMap<
    _,
    Blake2_128Concat,
    u8,  // content_type
    BalanceOf<T>,
    ValueQuery,
>;
```

### 8.2 接口设计

#### 8.2.1 创建内容接口（扩展）

```rust
/// 创建内容并支付押金（扩展现有接口）
fn create_content_with_deposit(
    origin: OriginFor<T>,
    content_data: ContentData,
) -> DispatchResult {
    let who = ensure_signed(origin)?;
    
    // 1. 计算所需押金
    let required_deposit = Self::calculate_deposit(
        &content_data.content_type,
        &content_data,
        &who,
    )?;
    
    // 2. 检查余额
    let balance = T::Fungible::balance(&who);
    ensure!(
        balance >= required_deposit,
        Error::<T>::InsufficientBalance
    );
    
    // 3. 冻结押金
    T::Fungible::hold(
        &HoldReason::ContentDeposit.into(),
        &who,
        required_deposit,
    )?;
    
    // 4. 创建内容（调用目标 Pallet）
    let content_id = T::ContentCreator::create_content(
        &who,
        content_data,
    )?;
    
    // 5. 记录押金信息
    let now = <frame_system::Pallet<T>>::block_number();
    let deposit_info = DepositInfo {
        owner: who.clone(),
        initial_amount: required_deposit,
        minimum: Self::get_min_deposit(content_data.content_type),
        current_balance: required_deposit,
        created_at: now,
        violation_count: 0,
    };
    
    ContentDeposits::<T>::insert(content_id, deposit_info);
    
    // 6. 发送事件
    Self::deposit_event(Event::ContentCreatedWithDeposit {
        content_id,
        owner: who,
        deposit: required_deposit,
    });
    
    Ok(())
}
```

#### 8.2.2 补齐押金接口

```rust
/// 补齐内容押金
#[pallet::call_index(10)]
pub fn top_up_deposit(
    origin: OriginFor<T>,
    content_id: u64,
    amount: BalanceOf<T>,
) -> DispatchResult {
    let who = ensure_signed(origin)?;
    
    // 1. 检查拥有者权限
    let deposit_info = ContentDeposits::<T>::get(content_id)
        .ok_or(Error::<T>::ContentNotFound)?;
    ensure!(deposit_info.owner == who, Error::<T>::NotOwner);
    
    // 2. 检查余额
    let balance = T::Fungible::balance(&who);
    ensure!(balance >= amount, Error::<T>::InsufficientBalance);
    
    // 3. 冻结押金
    T::Fungible::hold(
        &HoldReason::ContentDeposit.into(),
        &who,
        amount,
    )?;
    
    // 4. 更新押金记录
    ContentDeposits::<T>::mutate(content_id, |info| {
        if let Some(info) = info {
            info.current_balance = info.current_balance.saturating_add(amount);
        }
    });
    
    // 5. 检查是否可以解锁
    let new_balance = Self::get_content_deposit(content_id)
        .unwrap_or_default();
    let min_deposit = deposit_info.minimum;
    
    if new_balance >= min_deposit && Self::is_content_locked(content_id) {
        // 解锁内容
        ContentLocks::<T>::remove(content_id);
        
        Self::deposit_event(Event::ContentUnlocked {
            content_id,
            reason_cid: b"Deposit topped up".to_vec(),
        });
    }
    
    // 6. 发送事件
    Self::deposit_event(Event::DepositToppedUp {
        content_id,
        who,
        amount,
        new_balance,
    });
    
    Ok(())
}
```

#### 8.2.3 查询接口

```rust
/// 查询内容押金信息
pub fn get_content_deposit_info(
    content_id: u64,
) -> Option<DepositInfo<T::AccountId, BalanceOf<T>>> {
    ContentDeposits::<T>::get(content_id)
}

/// 查询内容是否被锁定
pub fn is_content_locked(content_id: u64) -> bool {
    ContentLocks::<T>::contains_key(content_id)
}

/// 查询需要补齐的押金金额
pub fn get_required_topup(content_id: u64) -> Option<BalanceOf<T>> {
    let deposit_info = ContentDeposits::<T>::get(content_id)?;
    let current = Self::get_content_deposit(content_id)?;
    let minimum = deposit_info.minimum;
    
    if current < minimum {
        Some(minimum.saturating_sub(current))
    } else {
        Some(BalanceOf::<T>::zero())
    }
}
```

### 8.3 事件设计

```rust
#[pallet::event]
pub enum Event<T: Config> {
    /// 内容已创建并支付押金
    ContentCreatedWithDeposit {
        content_id: u64,
        owner: T::AccountId,
        deposit: BalanceOf<T>,
    },
    
    /// 押金已罚没
    DepositSlashed {
        content_id: u64,
        amount: BalanceOf<T>,
        reason: Vec<u8>,
    },
    
    /// 内容已锁定
    ContentLocked {
        content_id: u64,
        reason: LockReason,
        required: BalanceOf<T>,
        current: BalanceOf<T>,
    },
    
    /// 内容已解锁
    ContentUnlocked {
        content_id: u64,
        reason_cid: Vec<u8>,
    },
    
    /// 押金已补齐
    DepositToppedUp {
        content_id: u64,
        who: T::AccountId,
        amount: BalanceOf<T>,
        new_balance: BalanceOf<T>,
    },
    
    /// 治理强制删除内容
    ContentForceDeleted {
        content_id: u64,
        reason_cid: Vec<u8>,
    },
    
    /// 最低押金标准已更新
    MinDepositUpdated {
        content_type: u8,
        new_min_deposit: BalanceOf<T>,
    },
}
```

---

## 9. 可行性分析

### 9.1 技术可行性

#### 9.1.1 现有基础设施

**优势**：
- ✅ 已有 Holds API（押金管理）
- ✅ 已有申诉系统（投诉流程）
- ✅ 已有内容委员会（治理机制）
- ✅ 已有自动执行机制

**需要开发**：
- ⚠️ 押金计算逻辑（1周）
- ⚠️ 锁定机制（3天）
- ⚠️ 治理接口（3天）
- ⚠️ 押金补齐接口（2天）

**技术难度**：**中** ⭐⭐⭐

**实现时间**：**2-3 周**

### 9.2 经济可行性

#### 9.2.1 成本分析

**开发成本**：
- 开发时间：2-3 周
- 测试时间：1 周
- 总计：3-4 周

**运营成本**：
- 链上存储：低（仅存储元数据）
- Gas 费用：由用户支付
- 维护成本：低（自动化流程）

#### 9.2.2 收益分析

**直接收益**：
- 押金资金池（可用于其他用途）
- 罚没资金进入国库（20%）
- 提高内容质量

**间接收益**：
- 减少违规内容
- 提高用户信任
- 增强平台合规性

**结论**：**经济可行** ✅

### 9.3 社会可行性

#### 9.3.1 用户接受度

**优势**：
- 押金机制提高内容质量
- 透明公正的流程
- 给予补齐机会

**挑战**：
- 押金门槛可能影响用户参与
- 需要用户理解机制

**应对**：
- 设置合理的押金标准
- 完善前端 UI/UX
- 提供详细使用指南

**结论**：**社会可行** ✅

---

## 10. 风险评估与应对

### 10.1 技术风险

#### 10.1.1 押金计算错误

**风险**：押金计算错误导致用户损失

**应对**：
- 多重验证机制
- 治理可调整押金
- 审计和监控

#### 10.1.2 锁定状态异常

**风险**：内容被误锁定或无法解锁

**应对**：
- 治理可解除锁定
- 多重检查机制
- 事件记录和审计

### 10.2 经济风险

#### 10.2.1 押金门槛过高

**风险**：押金过高影响用户参与

**应对**：
- 动态调整押金标准
- 支持分期支付（后续优化）
- 信用等级减免（后续优化）

#### 10.2.2 恶意罚没

**风险**：恶意投诉导致押金被罚没

**应对**：
- 委员会审核机制
- 答辩保护机制
- 申诉机制

### 10.3 治理风险

#### 10.3.1 治理滥用

**风险**：治理权限被滥用

**应对**：
- 2/3 多数投票
- 透明记录
- 社区监督

---

## 11. 实施路线图

### 11.1 Phase 1：押金机制（1周）

**目标**：实现内容创建押金机制

**任务**：
1. ✅ 设计押金计算逻辑
2. ✅ 实现押金冻结（Holds API）
3. ✅ 实现押金查询接口
4. ✅ 扩展内容创建接口

**交付物**：
- 押金计算模块
- 押金管理接口
- 内容创建扩展

### 11.2 Phase 2：罚没机制（1周）

**目标**：实现投诉罚没机制

**任务**：
1. ✅ 扩展申诉系统支持押金罚没
2. ✅ 实现罚没金额计算
3. ✅ 实现罚没执行
4. ✅ 实现资金分配

**交付物**：
- 罚没计算模块
- 罚没执行接口
- 资金分配逻辑

### 11.3 Phase 3：锁定机制（3天）

**目标**：实现押金不足锁定机制

**任务**：
1. ✅ 实现锁定检查
2. ✅ 实现锁定存储
3. ✅ 实现权限检查
4. ✅ 实现自动解锁

**交付物**：
- 锁定管理模块
- 权限检查接口
- 自动解锁逻辑

### 11.4 Phase 4：治理机制（3天）

**目标**：实现公共治理处理机制

**任务**：
1. ✅ 实现治理提案接口
2. ✅ 实现强制删除接口
3. ✅ 实现解除锁定接口
4. ✅ 实现押金调整接口

**交付物**：
- 治理接口
- 提案流程
- 执行逻辑

### 11.5 Phase 5：补齐机制（2天）

**目标**：实现押金补齐机制

**任务**：
1. ✅ 实现补齐接口
2. ✅ 实现自动解锁
3. ✅ 实现查询接口
4. ✅ 前端 UI 集成

**交付物**：
- 补齐接口
- 查询接口
- 前端页面

### 11.6 Phase 6：测试和优化（1周）

**目标**：全面测试和性能优化

**任务**：
1. ✅ 单元测试和集成测试
2. ✅ 性能测试和优化
3. ✅ 安全审计
4. ✅ 文档完善

**交付物**：
- 测试报告
- 性能优化报告
- 用户使用指南

---

## 12. 总结

### 12.1 方案优势

1. ✅ **预防违规**：押金机制提高违规成本
2. ✅ **快速响应**：自动罚没和锁定机制
3. ✅ **保护内容**：押金不足时锁定，防止恶意修改
4. ✅ **治理兜底**：公共治理处理复杂情况
5. ✅ **透明公正**：所有流程链上可查

### 12.2 核心机制

- **创建押金**：内容创建者必须支付押金
- **投诉罚没**：公众投诉成立后罚没押金
- **押金不足锁定**：押金不足时锁定内容
- **公共治理**：押金不足时通过治理处理

### 12.3 实施建议

1. **优先实施 Phase 1-2**：押金和罚没机制是核心
2. **逐步完善**：根据实际使用情况优化
3. **社区参与**：鼓励社区参与治理
4. **持续监控**：监控系统运行情况

### 12.4 预期效果

- **违规内容减少**：押金机制提高违规成本
- **响应速度提升**：自动机制快速响应
- **内容质量提升**：押金机制筛选优质内容
- **治理能力增强**：公共治理处理复杂情况

---

## 13. 附录

### 13.1 押金标准参考

| 内容类型 | 基础押金 | 规模系数 | 违规系数 | 信用系数 |
|---------|---------|---------|---------|---------|
| 墓地 | 100 DUST | 1.0-2.0 | 1.0-3.0 | 0.5-1.5 |
| 陵园 | 500 DUST | 1.0-3.0 | 1.0-5.0 | 0.5-1.5 |
| 逝者档案 | 50 DUST | 1.0-1.5 | 1.0-2.0 | 0.5-1.5 |
| 媒体 | 20 DUST | 1.0-1.2 | 1.0-1.5 | 0.5-1.5 |
| 文本 | 10 DUST | 1.0-1.1 | 1.0-1.5 | 0.5-1.5 |

### 13.2 相关文档

- [非法内容投诉治理方案](./非法内容投诉治理方案.md)
- [pallet-stardust-appeals README](../pallets/stardust-appeals/README.md)
- [Holds API 文档](https://docs.substrate.io/reference/how-to-guides/pallet-design/use-holds-api/)

### 13.3 联系方式

- **技术问题**：开发团队
- **治理问题**：内容委员会
- **紧急情况**：Root 账户

---

**文档维护**: Stardust 开发团队  
**最后更新**: 2025-11-13  
**版本**: v1.0.0

