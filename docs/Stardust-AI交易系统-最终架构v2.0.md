# Stardust AI 交易系统 - 最终架构 v2.0（纯 DUST 版）

## 📋 文档版本

- **版本**：v2.0
- **日期**：2025-11-05
- **状态**：已实现
- **作者**：Stardust AI 团队

---

## 目录

- [系统概述](#系统概述)
- [核心特性](#核心特性)
- [技术架构](#技术架构)
- [合约设计](#合约设计)
- [资金流转](#资金流转)
- [安全机制](#安全机制)
- [部署指南](#部署指南)
- [使用示例](#使用示例)

---

## 系统概述

Stardust AI 交易系统 v2.0 是一个**纯 DUST 入口**的去中心化 AI 驱动交易平台，用户仅可以使用 DUST 代币参与 AI 交易，系统自动完成货币转换和资金管理。

### 核心理念

```
用户只需要 DUST → 系统自动处理一切 → 获得 AI 交易收益
```

### 三层架构

```
┌────────────────────────────────────────────────────────────────┐
│                    Stardust AI 交易系统架构                        │
└────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  Layer 1: Stardust 链（Substrate）                            │
├──────────────────────────────────────────────────────────────┤
│  - pallet-dust-bridge: DUST 跨链桥接                          │
│  - pallet-ai-trader: AI 策略管理                              │
│  - 原生 DUST 代币                                             │
│  ┌───────────────────────────────────────────────────┐        │
│  │  用户持有原生 DUST                                  │        │
│  │  ↓                                                │        │
│  │  桥接到 Arbitrum（bridge_to_arbitrum）             │        │
│  │  ↓                                                │        │
│  │  获得 ERC20 DUST                                   │        │
│  └───────────────────────────────────────────────────┘        │
└──────────────────────────────────────────────────────────────┘
                        ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 2: Arbitrum（EVM 兼容）                                │
├──────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐                    │
│  │  DUSTToken.sol  │  │ DUSTBridge.sol  │                    │
│  │  (ERC20)        │◄─│ (桥接)          │                    │
│  └─────────────────┘  └─────────────────┘                    │
│           │                                                   │
│           ▼                                                   │
│  ┌───────────────────────────────────────────────┐           │
│  │  StardustVaultRouter.sol                      │           │
│  │  - depositWithDUST(dustAmount)                │           │
│  │    • DUST → Uniswap → USDC                   │           │
│  │    • USDC → Vault → stUSDC                   │           │
│  │  - withdrawToDUST(stUsdcAmount)               │           │
│  │    • stUSDC → Uniswap → USDC                 │           │
│  │    • USDC → Uniswap → DUST                   │           │
│  └───────────────────────────────────────────────┘           │
│           │                                                   │
│           ▼                                                   │
│  ┌───────────────────────────────────────────────┐           │
│  │  StardustTradingVault.sol                     │           │
│  │  - 持有 USDC 资金                              │           │
│  │  - 发行 stUSDC 份额代币                        │           │
│  │  - OCW 更新 NAV                               │           │
│  │  - 禁止直接提取（仅 DEX）                      │           │
│  └───────────────────────────────────────────────┘           │
│           │                                                   │
│           ▼                                                   │
│  ┌───────────────────────────────────────────────┐           │
│  │  Uniswap V3 流动性池                           │           │
│  │  - DUST / USDC                                │           │
│  │  - stUSDC / USDC                              │           │
│  └───────────────────────────────────────────────┘           │
└──────────────────────────────────────────────────────────────┘
                        ▼
┌──────────────────────────────────────────────────────────────┐
│  Layer 3: Hyperliquid（去中心化衍生品交易所）                   │
├──────────────────────────────────────────────────────────────┤
│  - API Wallet 执行交易                                        │
│  - 主账户控制资金                                             │
│  - OCW 自动化交易                                             │
│  - DeepSeek AI 生成信号                                       │
└──────────────────────────────────────────────────────────────┘
```

---

## 核心特性

### 1. 纯 DUST 入口 ✅

| 特性 | 说明 |
|------|------|
| **单币种入口** | 用户仅需 DUST，无需 USDC |
| **自动转换** | Router 自动完成 DUST ↔ USDC |
| **强化需求** | 增强 DUST 实用性和需求 |

### 2. 锁定-铸造桥接 ✅

| 特性 | 说明 |
|------|------|
| **跨链桥接** | Stardust 原生 DUST ↔ Arbitrum ERC20 DUST |
| **OCW 中继** | 自动化处理桥接请求 |
| **安全保障** | 多签账户 + 治理监督 |

### 3. 份额代币化 ✅

| 特性 | 说明 |
|------|------|
| **stUSDC** | 代表金库份额的 ERC20 代币 |
| **浮动价值** | 价格随 AI 策略表现波动 |
| **DEX 流动性** | Uniswap 提供即时退出 |

### 4. AI 自动化交易 ✅

| 特性 | 说明 |
|------|------|
| **DeepSeek AI** | 生成交易信号 |
| **OCW 执行** | 自动化执行交易 |
| **Hyperliquid** | 去中心化衍生品交易 |

---

## 技术架构

### 合约组件

#### 1. DUSTToken.sol

```solidity
/**
 * @notice Stardust DUST 代币（Arbitrum ERC20 版本）
 * 
 * 核心功能：
 * - 标准 ERC20 代币
 * - 仅桥接合约可铸造/销毁
 * - 支持暂停/恢复
 * 
 * 关键接口：
 * - mint(to, amount, bridgeId) [BRIDGE_ROLE]
 * - burn(from, amount, bridgeId) [BRIDGE_ROLE]
 * - pause() / unpause() [PAUSER_ROLE]
 */
contract DUSTToken is ERC20, AccessControl, Pausable {
    bytes32 public constant BRIDGE_ROLE = keccak256("BRIDGE_ROLE");
    bytes32 public constant PAUSER_ROLE = keccak256("PAUSER_ROLE");
    
    function mint(address to, uint256 amount, bytes32 bridgeId) external;
    function burn(address from, uint256 amount, bytes32 bridgeId) external;
}
```

#### 2. DUSTBridge.sol

```solidity
/**
 * @notice Arbitrum 桥接合约（锁定-铸造模型）
 * 
 * 核心功能：
 * - 正向桥接：铸造 ERC20 DUST
 * - 反向桥接：销毁 ERC20 DUST
 * - 防重放攻击
 * 
 * 关键接口：
 * - mint(bridgeId, to, amount, stardustTxHash) [RELAYER_ROLE]
 * - burnAndBridgeBack(amount, substrateAddress)
 * - setLimits(minAmount, maxAmount) [ADMIN]
 */
contract DUSTBridge is AccessControl, ReentrancyGuard, Pausable {
    mapping(uint64 => bool) public processedBridgeIds;
    
    function mint(...) external onlyRole(RELAYER_ROLE);
    function burnAndBridgeBack(...) external;
}
```

#### 3. StardustTradingVault.sol

```solidity
/**
 * @notice Stardust AI 交易金库
 * 
 * 核心功能：
 * - 接收 USDC，发行 stUSDC
 * - OCW 更新 NAV
 * - 禁止直接提取
 * 
 * 关键接口：
 * - depositFromRouter(user, usdcAmount) [ROUTER_ROLE]
 * - updateNAV(newNAV) [OCW_ROLE]
 * - getSharePrice()
 */
contract StardustTradingVault is ERC20, AccessControl {
    uint256 public totalAssets;  // NAV
    uint256 public performanceFee = 1000; // 10%
    uint256 public managementFee = 200;   // 2%
    
    function depositFromRouter(...) external onlyRole(ROUTER_ROLE);
    function updateNAV(...) external onlyRole(OCW_ROLE);
}
```

#### 4. StardustVaultRouter.sol

```solidity
/**
 * @notice DUST 交易路由（纯 DUST 入口）
 * 
 * 核心功能：
 * - DUST → USDC → Vault → stUSDC
 * - stUSDC → USDC → DUST
 * - Uniswap V3 集成
 * 
 * 关键接口：
 * - depositWithDUST(dustAmount, minUsdcOut)
 * - withdrawToDUST(stUsdcAmount, minDustOut)
 */
contract StardustVaultRouter is AccessControl, ReentrancyGuard {
    ISwapRouter public immutable uniswapRouter;
    
    function depositWithDUST(...) external nonReentrant;
    function withdrawToDUST(...) external nonReentrant;
}
```

---

## 资金流转

### 存入流程

```
┌──────────────────────────────────────────────────────────────┐
│                       存入流程详解                              │
└──────────────────────────────────────────────────────────────┘

Step 1: 用户在 Stardust 链持有原生 DUST
        └─ 余额：1000 DUST

Step 2: 桥接到 Arbitrum
        └─ 调用：api.tx.dustBridge.bridgeToArbitrum(1000 DUST, ethAddress)
        └─ 锁定：1000 原生 DUST → 桥接账户
        └─ OCW：监听事件 → 调用 Arbitrum mint()
        └─ 铸造：1000 ERC20 DUST

Step 3: 存入 AI 交易系统
        └─ 调用：StardustVaultRouter.depositWithDUST(1000 DUST, minUsdcOut)
        └─ Uniswap：1000 DUST → 100 USDC（假设价格 0.1）
        └─ Vault：收到 100 USDC
        └─ 发行：100 stUSDC（假设首次存入，1:1）

Step 4: 用户获得 stUSDC
        └─ 余额：100 stUSDC
        └─ 价值：100 USDC（初始）
        └─ 随 AI 表现浮动
```

### 提取流程

```
┌──────────────────────────────────────────────────────────────┐
│                       提取流程详解                              │
└──────────────────────────────────────────────────────────────┘

Step 1: 用户持有 stUSDC
        └─ 余额：100 stUSDC
        └─ 当前价值：110 USDC（AI 策略盈利）

Step 2: 提取为 DUST
        └─ 调用：StardustVaultRouter.withdrawToDUST(100 stUSDC, minDustOut)
        └─ Uniswap：100 stUSDC → 110 USDC
        └─ Uniswap：110 USDC → 1100 DUST（假设价格 0.1）

Step 3: 用户获得 DUST
        └─ 余额：1100 DUST（ERC20）
        └─ 盈利：+100 DUST

Step 4: 桥接回 Stardust（可选）
        └─ 调用：DUSTBridge.burnAndBridgeBack(1100 DUST, substrateAddr)
        └─ 销毁：1100 ERC20 DUST
        └─ OCW：监听事件 → 调用 Substrate unlock()
        └─ 解锁：1100 原生 DUST
```

---

## 安全机制

### 1. 桥接安全

| 机制 | 说明 |
|------|------|
| **多签账户** | 桥接锁定账户使用 5/3 多签 |
| **治理监督** | 重大操作需社区投票 |
| **防重放** | 记录已处理的桥接 ID |
| **金额限制** | 最小 1 DUST，最大 1M DUST |

### 2. 合约安全

| 机制 | 说明 |
|------|------|
| **访问控制** | 使用 OpenZeppelin AccessControl |
| **重入保护** | 使用 ReentrancyGuard |
| **暂停机制** | 紧急情况可暂停合约 |
| **角色分离** | ADMIN / RELAYER / OCW / PAUSER |

### 3. 交易安全

| 机制 | 说明 |
|------|------|
| **滑点保护** | 最大滑点 3%，可调整 |
| **金额验证** | 最小交换金额 10 DUST |
| **时间限制** | Uniswap 交换 5 分钟deadline |
| **API Wallet** | Hyperliquid 使用 API Wallet |

---

## 部署指南

### 前置条件

1. **Hardhat 环境**
```bash
cd contracts/arbitrum
npm install
```

2. **配置环境变量**
```bash
cp .env.example .env
# 编辑 .env 文件，填入私钥和 API Key
```

3. **编译合约**
```bash
npm run compile
```

### 部署到测试网

```bash
# Arbitrum Sepolia 测试网
npm run deploy:arbitrum-sepolia
```

### 部署到主网

```bash
# Arbitrum 主网
npm run deploy:arbitrum
```

### 验证合约

```bash
npx hardhat verify --network arbitrum <CONTRACT_ADDRESS>
```

### 部署后配置

1. **创建 Uniswap 流动性池**
```bash
# DUST / USDC 池
# stUSDC / USDC 池
```

2. **配置 OCW 中继服务**
```bash
# 启动 Substrate 节点
./stardust-node --enable-offchain-worker

# 配置 Arbitrum RPC
export ARBITRUM_RPC=https://arb1.arbitrum.io/rpc
```

3. **授予角色**
```bash
# 授予 OCW 地址 RELAYER_ROLE
# 授予 OCW 地址 OCW_ROLE
```

---

## 使用示例

### 前端集成

```typescript
// 1. 桥接 DUST 到 Arbitrum
import { ApiPromise } from '@polkadot/api';
import { ethers } from 'ethers';

// Stardust 链
const api = await ApiPromise.create({
  provider: new WsProvider('ws://127.0.0.1:9944'),
});

const amount = ethers.parseEther('1000'); // 1000 DUST
const ethAddress = '0x1234567890abcdef1234567890abcdef12345678';

await api.tx.dustBridge
  .bridgeToArbitrum(amount.toString(), ethAddress)
  .signAndSend(account);

// 2. 存入 AI 交易系统（Arbitrum）
const provider = new ethers.JsonRpcProvider('https://arb1.arbitrum.io/rpc');
const wallet = new ethers.Wallet(privateKey, provider);

const routerAddress = '0x...';
const router = new ethers.Contract(routerAddress, ROUTER_ABI, wallet);

const dustAmount = ethers.parseEther('1000');
const minUsdcOut = ethers.parseUnits('90', 6); // 最少 90 USDC（10% 滑点）

const tx = await router.depositWithDUST(dustAmount, minUsdcOut);
await tx.wait();

console.log('✅ 已存入 AI 交易系统');

// 3. 查询份额和收益
const vault = new ethers.Contract(vaultAddress, VAULT_ABI, provider);
const sharePrice = await vault.getSharePrice();
console.log(`当前份额价格: ${ethers.formatEther(sharePrice)}`);

// 4. 提取收益
const stUsdcAmount = ethers.parseEther('100');
const minDustOut = ethers.parseEther('900'); // 最少 900 DUST

const withdrawTx = await router.withdrawToDUST(stUsdcAmount, minDustOut);
await withdrawTx.wait();

console.log('✅ 已提取 DUST');
```

---

## 总结

Stardust AI 交易系统 v2.0（纯 DUST 版）实现了：

✅ **纯 DUST 入口**：用户只需 DUST，无需其他代币  
✅ **跨链桥接**：Stardust ↔ Arbitrum 无缝桥接  
✅ **自动转换**：DUST ↔ USDC 自动完成  
✅ **份额代币化**：stUSDC 代表金库份额  
✅ **AI 自动化**：DeepSeek AI + OCW 自动交易  
✅ **即时退出**：Uniswap DEX 提供流动性  
✅ **安全保障**：多签 + 治理 + 审计  

这是一个真正去中心化、自动化、安全的 AI 交易系统！

---

**文档版本**: v2.0  
**最后更新**: 2025-11-05  
**作者**: Stardust AI 团队

