# 押金与申诉治理系统 - 前端设计方案

> 主文档：`押金与申诉治理系统-完整设计方案.md`

## 📱 前端架构

### 技术栈

```
React 18 + TypeScript + Ant Design 5
├─ 状态管理：Zustand
├─ API调用：@polkadot/api
├─ 路由：React Router v6
├─ 样式：TailwindCSS + CSS Modules
└─ 响应式：移动端、网页端、桌面端自适应
```

### 目录结构

```
stardust-dapp/src/
├─ pages/
│  ├─ Appeals/                    ← 申诉中心（重命名）
│  │  ├─ index.tsx               ← 申诉列表
│  │  ├─ AppealDetail.tsx        ← 申诉详情
│  │  ├─ SubmitAppeal.tsx        ← 提交申诉
│  │  └─ AppealHistory.tsx       ← 申诉历史
│  └─ Deposits/                   ← 押金管理（新增）
│     ├─ index.tsx               ← 我的押金
│     ├─ DepositDetail.tsx       ← 押金详情
│     └─ DepositHistory.tsx      ← 押金历史
├─ components/
│  ├─ Appeals/
│  │  ├─ AppealCard.tsx          ← 申诉卡片
│  │  ├─ AppealStatus.tsx        ← 状态显示
│  │  ├─ AppealTimeline.tsx      ← 时间线
│  │  ├─ DomainSelector.tsx      ← 域选择器
│  │  └─ ActionSelector.tsx      ← 操作选择器
│  └─ Deposits/
│     ├─ DepositCard.tsx         ← 押金卡片
│     ├─ DepositStatus.tsx       ← 状态显示
│     └─ DepositCalculator.tsx   ← 押金计算器
├─ services/
│  ├─ appeals.ts                 ← 申诉API（重命名）
│  └─ deposits.ts                ← 押金API（新增）
├─ hooks/
│  ├─ useMemoAppeals.ts          ← 申诉Hooks（重命名）
│  ├─ useMemoDeposits.ts         ← 押金Hooks（新增）
│  └─ useDynamicPricing.ts       ← 动态定价Hooks（新增）
├─ types/
│  ├─ appeals.ts                 ← 申诉类型（重命名）
│  └─ deposits.ts                ← 押金类型（新增）
└─ utils/
   ├─ appealHelpers.ts           ← 申诉工具函数
   └─ depositHelpers.ts          ← 押金工具函数
```

---

## 🎨 UI设计

### 页面1：申诉中心（/appeals）

#### 布局设计

```
┌────────────────────────────────────────────────────────────┐
│  [申诉中心]                                 [提交申诉] 按钮  │
├────────────────────────────────────────────────────────────┤
│  筛选栏：                                                   │
│  [状态: 全部 ▼]  [域: 全部 ▼]  [时间: 全部 ▼]  [搜索...]  │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 申诉 #123                          [已批准] 🟢       │  │
│  │ 域: 墓地 | 操作: 清空封面                            │  │
│  │ 申诉人: alice...123 | 提交时间: 2025-10-20          │  │
│  │ 押金: 12.5 DUST ($10 USD) | 公示期至: 2025-11-20    │  │
│  │ [查看详情]                                           │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 申诉 #122                          [待审批] 🟡       │  │
│  │ 域: 逝者档案 | 操作: 转让所有权                      │  │
│  │ 申诉人: bob...456 | 提交时间: 2025-10-19            │  │
│  │ 押金: 25.0 DUST ($10 USD × 2.0)                     │  │
│  │ [查看详情]                                           │  │
│  └──────────────────────────────────────────────────────┘  │
│  [分页: 1 2 3 ... 10]                                      │
└────────────────────────────────────────────────────────────┘
```

#### 组件实现

```typescript
// pages/Appeals/index.tsx

import React, { useState, useEffect } from 'react';
import { Button, Select, Input, Card, Tag, Space } from 'antd';
import { useMemoAppeals } from '@/hooks/useMemoAppeals';
import { AppealCard } from '@/components/Appeals/AppealCard';
import { useNavigate } from 'react-router-dom';

export const AppealsPage: React.FC = () => {
  const navigate = useNavigate();
  const { appeals, loading, fetchAppeals } = useMemoAppeals();
  const [filters, setFilters] = useState({
    status: 'all',
    domain: 'all',
    timeRange: 'all',
  });

  useEffect(() => {
    fetchAppeals(filters);
  }, [filters]);

  return (
    <div className="appeals-page">
      {/* 头部 */}
      <div className="page-header">
        <h1>申诉中心</h1>
        <Button 
          type="primary" 
          size="large"
          onClick={() => navigate('/appeals/submit')}
        >
          提交申诉
        </Button>
      </div>

      {/* 筛选栏 */}
      <div className="filters">
        <Space size="middle">
          <Select
            value={filters.status}
            onChange={(v) => setFilters({ ...filters, status: v })}
            style={{ width: 120 }}
          >
            <Select.Option value="all">全部状态</Select.Option>
            <Select.Option value="0">待审批</Select.Option>
            <Select.Option value="1">已批准</Select.Option>
            <Select.Option value="2">已驳回</Select.Option>
            <Select.Option value="4">已执行</Select.Option>
          </Select>

          <Select
            value={filters.domain}
            onChange={(v) => setFilters({ ...filters, domain: v })}
            style={{ width: 120 }}
          >
            <Select.Option value="all">全部域</Select.Option>
            <Select.Option value="1">墓地</Select.Option>
            <Select.Option value="2">逝者档案</Select.Option>
            <Select.Option value="3">供奉品</Select.Option>
            <Select.Option value="4">媒体</Select.Option>
            <Select.Option value="5">文本</Select.Option>
          </Select>

          <Input.Search
            placeholder="搜索申诉ID或地址"
            style={{ width: 200 }}
            onSearch={(value) => setFilters({ ...filters, search: value })}
          />
        </Space>
      </div>

      {/* 申诉列表 */}
      <div className="appeals-list">
        {appeals.map((appeal) => (
          <AppealCard 
            key={appeal.id} 
            appeal={appeal}
            onClick={() => navigate(`/appeals/${appeal.id}`)}
          />
        ))}
      </div>
    </div>
  );
};
```

---

### 页面2：提交申诉（/appeals/submit）

#### 布局设计

```
┌────────────────────────────────────────────────────────────┐
│  [提交申诉]                                                 │
├────────────────────────────────────────────────────────────┤
│  步骤进度：                                                 │
│  ● 选择对象 → ○ 选择操作 → ○ 填写理由 → ○ 确认押金        │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Step 1: 选择申诉对象                                  │  │
│  │                                                        │  │
│  │ 申诉域：                                              │  │
│  │ [墓地 ▼]                                              │  │
│  │                                                        │  │
│  │ 对象ID：                                              │  │
│  │ [123]                                                 │  │
│  │                                                        │  │
│  │ 对象信息预览：                                        │  │
│  │ ┌──────────────────────────────────────────────────┐ │  │
│  │ │ 墓地 #123                                         │ │  │
│  │ │ 名称: 张三的纪念地                                │ │  │
│  │ │ 所有者: alice...123                               │ │  │
│  │ │ 创建时间: 2024-01-01                              │ │  │
│  │ └──────────────────────────────────────────────────┘ │  │
│  │                                                        │  │
│  │                                  [上一步]  [下一步] │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

#### 组件实现

```typescript
// pages/Appeals/SubmitAppeal.tsx

import React, { useState } from 'react';
import { Steps, Form, Select, Input, InputNumber, Button, Upload, Card, Alert } from 'antd';
import { DomainSelector } from '@/components/Appeals/DomainSelector';
import { ActionSelector } from '@/components/Appeals/ActionSelector';
import { DepositCalculator } from '@/components/Deposits/DepositCalculator';
import { useMemoAppeals } from '@/hooks/useMemoAppeals';
import { useDynamicPricing } from '@/hooks/useDynamicPricing';

export const SubmitAppealPage: React.FC = () => {
  const [currentStep, setCurrentStep] = useState(0);
  const [form] = Form.useForm();
  const { submitAppeal, submitting } = useMemoAppeals();
  const { calculateDeposit } = useDynamicPricing();

  const handleSubmit = async (values: any) => {
    const result = await submitAppeal({
      domain: values.domain,
      target: values.target,
      action: values.action,
      reasonCid: values.reasonCid,
      evidenceCid: values.evidenceCid,
    });

    if (result.success) {
      // 导航到申诉详情
      navigate(`/appeals/${result.appealId}`);
    }
  };

  const steps = [
    {
      title: '选择对象',
      content: (
        <>
          <Form.Item 
            name="domain" 
            label="申诉域" 
            rules={[{ required: true }]}
          >
            <DomainSelector />
          </Form.Item>

          <Form.Item 
            name="target" 
            label="对象ID" 
            rules={[{ required: true }]}
          >
            <InputNumber 
              style={{ width: '100%' }} 
              placeholder="输入对象ID"
            />
          </Form.Item>

          {/* 对象信息预览 */}
          <ObjectPreview 
            domain={form.getFieldValue('domain')} 
            target={form.getFieldValue('target')} 
          />
        </>
      ),
    },
    {
      title: '选择操作',
      content: (
        <>
          <Form.Item 
            name="action" 
            label="申诉操作" 
            rules={[{ required: true }]}
          >
            <ActionSelector domain={form.getFieldValue('domain')} />
          </Form.Item>

          <Alert
            message="操作说明"
            description={getActionDescription(
              form.getFieldValue('domain'),
              form.getFieldValue('action')
            )}
            type="info"
          />
        </>
      ),
    },
    {
      title: '填写理由',
      content: (
        <>
          <Form.Item 
            name="reason" 
            label="申诉理由" 
            rules={[{ required: true, min: 50 }]}
          >
            <Input.TextArea 
              rows={6} 
              placeholder="详细说明申诉理由（至少50字）"
            />
          </Form.Item>

          <Form.Item 
            name="evidence" 
            label="上传证据"
          >
            <Upload.Dragger
              name="files"
              multiple
              customRequest={({ file, onSuccess }) => {
                // 上传到IPFS
                uploadToIPFS(file).then((cid) => {
                  onSuccess({ cid });
                });
              }}
            >
              <p>点击或拖拽文件到此区域上传</p>
              <p>支持图片、PDF、文档等格式</p>
            </Upload.Dragger>
          </Form.Item>
        </>
      ),
    },
    {
      title: '确认押金',
      content: (
        <>
          <DepositCalculator
            domain={form.getFieldValue('domain')}
            action={form.getFieldValue('action')}
            target={form.getFieldValue('target')}
          />

          <Alert
            message="押金说明"
            description={
              <>
                <p>• 基准押金：$10 USD（根据MEMO实时汇率动态计算）</p>
                <p>• 当前操作倍数：{getActionMultiplier(form.getFieldValue('domain'), form.getFieldValue('action'))}×</p>
                <p>• 申诉通过：全额退回押金</p>
                <p>• 申诉驳回：扣除30%押金</p>
                <p>• 主动撤回：扣除10%押金</p>
              </>
            }
            type="warning"
          />

          <Form.Item>
            <Button 
              type="primary" 
              size="large" 
              block
              loading={submitting}
              onClick={() => form.submit()}
            >
              确认提交申诉
            </Button>
          </Form.Item>
        </>
      ),
    },
  ];

  return (
    <div className="submit-appeal-page">
      <Card>
        <Steps current={currentStep} items={steps.map(s => ({ title: s.title }))} />
        
        <Form 
          form={form} 
          layout="vertical" 
          onFinish={handleSubmit}
          style={{ marginTop: 32 }}
        >
          {steps[currentStep].content}
        </Form>

        <div className="step-actions">
          {currentStep > 0 && (
            <Button onClick={() => setCurrentStep(currentStep - 1)}>
              上一步
            </Button>
          )}
          {currentStep < steps.length - 1 && (
            <Button type="primary" onClick={() => setCurrentStep(currentStep + 1)}>
              下一步
            </Button>
          )}
        </div>
      </Card>
    </div>
  );
};
```

---

### 页面3：申诉详情（/appeals/:id）

#### 布局设计

```
┌────────────────────────────────────────────────────────────┐
│  [← 返回列表]              申诉 #123                        │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌────────────────────────────┐  │
│  │ 基本信息              │  │ 时间线                      │  │
│  │                       │  │                            │  │
│  │ 状态：[已批准] 🟢    │  │ ● 2025-10-20 10:00        │  │
│  │ 域：墓地              │  │   提交申诉                 │  │
│  │ 对象：#123            │  │   押金冻结: 12.5 DUST     │  │
│  │ 操作：清空封面        │  │                            │  │
│  │ 申诉人：alice...123   │  │ ● 2025-10-22 15:30        │  │
│  │ 押金：12.5 DUST       │  │   委员会批准               │  │
│  │       ($10 USD)       │  │   公示期: 30天             │  │
│  │                       │  │                            │  │
│  │ 押金ID：#456          │  │ ○ 2025-11-21 15:30        │  │
│  │ [查看押金详情]        │  │   预计执行                 │  │
│  └──────────────────────┘  └────────────────────────────┘  │
├────────────────────────────────────────────────────────────┤
│  申诉理由：                                                 │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 该墓地封面图片存在不当内容，违反社区规范...          │  │
│  │ （详细理由文本）                                      │  │
│  └──────────────────────────────────────────────────────┘  │
├────────────────────────────────────────────────────────────┤
│  证据材料：                                                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                      │
│  │ 📷      │ │ 📄      │ │ 📎      │                      │
│  │ 图片1   │ │ 文档1   │ │ 附件1   │                      │
│  └─────────┘ └─────────┘ └─────────┘                      │
├────────────────────────────────────────────────────────────┤
│  操作：                                                     │
│  [撤回申诉]  [补充证据]                                     │
└────────────────────────────────────────────────────────────┘
```

---

### 页面4：我的押金（/deposits）

#### 布局设计

```
┌────────────────────────────────────────────────────────────┐
│  [我的押金]                                                 │
├────────────────────────────────────────────────────────────┤
│  押金概览：                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ 总押金    │ │ 冻结中    │ │ 已释放    │ │ 已罚没    │      │
│  │ 50 DUST  │ │ 25 DUST  │ │ 20 DUST  │ │ 5 DUST   │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
├────────────────────────────────────────────────────────────┤
│  筛选：                                                     │
│  [状态: 全部 ▼]  [用途: 全部 ▼]  [时间: 全部 ▼]           │
├────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 押金 #456                          [冻结中] 🔒       │  │
│  │ 用途: 申诉 #123 (墓地 - 清空封面)                    │  │
│  │ 金额: 12.5 DUST ($10 USD)                            │  │
│  │ 冻结时间: 2025-10-20 10:00                           │  │
│  │ 预计释放: 2025-11-21（公示期结束后）                 │  │
│  │ [查看详情] [查看关联申诉]                            │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 押金 #455                          [已释放] ✅       │  │
│  │ 用途: 申诉 #120 (逝者档案 - 设置可见性)              │  │
│  │ 金额: 10.0 DUST ($10 USD) → 全额退回                │  │
│  │ 冻结时间: 2025-10-15 | 释放时间: 2025-10-18         │  │
│  │ [查看详情]                                           │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

## 🔧 核心Hooks

### useMemoAppeals

```typescript
// hooks/useMemoAppeals.ts

import { useState, useCallback } from 'react';
import { useApi } from '@/hooks/useApi';
import { useAccount } from '@/hooks/useAccount';
import { Appeal, SubmitAppealParams } from '@/types/appeals';

export function useMemoAppeals() {
  const { api } = useApi();
  const { account } = useAccount();
  const [appeals, setAppeals] = useState<Appeal[]>([]);
  const [loading, setLoading] = useState(false);

  // 获取申诉列表
  const fetchAppeals = useCallback(async (filters?: any) => {
    setLoading(true);
    try {
      // 从链上查询
      const appealIds = await api.query.memoAppeals.appealsByAccount(account.address);
      
      const appealData = await Promise.all(
        appealIds.map(async (id) => {
          const appeal = await api.query.memoAppeals.appeals(id);
          return {
            id: id.toNumber(),
            ...appeal.toJSON(),
          };
        })
      );
      
      setAppeals(appealData);
    } catch (error) {
      console.error('Failed to fetch appeals:', error);
    } finally {
      setLoading(false);
    }
  }, [api, account]);

  // 提交申诉
  const submitAppeal = useCallback(async (params: SubmitAppealParams) => {
    try {
      // 上传理由到IPFS
      const reasonCid = await uploadToIPFS(params.reason);
      
      // 上传证据到IPFS
      const evidenceCid = await uploadToIPFS(params.evidence);
      
      // 调用链上extrinsic
      const tx = api.tx.memoAppeals.submitAppeal(
        params.domain,
        params.target,
        params.action,
        reasonCid,
        evidenceCid
      );
      
      // 签名并发送
      const result = await tx.signAndSend(account.address);
      
      // 从事件中提取appeal_id
      const appealId = extractAppealId(result.events);
      
      return {
        success: true,
        appealId,
        txHash: result.txHash,
      };
    } catch (error) {
      return {
        success: false,
        error: error.message,
      };
    }
  }, [api, account]);

  // 撤回申诉
  const withdrawAppeal = useCallback(async (appealId: number) => {
    try {
      const tx = api.tx.memoAppeals.withdrawAppeal(appealId);
      await tx.signAndSend(account.address);
      return { success: true };
    } catch (error) {
      return { success: false, error: error.message };
    }
  }, [api, account]);

  return {
    appeals,
    loading,
    fetchAppeals,
    submitAppeal,
    withdrawAppeal,
  };
}
```

### useMemoDeposits

```typescript
// hooks/useMemoDeposits.ts

import { useState, useCallback } from 'react';
import { useApi } from '@/hooks/useApi';
import { useAccount } from '@/hooks/useAccount';
import { Deposit } from '@/types/deposits';

export function useMemoDeposits() {
  const { api } = useApi();
  const { account } = useAccount();
  const [deposits, setDeposits] = useState<Deposit[]>([]);
  const [loading, setLoading] = useState(false);

  // 获取押金列表
  const fetchDeposits = useCallback(async () => {
    setLoading(true);
    try {
      const depositIds = await api.query.deposits.depositsByAccount(account.address);
      
      const depositData = await Promise.all(
        depositIds.map(async (id) => {
          const deposit = await api.query.deposits.deposits(id);
          return {
            id: id.toNumber(),
            ...deposit.toJSON(),
          };
        })
      );
      
      setDeposits(depositData);
    } catch (error) {
      console.error('Failed to fetch deposits:', error);
    } finally {
      setLoading(false);
    }
  }, [api, account]);

  // 获取押金详情
  const getDepositDetail = useCallback(async (depositId: number) => {
    try {
      const deposit = await api.query.deposits.deposits(depositId);
      return deposit.toJSON();
    } catch (error) {
      console.error('Failed to fetch deposit:', error);
      return null;
    }
  }, [api]);

  return {
    deposits,
    loading,
    fetchDeposits,
    getDepositDetail,
  };
}
```

### useDynamicPricing

```typescript
// hooks/useDynamicPricing.ts

import { useState, useCallback, useEffect } from 'react';
import { useApi } from '@/hooks/useApi';

export function useDynamicPricing() {
  const { api } = useApi();
  const [memoPrice, setMemoPrice] = useState<number>(0);  // DUST/USD价格
  const [loading, setLoading] = useState(false);

  // 获取MEMO价格
  const fetchMemoPrice = useCallback(async () => {
    setLoading(true);
    try {
      // 从pallet-pricing获取聚合价格
      const otcPrice = await api.query.pricing.otcPriceAggregate();
      const bridgePrice = await api.query.pricing.bridgePriceAggregate();
      
      // 取平均值
      const avgPrice = (otcPrice.priceUsdt + bridgePrice.priceUsdt) / 2;
      setMemoPrice(avgPrice / 1_000_000);  // 转为USD
    } catch (error) {
      console.error('Failed to fetch DUST price:', error);
    } finally {
      setLoading(false);
    }
  }, [api]);

  // 定时刷新价格
  useEffect(() => {
    fetchMemoPrice();
    const interval = setInterval(fetchMemoPrice, 60000);  // 每分钟刷新
    return () => clearInterval(interval);
  }, [fetchMemoPrice]);

  // 计算押金
  const calculateDeposit = useCallback((
    domain: number,
    action: number,
    target: number
  ) => {
    const baseUsd = 10;  // $10 USD
    
    // 根据domain/action确定倍数
    const multiplier = getMultiplier(domain, action);
    
    // 计算MEMO数量
    const memoAmount = (baseUsd / memoPrice) * multiplier;
    
    return {
      memoAmount: memoAmount.toFixed(2),
      usdValue: (baseUsd * multiplier).toFixed(2),
      multiplier,
      memoPrice: memoPrice.toFixed(4),
    };
  }, [memoPrice]);

  return {
    memoPrice,
    loading,
    fetchMemoPrice,
    calculateDeposit,
  };
}

// 辅助函数：获取倍数
function getMultiplier(domain: number, action: number): number {
  const multiplierMap: Record<string, number> = {
    // grave
    '1-10': 1.0,  // 清空封面
    '1-11': 2.0,  // 转让所有权
    '1-12': 1.0,  // 设置限制
    
    // deceased
    '2-1': 1.0,   // 设置可见性
    '2-2': 1.5,   // 清空主图
    '2-4': 2.0,   // 转让所有权
    
    // deceased-media
    '4-30': 1.0,  // 隐藏媒体
    '4-31': 1.5,  // 替换URI
    
    // deceased-text
    '5-20': 1.5,  // 删除生平
    '5-21': 1.5,  // 删除悼词
    '5-22': 1.0,  // 编辑生平
  };
  
  const key = `${domain}-${action}`;
  return multiplierMap[key] || 1.0;
}
```

---

## 🎨 组件设计

### AppealCard

```typescript
// components/Appeals/AppealCard.tsx

import React from 'react';
import { Card, Tag, Space, Typography, Button } from 'antd';
import { Appeal } from '@/types/appeals';
import { AppealStatus } from './AppealStatus';
import { formatAddress, formatDate, formatMemo } from '@/utils';

interface AppealCardProps {
  appeal: Appeal;
  onClick?: () => void;
}

export const AppealCard: React.FC<AppealCardProps> = ({ appeal, onClick }) => {
  return (
    <Card 
      hoverable 
      onClick={onClick}
      className="appeal-card"
    >
      <div className="card-header">
        <Typography.Title level={5}>
          申诉 #{appeal.id}
        </Typography.Title>
        <AppealStatus status={appeal.status} />
      </div>

      <Space direction="vertical" size="small" style={{ width: '100%' }}>
        <div>
          <span className="label">域:</span>
          <Tag color="blue">{getDomainName(appeal.domain)}</Tag>
          <span className="label">操作:</span>
          <Tag>{getActionName(appeal.domain, appeal.action)}</Tag>
        </div>

        <div>
          <span className="label">申诉人:</span>
          <Typography.Text code>{formatAddress(appeal.who)}</Typography.Text>
        </div>

        <div>
          <span className="label">押金:</span>
          <Typography.Text strong>
            {formatMemo(appeal.depositAmount)} DUST
          </Typography.Text>
          <Typography.Text type="secondary">
            {' '}(${appeal.usdValue} USD)
          </Typography.Text>
        </div>

        <div>
          <span className="label">提交时间:</span>
          <Typography.Text>{formatDate(appeal.createdAt)}</Typography.Text>
        </div>

        {appeal.executeAt && (
          <div>
            <span className="label">预计执行:</span>
            <Typography.Text type="warning">
              {formatDate(appeal.executeAt)}
            </Typography.Text>
          </div>
        )}
      </Space>

      <div className="card-footer">
        <Button type="link" onClick={onClick}>
          查看详情 →
        </Button>
      </div>
    </Card>
  );
};
```

### DepositCalculator

```typescript
// components/Deposits/DepositCalculator.tsx

import React, { useEffect, useState } from 'react';
import { Card, Descriptions, Spin, Typography } from 'antd';
import { useDynamicPricing } from '@/hooks/useDynamicPricing';

interface DepositCalculatorProps {
  domain: number;
  action: number;
  target: number;
}

export const DepositCalculator: React.FC<DepositCalculatorProps> = ({
  domain,
  action,
  target,
}) => {
  const { calculateDeposit, memoPrice, loading } = useDynamicPricing();
  const [deposit, setDeposit] = useState<any>(null);

  useEffect(() => {
    if (domain && action && target) {
      const result = calculateDeposit(domain, action, target);
      setDeposit(result);
    }
  }, [domain, action, target, calculateDeposit]);

  if (loading || !deposit) {
    return <Spin />;
  }

  return (
    <Card title="押金计算" className="deposit-calculator">
      <Descriptions column={1} bordered>
        <Descriptions.Item label="MEMO当前价格">
          ${deposit.memoPrice} USD
        </Descriptions.Item>
        <Descriptions.Item label="基准押金">
          $10 USD
        </Descriptions.Item>
        <Descriptions.Item label="风险倍数">
          {deposit.multiplier}×
        </Descriptions.Item>
        <Descriptions.Item label="总金额（USD）">
          <Typography.Text strong>${deposit.usdValue} USD</Typography.Text>
        </Descriptions.Item>
        <Descriptions.Item label="总金额（DUST）">
          <Typography.Text strong type="success">
            {deposit.memoAmount} DUST
          </Typography.Text>
        </Descriptions.Item>
      </Descriptions>

      <div className="deposit-note" style={{ marginTop: 16 }}>
        <Typography.Text type="secondary">
          * 押金根据MEMO实时价格动态计算，确保价值锚定美元
        </Typography.Text>
      </div>
    </Card>
  );
};
```

---

## 📱 响应式设计

### 移动端适配

```typescript
// 使用CSS媒体查询 + Ant Design响应式组件

// 布局调整
const AppealsList: React.FC = () => {
  return (
    <Row gutter={[16, 16]}>
      {appeals.map((appeal) => (
        <Col xs={24} sm={24} md={12} lg={8} xl={6} key={appeal.id}>
          <AppealCard appeal={appeal} />
        </Col>
      ))}
    </Row>
  );
};

// 表单适配
const SubmitAppealForm: React.FC = () => {
  return (
    <Form layout="vertical" {...formItemLayout}>
      {/* 移动端单列，桌面端双列 */}
    </Form>
  );
};
```

---

## 🎨 UI风格统一

### 颜色方案

```typescript
// 保持与钱包页面风格一致

const theme = {
  colors: {
    primary: '#1890ff',      // 主色调
    success: '#52c41a',      // 成功/已批准
    warning: '#faad14',      // 待审批
    danger: '#ff4d4f',       // 已驳回
    info: '#13c2c2',         // 信息
    background: '#f0f2f5',   // 背景色
  },
  gradients: {
    primary: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
    success: 'linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%)',
  },
};
```

---

**下一文档**：`押金与申诉治理系统-实施路线图.md`

