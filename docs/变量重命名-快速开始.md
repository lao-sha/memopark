# 🚀 变量重命名 - 快速开始指南

**📅 日期**: 2025-10-29  
**🎯 目标**: 完成memo→dust变量的全面重命名  
**⏱️ 预计时间**: 2-4小时

---

## 📊 概览

### 重命名范围
| 类别 | 数量 | 优先级 | 风险 |
|------|------|--------|------|
| 前端变量 | ~60处 | 🔴 高 | 🟢 低 |
| 函数名 | ~10处 | 🔴 高 | 🟢 低 |
| API路径 | ~30处 | 🟡 中 | 🟡 中 |
| 总计 | ~100处 | - | - |

---

## 📁 文档和脚本清单

### 核心文档
- ✅ `变量重命名方案-memo变量分析.md` - 详细分析报告
- ✅ `变量重命名-快速开始.md` - 本文档

### 自动化脚本
- ✅ `rename-memo-variables.sh` - 前端变量重命名（安全，立即可用）
- ✅ `update-api-paths.sh` - API路径更新（需要链端就绪）

### 生成的测试指南
- 🔄 `API-TEST-GUIDE.md` - API更新后的测试指南（运行脚本后生成）

---

## 🎯 执行路线图

### 路线A: 完整执行（推荐）⭐️

```
阶段1: 前端变量重命名 (30分钟)
   ↓
阶段2: 编译验证 (10分钟)
   ↓
阶段3: 链端API确认 (30分钟)
   ↓
阶段4: API路径更新 (20分钟)
   ↓
阶段5: 完整功能测试 (1-2小时)
```

### 路线B: 分阶段执行（保守）

```
阶段1: 仅前端变量重命名
   ↓
   手动测试 + 验证
   ↓
（等待1-2天）
   ↓
阶段2: API路径更新
   ↓
   完整测试
```

---

## 🚀 阶段1: 前端变量重命名

### 1.1 执行前检查

```bash
cd /home/xiaodong/文档/memopark

# 确认目录存在
ls -d stardust-dapp stardust-governance

# 确认Git状态干净
git status

# 确认脚本可执行
ls -lh docs/rename-memo-variables.sh
```

### 1.2 执行脚本

```bash
# 方式1: 交互式执行（推荐）
./docs/rename-memo-variables.sh

# 方式2: 查看脚本内容后执行
cat docs/rename-memo-variables.sh
./docs/rename-memo-variables.sh
```

### 1.3 脚本执行流程

脚本会自动完成以下步骤：

1. **创建备份** ✅
   - 提交当前所有更改
   - 创建Git标签 `before-variable-rename`

2. **重命名基础变量** ✅
   - `memoAmount` → `dustAmount`
   - `setMemoAmount` → `setDustAmount`
   - `memoReceive` → `dustReceive`

3. **重命名函数名** ✅
   - `formatMemoAmount` → `formatDustAmount`
   - `formatMemo` → `formatDust`

4. **验证React Hook** ✅
   - 确认 `useMemo` 未被误改

5. **统计修改** ✅
   - 显示修改的文件数
   - 显示修改统计

6. **提交更改** ✅
   - Git提交

7. **编译验证（可选）** ⚠️
   - 可以跳过，留待后续手动执行

### 1.4 预期输出

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🔄 前端变量重命名脚本
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

此脚本将执行以下操作：
  1. 创建Git备份
  2. 重命名基础变量 (memoAmount等)
  3. 重命名函数名 (formatMemoAmount等)
  4. 验证React Hook未被误改
  5. 统计修改
  6. 提交更改
  7. 编译验证（可选）

是否继续？[y/N]: y

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  阶段1: 创建备份
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ℹ️  创建Git备份...
ℹ️  创建Git标签...
✅ 备份创建完成

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  阶段2: 重命名基础变量
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ℹ️  开始重命名以下变量：
  - memoAmount      → dustAmount
  - setMemoAmount   → setDustAmount
  - memoReceive     → dustReceive

是否继续？[y/N]: y

✅ memoAmount → dustAmount 完成
✅ setMemoAmount → setDustAmount 完成
✅ memoReceive → dustReceive 完成
✅ 基础变量重命名完成

...

🎉 变量重命名完成
✅ 所有memo相关变量已重命名为dust
```

### 1.5 如果出错 - 回滚方案

```bash
# 立即回滚到重命名前
cd /home/xiaodong/文档/memopark
git reset --hard before-variable-rename

# 验证回滚成功
git log --oneline -3
```

---

## 🧪 阶段2: 编译验证

### 2.1 前端编译

```bash
cd /home/xiaodong/文档/memopark/stardust-dapp

# 清理缓存
rm -rf node_modules/.vite

# 编译
npm run build
```

### 2.2 预期结果

- ✅ 编译成功，无错误
- ✅ 可能有少量警告（正常）
- ❌ 如果有 `Cannot find name 'useDust'` 错误，说明React Hook被误改

### 2.3 如果编译失败

```bash
# 检查错误类型
npm run build 2>&1 | grep -i "error"

# 如果是useDust错误，手动修复
grep -r "useDust" src/
# 手动将 useDust 改回 useMemo

# 重新编译
npm run build
```

---

## 🔍 阶段3: 链端API确认

**⚠️ 重要：在执行API路径更新前，必须确认链端已准备就绪！**

### 3.1 检查链端pallet名称

```bash
# 检查链端运行时配置
cd /home/xiaodong/文档/memopark
grep -r "pallet-stardust-appeals" runtime/Cargo.toml

# 确认构建成功
grep "stardustAppeals\|stardust_appeals" runtime/src/lib.rs
```

### 3.2 使用Polkadot.js Apps测试

1. 启动链端节点（如果未启动）:
```bash
cd /home/xiaodong/文档/memopark
./target/release/stardust-node --dev --tmp
```

2. 打开 Polkadot.js Apps:
   - 浏览器访问: https://polkadot.js.org/apps/
   - 连接到: `ws://127.0.0.1:9944`

3. 测试新API:
   - Developer → Chain State
   - 选择模块 `stardustAppeals`
   - 选择查询 `appeals(id)`
   - 输入ID: `1`
   - 点击 `+` 查询

4. 确认结果:
   - ✅ 查询成功 → 可以继续执行阶段4
   - ❌ 查询失败 → **不要执行阶段4**，需要先修复链端

### 3.3 检查价格API（可选）

```bash
# 检查pricing pallet的函数名
cd /home/xiaodong/文档/memopark
grep -A 5 "fn get.*market.*price" pallets/pricing/src/lib.rs
```

如果看到 `getMemoMarketPriceWeighted`，说明链端未更新，API路径脚本会跳过价格API更新。

---

## 🔄 阶段4: API路径更新

**⚠️ 前提条件**:
- [x] 阶段3确认链端API可用
- [x] 节点正在运行
- [x] 已测试 `stardustAppeals` API

### 4.1 执行脚本

```bash
cd /home/xiaodong/文档/memopark

# 执行API路径更新脚本
./docs/update-api-paths.sh
```

### 4.2 脚本确认提示

脚本会要求严格确认：

```
⚠️⚠️⚠️  重要警告  ⚠️⚠️⚠️

此脚本会修改API查询路径，必须满足以下前提：

  ✓ 链端pallet-memo-appeals已重命名为pallet-stardust-appeals
  ✓ 链端已重新编译并启动
  ✓ 已手动测试新API路径可用：
    api.query.stardustAppeals.appeals(1)

❌ 如果链端未就绪，执行此脚本会导致前端无法查询数据！

确认所有前提条件已满足？[yes/NO]:
```

**必须输入完整的 `yes` 才能继续！**

### 4.3 脚本执行内容

1. **创建备份**
   - Git标签 `before-api-path-update`

2. **更新治理前端**
   - `memoAppeals` → `stardustAppeals`
   - `memoContentGovernance` → `stardustAppeals`

3. **更新主前端价格API（可选）**
   - `getMemoMarketPriceWeighted` → `getDustMarketPriceWeighted`

4. **验证修改**
   - 检查是否有遗漏

5. **生成测试指南**
   - 创建 `API-TEST-GUIDE.md`

### 4.4 如果API路径更新失败

```bash
# 立即回滚
cd /home/xiaodong/文档/memopark
git reset --hard before-api-path-update

# 检查链端状态
ps aux | grep stardust-node
```

---

## 🧪 阶段5: 完整功能测试

### 5.1 启动所有服务

```bash
# 终端1: 启动链端节点
cd /home/xiaodong/文档/memopark
./target/release/stardust-node --dev --tmp

# 终端2: 启动主前端
cd /home/xiaodong/文档/memopark/stardust-dapp
npm run dev

# 终端3: 启动治理前端
cd /home/xiaodong/文档/memopark/stardust-governance
npm run dev
```

### 5.2 核心功能测试清单

#### 主前端测试

**测试1: Bridge兑换** 🔴 关键
- [ ] 打开Bridge页面
- [ ] 输入兑换数量（dustAmount变量）
- [ ] 确认价格计算正确
- [ ] 提交兑换交易
- [ ] 确认无JS错误

**测试2: OTC订单** 🔴 关键
- [ ] 打开创建订单页面
- [ ] 查看市场价格显示
- [ ] 创建测试订单
- [ ] 确认金额格式化正确

**测试3: 价格监控** 🟡 重要
- [ ] 打开价格Dashboard
- [ ] 确认价格数据加载
- [ ] 检查控制台无API错误

#### 治理前端测试

**测试4: 申诉列表** 🔴 关键
- [ ] 打开申诉管理
- [ ] 确认列表正常加载
- [ ] 使用筛选功能
- [ ] 检查API调用正常

**测试5: 申诉详情** 🟡 重要
- [ ] 点击任意申诉
- [ ] 确认详情显示完整
- [ ] 测试投票功能

### 5.3 控制台检查

打开浏览器开发者工具（F12）：

**正常状态**:
```
✅ 无 "xxx is undefined" 错误
✅ 无 "Cannot find property" 错误
✅ API调用成功（200状态）
```

**异常状态**:
```
❌ query.stardustAppeals is undefined
❌ Cannot find dustAmount
❌ API调用失败（404/500）
```

如果发现异常，立即回滚：
```bash
git reset --hard before-api-path-update
```

---

## 📊 修改统计

### 预期修改统计

```bash
cd /home/xiaodong/文档/memopark

# 查看修改的文件数
git diff before-variable-rename --name-only | wc -l
# 预期: 20-30个文件

# 查看修改行数
git diff before-variable-rename --stat
# 预期: ~150行修改

# 查看具体修改
git diff before-variable-rename | grep -E "^\+.*dust|^\-.*memo" | head -20
```

### 修改分布

| 目录 | 文件数 | 修改行数 |
|------|--------|----------|
| stardust-dapp/src/features/bridge | 5 | ~40 |
| stardust-dapp/src/components/trading | 2 | ~15 |
| stardust-dapp/src/services | 1 | ~10 |
| stardust-governance/src | 6 | ~30 |
| **总计** | **~15** | **~100** |

---

## ✅ 完成验收标准

### 代码层面
- [x] 所有 `memoAmount` 已改为 `dustAmount`
- [x] 所有 `formatMemoAmount` 已改为 `formatDustAmount`
- [x] React Hook `useMemo` 未被误改
- [x] API路径 `memoAppeals` 已改为 `stardustAppeals`
- [x] 编译通过，无错误

### 功能层面
- [ ] Bridge兑换功能正常
- [ ] OTC订单创建正常
- [ ] 价格查询正常
- [ ] 申诉管理正常
- [ ] 所有金额计算正确

### 文档层面
- [x] 生成 `变量重命名方案-memo变量分析.md`
- [x] 生成 `变量重命名-快速开始.md`
- [ ] 生成 `API-TEST-GUIDE.md`（脚本自动生成）
- [ ] 记录测试结果

---

## 🚨 常见问题

### Q1: 执行脚本时提示"权限不足"

**解决**:
```bash
chmod +x docs/rename-memo-variables.sh
chmod +x docs/update-api-paths.sh
```

### Q2: React Hook被误改为useDust

**解决**:
```bash
# 搜索并替换
cd /home/xiaodong/文档/memopark/stardust-dapp/src
find . -name "*.tsx" -o -name "*.ts" | xargs sed -i 's/useDust/useMemo/g'
```

### Q3: API查询失败

**原因**: 链端pallet名称不匹配

**解决**:
```bash
# 回滚API路径修改
git reset --hard before-api-path-update

# 确认链端pallet名称
grep "pallet-stardust-appeals" runtime/Cargo.toml

# 重新编译链端
cargo build --release
```

### Q4: 编译时提示找不到dustAmount

**原因**: 可能有文件被遗漏或修改不完整

**解决**:
```bash
# 查找剩余的memoAmount
cd /home/xiaodong/文档/memopark/stardust-dapp/src
grep -r "memoAmount" . --include="*.ts" --include="*.tsx"

# 手动修复
# 或重新运行脚本
```

### Q5: 想回滚所有修改

**方案1: 回滚到重命名前**
```bash
cd /home/xiaodong/文档/memopark
git reset --hard before-variable-rename
```

**方案2: 只回滚API路径修改**
```bash
git reset --hard before-api-path-update
```

**方案3: 回滚到特定提交**
```bash
git log --oneline -10
git reset --hard <commit-hash>
```

---

## 📋 执行检查清单

打印此清单，在执行过程中勾选：

```
变量重命名 - 执行检查清单
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

准备工作
□ 确认stardust-dapp目录存在
□ 确认stardust-governance目录存在
□ Git状态干净
□ 脚本有执行权限

阶段1: 前端变量重命名
□ 执行 rename-memo-variables.sh
□ 备份创建成功
□ 变量重命名完成
□ React Hook验证通过
□ 修改已提交

阶段2: 编译验证
□ stardust-dapp编译通过
□ 无useDust错误
□ 无其他编译错误

阶段3: 链端API确认
□ 链端节点正在运行
□ Polkadot.js Apps连接成功
□ stardustAppeals API测试通过
□ 价格API检查完成（可选）

阶段4: API路径更新
□ 确认链端API就绪
□ 执行 update-api-paths.sh
□ 严格确认提示通过
□ API路径更新完成
□ 测试指南已生成

阶段5: 功能测试
□ 链端节点启动
□ 主前端启动
□ 治理前端启动
□ Bridge兑换测试通过
□ OTC订单测试通过
□ 价格查询测试通过
□ 申诉管理测试通过
□ 控制台无错误

文档更新
□ API-TEST-GUIDE.md已生成
□ 测试结果已记录
□ 变更日志已更新

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

完成时间: ____________
执行人员: ____________
测试结果: [ ] 通过 / [ ] 失败
备注: ____________________________________________________
```

---

## 🎯 下一步

完成变量重命名后，继续：

1. **前端目录和文件重命名**
   - `memopark-dapp` → `stardust-dapp` ✅ (已完成)
   - `memopark-governance` → `stardust-governance` ✅ (已完成)

2. **链端README和文档更新**
   - 更新所有文档中的MEMO → DUST
   - 更新接口文档

3. **Git仓库重命名**
   - 更新Git remote URL
   - 推送到新仓库

4. **域名和部署**
   - 更新域名配置
   - 重新部署前端

---

## 📞 支持

如遇到问题，参考：
1. `变量重命名方案-memo变量分析.md` - 详细分析
2. `API-TEST-GUIDE.md` - 测试指南
3. Git历史记录：`git log --oneline -10`
4. 回滚命令：`git reset --hard <tag-name>`

---

**📅 文档生成时间**: 2025-10-29  
**✍️ 创建者**: AI Assistant  
**🔄 版本**: v1.0  
**🎯 状态**: 就绪，可立即执行

