# 📖 前端项目重命名 - 手动操作指南

**📅 日期**: 2025-10-29  
**🎯 目标**: stardust → **stardust**，DUST → **DUST**  
**📊 当前进度**: 40% (链端已完成)

---

## 🔍 概览

### 已完成
✅ 阶段1-4: 链端Pallet重命名 (100%)
- 6个Pallet目录已重命名
- Runtime配置已更新
- 编译验证通过

### 待完成
⏳ 阶段5: 全局代码替换  
⏳ 阶段6: 前端项目重命名 ← **当前阶段**  
⏳ 阶段7: 文档更新  
⏳ 阶段8: 根目录重命名  
⏳ 阶段9: 编译验证  
⏳ 阶段10: 功能测试  

---

## 🚀 快速开始

### 方式1: 使用自动化脚本（推荐）

```bash
# 1. 进入项目目录
cd /home/xiaodong/文档/stardust

# 2. 添加执行权限
chmod +x docs/重命名脚本-前端项目.sh

# 3. 执行脚本
./docs/重命名脚本-前端项目.sh
```

**脚本功能**:
- ✅ 自动创建备份
- ✅ 重命名所有前端目录
- ✅ 更新配置文件
- ✅ 批量替换代码
- ✅ 提交Git更改
- ✅ 验证结果

**预计耗时**: 15-20分钟（含用户确认）

---

### 方式2: 手动逐步执行

如果您偏好手动控制每一步，请继续阅读下面的详细步骤。

---

## 📋 详细手动步骤

### 准备工作

#### 1. 创建备份
```bash
cd /home/xiaodong/文档/stardust

# 提交当前更改
git add -A
git commit -m "Checkpoint: 前端重命名前备份"

# 创建Git标签
git tag -a before-frontend-rename -m "备份点: 前端重命名之前"

# 验证标签
git tag -l | grep before
```

**✅ 验证**: 应该看到 `before-frontend-rename` 标签

---

### 阶段6: 前端项目重命名

#### 6.1 重命名前端目录（10分钟）

```bash
cd /home/xiaodong/文档/stardust

# 重命名主DApp
mv stardust-dapp stardust-dapp
echo "✅ 重命名: stardust-dapp → stardust-dapp"

# 重命名治理前端（网页端）
mv stardust-governance stardust-governance
echo "✅ 重命名: stardust-governance → stardust-governance"

# 重命名治理工具（移动端）
mv stardust-gov stardust-gov
echo "✅ 重命名: stardust-gov → stardust-gov"

# 重命名治理脚本
mv stardust-gov-scripts stardust-gov-scripts
echo "✅ 重命名: stardust-gov-scripts → stardust-gov-scripts"

# 验证
ls -d stardust-* | grep -E "dapp|gov"
```

**✅ 验证**: 应该看到4个 `stardust-*` 目录

---

#### 6.2 更新package.json（5分钟）

##### stardust-dapp
```bash
cd stardust-dapp

# 更新包名
sed -i 's/"stardust-dapp"/"stardust-dapp"/g' package.json

# 更新描述
sed -i 's/Stardust DApp/Stardust DApp/g' package.json
sed -i 's/Stardust/Stardust/g' package.json

# 验证
grep '"name":' package.json
grep '"description":' package.json
```

**✅ 验证**: 应该看到 `"stardust-dapp"` 和 `"Stardust DApp"`

##### stardust-governance
```bash
cd ../stardust-governance

sed -i 's/"stardust-governance"/"stardust-governance"/g' package.json
sed -i 's/Stardust/Stardust/g' package.json

# 验证
grep '"name":' package.json
```

##### stardust-gov
```bash
cd ../stardust-gov

sed -i 's/"stardust-gov"/"stardust-gov"/g' package.json
sed -i 's/Stardust/Stardust/g' package.json

# 验证
grep '"name":' package.json
```

##### stardust-gov-scripts
```bash
cd ../stardust-gov-scripts

sed -i 's/"stardust-gov-scripts"/"stardust-gov-scripts"/g' package.json
sed -i 's/Stardust/Stardust/g' package.json

# 验证
grep '"name":' package.json
```

---

#### 6.3 更新index.html（5分钟）

##### stardust-dapp
```bash
cd /home/xiaodong/文档/stardust/stardust-dapp

# 更新标题
sed -i 's/<title>Stardust<\/title>/<title>Stardust<\/title>/g' index.html

# 更新其他引用
sed -i 's/Stardust/Stardust/g' index.html

# 验证
grep '<title>' index.html
```

**✅ 验证**: 应该看到 `<title>Stardust</title>`

##### stardust-governance
```bash
cd ../stardust-governance

sed -i 's/<title>Stardust<\/title>/<title>Stardust<\/title>/g' index.html
sed -i 's/Stardust/Stardust/g' index.html

grep '<title>' index.html
```

##### stardust-gov
```bash
cd ../stardust-gov

sed -i 's/<title>Stardust<\/title>/<title>Stardust<\/title>/g' index.html
sed -i 's/Stardust/Stardust/g' index.html

grep '<title>' index.html
```

---

#### 6.4 更新前端代码显示文本（20分钟）

⚠️ **重要提示**: 
- 只替换UI显示文本
- 保留API变量名（如 `memoAmount`）不变
- 避免破坏性更改

##### 方式A: 选择性替换（推荐，安全）
```bash
cd /home/xiaodong/文档/stardust/stardust-dapp/src

# 只替换中文显示文本
find . -name "*.tsx" -o -name "*.ts" | xargs sed -i 's/纪念园/星尘宇宙/g'
find . -name "*.tsx" -o -name "*.ts" | xargs sed -i 's/MEMO代币/DUST代币/g'

echo "✅ 中文显示文本已更新"
```

##### 方式B: 全面替换（需谨慎）
```bash
# ⚠️ 注意：这会替换所有MEMO为DUST，可能影响API变量名
cd /home/xiaodong/文档/stardust/stardust-dapp/src

# 替换UI中的项目名称和代币名称
find . -name "*.tsx" -o -name "*.ts" | xargs sed -i 's/Stardust/Stardust/g'
find . -name "*.tsx" -o -name "*.ts" | xargs sed -i 's/\bMEMO\b/DUST/g'

echo "✅ 全面替换完成（需要测试）"
```

**建议**: 使用方式A，避免破坏API兼容性

##### stardust-governance
```bash
cd /home/xiaodong/文档/stardust/stardust-governance/src

find . -name "*.tsx" -o -name "*.ts" | xargs sed -i 's/Stardust/Stardust/g'
find . -name "*.tsx" -o -name "*.ts" | xargs sed -i 's/DUST/DUST/g'

echo "✅ stardust-governance 代码已更新"
```

---

#### 6.5 更新README文件（5分钟）

```bash
cd /home/xiaodong/文档/stardust

# 批量更新所有前端README
find stardust-dapp stardust-governance stardust-gov stardust-gov-scripts \
  -name "README.md" -type f \
  -exec sed -i 's/Stardust/Stardust/g; s/stardust/stardust/g; s/DUST/DUST/g' {} \;

echo "✅ 所有README已更新"

# 验证几个关键文件
grep -i "stardust" stardust-dapp/README.md | head -3
```

---

#### 6.6 提交更改

```bash
cd /home/xiaodong/文档/stardust

# 查看更改
git status

# 添加所有更改
git add -A

# 提交
git commit -m "阶段6完成: 前端项目重命名 (stardust→stardust)"

echo "✅ 前端重命名已提交"
```

---

### 阶段7: 文档更新（可选，推荐延后）

```bash
cd /home/xiaodong/文档/stardust/docs

# 批量更新文档
find . -name "*.md" -type f \
  -exec sed -i 's/Stardust/Stardust/g; s/stardust/stardust/g; s/\bMEMO\b/DUST/g' {} \;

# 更新根目录README
cd ..
sed -i 's/Stardust/Stardust/g; s/stardust/stardust/g; s/DUST/DUST/g' README.md

# 提交
git add -A
git commit -m "阶段7完成: 文档更新"

echo "✅ 文档已更新"
```

---

### 阶段8: 重命名根目录（最后执行）

⚠️ **警告**: 这是最后一步，会改变项目路径

```bash
cd /home/xiaodong/文档/

# 重命名根目录
mv stardust stardust

# 验证
cd stardust
pwd  # 应该显示 /home/xiaodong/文档/stardust

echo "✅ 根目录已重命名"
```

---

### 阶段9: 编译验证（30分钟）

#### 9.1 清理缓存
```bash
cd /home/xiaodong/文档/stardust

# 清理Cargo缓存
cargo clean

# 清理前端node_modules（可选）
rm -rf stardust-dapp/node_modules
rm -rf stardust-governance/node_modules
```

#### 9.2 编译Runtime
```bash
cargo check -p stardust-runtime

# 如果成功
echo "✅ Runtime编译通过"
```

#### 9.3 编译Node
```bash
cargo build --release -p stardust-node

# 预计耗时: 10-15分钟
```

#### 9.4 编译前端
```bash
cd stardust-dapp
npm install
npm run build

cd ../stardust-governance
npm install
npm run build
```

---

### 阶段10: 功能测试（30分钟）

#### 10.1 启动节点
```bash
cd /home/xiaodong/文档/stardust
./target/release/stardust-node --dev --tmp
```

**验证**:
- ✅ 节点启动成功
- ✅ 链ID显示为 "stardust"
- ✅ 区块正常产生

#### 10.2 启动前端
```bash
# 新终端
cd /home/xiaodong/文档/stardust/stardust-dapp
npm run dev
```

**验证**:
- ✅ 前端启动成功
- ✅ 页面标题显示 "Stardust"
- ✅ 代币显示为 "DUST"
- ✅ 可以连接到节点

#### 10.3 基础功能测试
1. 连接钱包 ✅
2. 查看余额（显示DUST） ✅
3. 发起一笔转账 ✅
4. 检查Polkadot.js Apps ✅

---

## 🔧 故障排查

### 问题1: 前端编译失败
```bash
# 清理缓存
cd stardust-dapp
rm -rf node_modules dist
npm install
npm run build
```

### 问题2: 节点启动失败
```bash
# 检查二进制文件
ls -lh target/release/stardust-node

# 重新编译
cargo clean
cargo build --release -p stardust-node
```

### 问题3: 需要回滚
```bash
# 回滚到前端重命名前
cd /home/xiaodong/文档/stardust
git reset --hard before-frontend-rename

# 或回滚到链端重命名前
git reset --hard before-rename-to-stardust
```

---

## 📊 进度跟踪清单

### 阶段6: 前端项目重命名
- [ ] 6.1 重命名前端目录
- [ ] 6.2 更新package.json
- [ ] 6.3 更新index.html
- [ ] 6.4 更新前端代码显示文本
- [ ] 6.5 更新README文件
- [ ] 6.6 提交更改

### 阶段7: 文档更新（可选）
- [ ] 批量更新docs/目录
- [ ] 更新根目录README.md
- [ ] 提交更改

### 阶段8: 根目录重命名
- [ ] 重命名项目根目录
- [ ] 验证路径

### 阶段9: 编译验证
- [ ] 清理缓存
- [ ] 编译Runtime
- [ ] 编译Node
- [ ] 编译前端

### 阶段10: 功能测试
- [ ] 启动节点
- [ ] 启动前端
- [ ] 基础功能测试

---

## 💡 最佳实践

### 1. 分阶段执行
- 不要一次性执行所有步骤
- 每完成一个阶段后验证
- 及时提交Git更改

### 2. 保持备份
- 执行前创建Git标签
- 定期提交更改
- 保留旧的编译产物（可选）

### 3. 谨慎替换
- 优先替换配置文件
- UI显示文本可以选择性替换
- API变量名建议保持不变

### 4. 验证充分
- 每步完成后验证结果
- 编译成功后再继续
- 功能测试覆盖核心流程

---

## 📞 需要帮助？

如果遇到问题：

1. **查看错误日志**
   ```bash
   cargo check 2>&1 | grep "error\[E"
   npm run build 2>&1 | grep "ERROR"
   ```

2. **检查Git状态**
   ```bash
   git status
   git log --oneline -5
   git tag -l
   ```

3. **回滚到安全点**
   ```bash
   git reset --hard <tag-name>
   ```

4. **继续AI辅助**
   - 可以随时要求我继续自动化执行
   - 提供具体错误信息，我会帮您解决

---

## 🎯 总结

- **自动化脚本**: 推荐使用 `docs/重命名脚本-前端项目.sh`
- **手动执行**: 适合需要精细控制的场景
- **预计总耗时**: 1-2小时（含验证和测试）
- **风险等级**: 低（有完整备份和回滚方案）

**祝您重命名顺利！** 🚀

