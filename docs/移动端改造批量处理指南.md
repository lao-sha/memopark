# 移动端改造批量处理指南

> **创建日期**: 2025-11-07  
> **目标**: 将74个页面改造为纯移动端设计  
> **状态**: 进行中  

---

## 📊 改造概览

### 已完成（8个CSS文件）

✅ ModernHomePage.css  
✅ CreateGravePage.css  
✅ GraveDetailPage.css  
✅ CreateDeceasedPage.css  
✅ ChatPage.css  
✅ ChatWindow.css  
✅ ChatList.css  
✅ styles.css (first-purchase)

### 待处理（74个TSX文件）

这些文件主要使用内联样式，需要批量修改。

---

## 🔧 批量修改规则

### 1. 容器宽度调整

**查找替换规则**：

```typescript
// 规则1: maxWidth 640 -> 480
查找: maxWidth: 640
替换: maxWidth: 480

// 规则2: maxWidth 800 -> 480
查找: maxWidth: 800
替换: maxWidth: 480

// 规则3: maxWidth 900 -> 480
查找: maxWidth: 900
替换: maxWidth: 480

// 规则4: maxWidth 1000 -> 480
查找: maxWidth: 1000
替换: maxWidth: 480

// 规则5: maxWidth 1200 -> 480
查找: maxWidth: 1200
替换: maxWidth: 480

// 规则6: maxWidth 1400 -> 480
查找: maxWidth: 1400
替换: maxWidth: 480
```

### 2. 内边距调整

```typescript
// 规则7: padding 32 -> 16
查找: padding: 32
替换: padding: 16

// 规则8: padding 24 -> 16
查找: padding: 24
替换: padding: 16

// 规则9: padding: '32px' -> '16px'
查找: padding: '32px'
替换: padding: '16px'

// 规则10: padding: '24px' -> '16px'
查找: padding: '24px'
替换: padding: '16px'
```

### 3. 响应式布局移除

```typescript
// 规则11: Row/Col 移除或简化
// 手动检查并简化 <Row gutter={...}><Col xs={...} sm={...}> 为单列布局

// 规则12: Grid 简化
// 将复杂的 grid-template-columns 简化为单列或固定列数
```

### 4. 按钮尺寸

```typescript
// 规则13: Button size="middle" -> "large"
查找: size="middle"
替换: size="large"

// 规则14: 确保按钮高度
// 检查所有 Button 是否有 size="large" 或自定义 height: 48
```

---

## 📝 具体文件修改清单

### 批次1: 纪念馆相关（8个文件）

| 文件 | 主要修改 | 状态 |
|------|---------|------|
| GraveListPage.tsx | maxWidth: 900→480, padding调整 | 🔄 处理中 |
| MyGravesPage.tsx | maxWidth: 900→480, padding调整 | 🔄 处理中 |
| CoverOptionsPage.tsx | maxWidth: 640→480 | 🔄 处理中 |
| CreateCoverOptionPage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| CarouselEditorPage.tsx | maxWidth: 800→480 | ⏳ 待处理 |
| GraveAudioPicker.tsx | modal宽度调整 | ⏳ 待处理 |
| GraveAudioPlayer.tsx | 播放器宽度调整 | ⏳ 待处理 |
| KinshipForm.tsx | 表单宽度调整 | ⏳ 待处理 |

### 批次2: 供奉相关（12个文件）

| 文件 | 主要修改 | 状态 |
|------|---------|------|
| OfferPage.tsx | 全局宽度调整 | ⏳ 待处理 |
| OfferingsTimeline.tsx | 列表布局优化 | ⏳ 待处理 |
| CategoryListPage.tsx | 分类列表优化 | ⏳ 待处理 |
| CreateCategoryPage.tsx | 表单优化 | ⏳ 待处理 |
| CreatePrimaryCategoryPage.tsx | 表单优化 | ⏳ 待处理 |
| CreateSacrificePage.tsx | 表单优化 | ⏳ 待处理 |
| CreateScenePage.tsx | 表单优化 | ⏳ 待处理 |
| CategoryBrowse.tsx | 浏览布局优化 | ⏳ 待处理 |
| MyOrders.tsx | 订单列表优化 | ⏳ 待处理 |
| OfferingsByWho.tsx | 列表优化 | ⏳ 待处理 |
| AdminCategory.tsx | 管理界面优化 | ⏳ 待处理 |
| AdminOfferRoutePage.tsx | 管理界面优化 | ⏳ 待处理 |

### 批次3: 钱包认证（10个文件）

| 文件 | 主要修改 | 状态 |
|------|---------|------|
| WalletManagePage.tsx | maxWidth: 800→480 | ⏳ 待处理 |
| WalletWelcomePage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| CreateWalletPage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| RestoreWalletPage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| WalletCreatedPage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| BackupMnemonicPage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| VerifyMnemonicPage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| SetPasswordPage.tsx | maxWidth: 640→480 | ⏳ 待处理 |
| AuthPage.tsx | maxWidth: 800→480 | ⏳ 待处理 |
| AuthEntryPage.tsx | 入口页面优化 | ⏳ 待处理 |

### 批次4: 逝者相关（4个文件）

| 文件 | 主要修改 | 状态 |
|------|---------|------|
| DeceasedListPage.tsx | 列表布局优化 | ⏳ 待处理 |
| RelationshipPage.tsx | 关系图优化 | ⏳ 待处理 |
| FriendsPage.tsx | 好友列表优化 | ⏳ 待处理 |
| LifeStoryPage.tsx | 生平展示优化 | ⏳ 待处理 |

### 批次5: OTC交易（15个文件）

| 文件 | 主要修改 | 状态 |
|------|---------|------|
| CreateMarketMakerPage.tsx | maxWidth: 900→480 | ⏳ 待处理 |
| MarketMakerConfigPage.tsx | maxWidth: 900→480 | ⏳ 待处理 |
| CreateOrderPage.tsx | maxWidth: 900→480 | ⏳ 待处理 |
| AdminOtcSettingsPage.tsx | maxWidth: 1000→480 | ⏳ 待处理 |
| ... (其他11个) | 宽度调整 | ⏳ 待处理 |

### 批次6: 管理后台（25个文件）

| 文件 | 主要修改 | 状态 |
|------|---------|------|
| DashboardPage.tsx | 仪表板简化 | ⏳ 待处理 |
| BillingAdminPage.tsx | 管理界面优化 | ⏳ 待处理 |
| EndowmentAdminPage.tsx | 管理界面优化 | ⏳ 待处理 |
| ... (其他22个) | 界面优化 | ⏳ 待处理 |

---

## 🚀 批量处理步骤

### 步骤1: 全局查找替换

使用VS Code / Cursor 的全局查找替换功能：

1. 打开查找替换（Ctrl+Shift+H 或 Cmd+Shift+H）
2. 勾选"使用正则表达式"
3. 在"包含的文件"中输入：`stardust-dapp/src/features/**/*.tsx`
4. 依次执行以下替换：

```
// 替换1
查找: maxWidth:\s*640
替换: maxWidth: 480

// 替换2
查找: maxWidth:\s*800
替换: maxWidth: 480

// 替换3
查找: maxWidth:\s*900
替换: maxWidth: 480

// 替换4
查找: maxWidth:\s*1000
替换: maxWidth: 480

// 替换5
查找: maxWidth:\s*1200
替换: maxWidth: 480

// 替换6
查找: maxWidth:\s*1400
替换: maxWidth: 480
```

### 步骤2: padding 调整

```
// 替换7
查找: padding:\s*32
替换: padding: 16

// 替换8
查找: padding:\s*24(?!px)
替换: padding: 16
```

### 步骤3: 手动检查

对于以下情况需要手动检查：
- Row/Col 响应式布局
- Grid 复杂布局
- 特殊的Modal宽度设置
- 图表组件的宽度

---

## 📋 验证清单

每个页面改造后需验证：

- [ ] 容器宽度不超过480px
- [ ] 大屏幕下居中显示
- [ ] 按钮高度至少44px
- [ ] 触控区域足够大
- [ ] 无横向滚动
- [ ] 文字清晰可读
- [ ] 图片适配移动端
- [ ] 表单易于填写

---

## 🎯 优先级

### P0 - 核心业务（立即处理）
- GraveListPage
- MyGravesPage  
- DeceasedListPage
- OfferPage
- WalletManagePage

### P1 - 重要功能（今日完成）
- 所有认证页面（10个）
- 供奉相关（12个）
- 逝者相关（4个）

### P2 - 次要功能（明日完成）
- OTC交易（15个）
- 其他功能（10个）

### P3 - 管理后台（延后处理）
- 管理后台（25个）

---

## 💡 自动化脚本（可选）

如需批量处理，可使用以下Node.js脚本：

```javascript
const fs = require('fs');
const path = require('path');

// 批量替换函数
function batchReplace(dir) {
  const files = fs.readdirSync(dir);
  
  files.forEach(file => {
    const filePath = path.join(dir, file);
    const stat = fs.statSync(filePath);
    
    if (stat.isDirectory()) {
      batchReplace(filePath);
    } else if (file.endsWith('.tsx')) {
      let content = fs.readFileSync(filePath, 'utf8');
      
      // 执行替换
      content = content.replace(/maxWidth:\s*640/g, 'maxWidth: 480');
      content = content.replace(/maxWidth:\s*800/g, 'maxWidth: 480');
      content = content.replace(/maxWidth:\s*900/g, 'maxWidth: 480');
      content = content.replace(/maxWidth:\s*1000/g, 'maxWidth: 480');
      content = content.replace(/maxWidth:\s*1200/g, 'maxWidth: 480');
      content = content.replace(/maxWidth:\s*1400/g, 'maxWidth: 480');
      
      fs.writeFileSync(filePath, content);
      console.log(`✅ Processed: ${filePath}`);
    }
  });
}

// 运行
batchReplace('./stardust-dapp/src/features');
```

---

## 📊 进度统计

- **已完成**: 8 / 82 (10%)
- **进行中**: 3 / 82 (4%)
- **待处理**: 71 / 82 (86%)

**预计完成时间**: 今日内完成P0和P1，明日完成P2，P3延后

---

**更新日期**: 2025-11-07  
**维护者**: Stardust 开发团队

