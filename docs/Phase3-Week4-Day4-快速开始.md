# Phase 3 Week 4 Day 4 快速开始

## 任务目标

**代码优化 + 性能验证**  
**预期结果**: 修复P0漏洞 + P1结构优化 + P2边界测试

---

## 任务清单

### 🔴 P0: 重复CID检查（30分钟）

**业务风险**: 重复pin导致状态覆盖、资源浪费、计费异常

**执行步骤**:
1. 添加`CidAlreadyPinned`错误定义
2. 在`request_pin`和`request_pin_for_deceased`开头添加检查
3. 调整`pin_duplicate_cid_fails`测试（改回期望错误）
4. 验证测试通过

### 🟡 P1: PinMeta结构优化（1小时）

**代码可读性**: tuple → struct

**执行步骤**:
1. 定义`PinMetadata` struct
2. 更新`PinMeta` storage定义
3. 更新所有`PinMeta::get()`调用处（约10处）
4. 更新所有测试中的解构
5. 验证19/19全部通过

### 🟢 P2: 边界测试增强（1小时）

**新增测试**:
1. `charge_due_grace_to_expired` - Grace→Expired转换
2. `pin_with_exact_existential_deposit` - 边界余额
3. `charge_due_with_empty_queue` - 空队列
4. `pin_max_replicas_boundary` - replicas边界

---

## 执行中...

