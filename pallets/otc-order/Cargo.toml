[package]
name = "pallet-otc-order"
version = "0.1.0"
license = "MIT-0"
edition.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
publish = false

[dependencies]
codec = { workspace = true, features = ["derive"] }
scale-info = { workspace = true, features = ["derive"], default-features = false }
frame-support = { workspace = true }
frame-system = { workspace = true }
sp-runtime = { workspace = true }
sp-core = { workspace = true }
sp-std = { workspace = true }
pallet-balances = { workspace = true }
pallet-timestamp = { workspace = true }
pallet-escrow = { path = "../escrow", default-features = false }
pallet-pricing = { path = "../pricing", default-features = false }
# 🆕 2025-10-20：移除 pallet-otc-listing 依赖（不再使用挂单机制）
pallet-evidence = { path = "../evidence", default-features = false }
pallet-arbitration = { path = "../arbitration", default-features = false }
pallet-memo-referrals = { path = "../memo-referrals", default-features = false }
pallet-affiliate = { path = "../affiliate", default-features = false }  # 🆕 2025-10-29: 替代 affiliate-config
pallet-market-maker = { path = "../market-maker", default-features = false }
# 🆕 2025-10-28：移除 pallet-buyer-credit 和 pallet-maker-credit，已整合为 pallet-credit
# pallet-buyer-credit = { path = "../buyer-credit", default-features = false }
# pallet-maker-credit = { path = "../maker-credit", default-features = false }
pallet-credit = { path = "../credit", default-features = false }

[dev-dependencies]
sp-io = { workspace = true }
sp-core = { workspace = true }

[features]
default = ["std"]
std = [
    "codec/std",
    "scale-info/std",
    "frame-support/std",
    "frame-system/std",
    "sp-runtime/std",
    "sp-core/std",
    "sp-std/std",
    "pallet-balances/std",
    "pallet-timestamp/std",
    "pallet-escrow/std",
    "pallet-pricing/std",
    "pallet-evidence/std",
    "pallet-arbitration/std",
    "pallet-memo-referrals/std",
    "pallet-affiliate/std",  # 🆕 2025-10-29: 替代 affiliate-config
    "pallet-market-maker/std",
    # "pallet-buyer-credit/std",  # 2025-10-28 已移除
    # "pallet-maker-credit/std",  # 2025-10-28 已移除
    "pallet-credit/std",
]


