# 快速开始指南

## 一、安装依赖

```bash
cd /home/xiaodong/文档/stardust/stardust-gov-scripts
npm install
```

## 二、使用脚本

### 1️⃣ 委员会治理脚本

**适用场景**: 委员会成员进行提案、投票、执行等治理操作

```bash
# 运行治理脚本
node governance-cli.js

# 或使用快捷命令
npm run gov
```

**操作步骤**:
1. ↑ ↓ 选择账户（3个预配置账户）
2. Enter 确认登录
3. 查看委员会成员身份
4. 选择要进入的委员会
5. 查看并处理提案/投票/执行

### 2️⃣ 批量转账脚本

**适用场景**: 从一个账户向多个地址批量转账随机金额

```bash
# 运行批量转账脚本
node batch-transfer.js

# 或使用快捷命令
npm run transfer
```

**脚本配置**:
- **发送账户**: `5CrDBEVDgXUwctSuV8EvQEBo2m187PcxoY36V7H7PGErHUW4`
- **接收地址**: 4个预设地址
- **金额范围**: 1,000,000,000 MEMO ~ 10,000,000,000 MEMO（随机，即10亿~100亿）

**执行流程**:
1. ✅ 自动验证发送账户
2. 🔌 连接区块链节点
3. 💰 检查余额是否充足
4. 📋 生成随机转账金额
5. ⚙️ 批量执行转账
6. 📊 显示结果统计

### 3️⃣ 创建挂单脚本（做市商专用）

**适用场景**: 做市商在链上创建OTC挂单

```bash
# 运行创建挂单脚本
node create-listing.js

# 或使用快捷命令
npm run listing
```

**做市商账户**:
- **做市商 1**: `5C7RjMrgfCJYyscR5Du1BLP99vFGgRDXjAt3ronftJZe39Qo`
- **做市商 2**: `5CRubhWmwNmJ3z2Ffqs3nf71XQGHBkfKSc1edNvuHZErqvdL`

**执行流程**:
1. ↑↓ 选择做市商账户
2. ✅ 验证身份和余额
3. 📝 交互式输入12个挂单参数
4. 📋 确认参数汇总
5. 🔨 构建并提交交易
6. 🎉 显示挂单创建结果

## 三、环境变量

### 自定义节点地址

```bash
# Linux/Mac
export STARDUST_WS=ws://192.168.1.100:9944
node governance-cli.js

# 或临时使用
STARDUST_WS=ws://192.168.1.100:9944 node batch-transfer.js
```

### Windows
```cmd
set STARDUST_WS=ws://192.168.1.100:9944
node governance-cli.js
```

## 四、预配置账户

### 成员 1
```
地址: 5CrDBEVDgXUwctSuV8EvQEBo2m187PcxoY36V7H7PGErHUW4
助记词: satoshi sure behave certain impulse ski slight track century kitchen clutch story
```

### 成员 2
```
地址: 5CSepuULuCiDSBjeRqr9ZburDSdTwTk5ro9BgV5u1SbHiQh9
助记词: scancel claw pretty almost under pepper volume cabbage warm brave name bullet
```

### 成员 3
```
地址: 5CotZ9gD2mLLBQ6sqL2b8gRS1Vxo6HfmRcQ2iu3T825DFgSq
助记词: report trend decline harbor hobby holiday hope recycle century end holiday display
```

## 五、快捷键（治理脚本）

- **↑ / ↓** : 上下选择
- **Enter** : 确认选择
- **Esc** : 返回上一层
- **Ctrl+C** : 退出脚本

## 六、常见问题

### Q1: 无法连接节点？
```
❌ Error: Unable to connect to ws://127.0.0.1:9944
```
**解决**: 
- 确认节点是否运行
- 检查 WebSocket 端口是否正确
- 尝试使用其他地址: `STARDUST_WS=ws://localhost:9944 node xxx.js`

### Q2: 地址验证失败？
```
❌ 地址验证失败
```
**解决**: 
- 确认助记词是否正确
- 检查是否有多余空格
- 确认密钥派生方式（SR25519）

### Q3: 余额不足？
```
❌ 余额不足！
```
**解决**: 
- 向账户充值足够代币
- 使用有余额的账户
- 减少转账金额或数量

### Q4: 不属于任何委员会？
```
⚠️ 当前账户不属于任何已知委员会
```
**解决**: 
- 使用有委员会成员权限的账户
- 或在链上添加该账户为委员会成员

### Q5: 批量转账需要多久？
每笔转账约需要 10-15 秒（含区块确认时间），4笔转账大约需要 1 分钟。

## 七、脚本说明

### governance-cli.js
- **类型**: 交互式 CLI
- **依赖**: 需要终端交互（TTY）
- **功能**: 委员会治理（提案/投票/执行）

### batch-transfer.js  
- **类型**: 自动化脚本
- **依赖**: 无需交互
- **功能**: 批量转账到多个地址

## 八、自定义配置

### 修改批量转账接收地址

编辑 `batch-transfer.js`，找到 `RECIPIENTS` 数组：

```javascript
const RECIPIENTS = [
  '你的地址1',
  '你的地址2',
  // 添加更多...
];
```

### 修改转账金额范围

```javascript
// 修改最小金额
const MIN_AMOUNT = 20_000_000n;

// 修改随机倍数（在 generateRandomAmount 函数中）
const max = min * 5n;  // 改为 5 倍
```

### 添加新的委员会

编辑 `governance-cli.js`，在 `COMMITTEE_DEFINITIONS` 中添加：

```javascript
{
  key: 'newCommittee',
  label: '新委员会名称',
  section: 'newCommitteeModule',
}
```

## 九、安全提示

⚠️ **重要**:
- 不要分享包含真实助记词的脚本
- 生产环境使用环境变量存储敏感信息
- 建议先在测试网络验证功能
- 转账前仔细检查地址和金额
- 转账操作不可撤销，请谨慎操作

## 十、技术支持

- 查看详细文档: `README.md`
- 批量转账说明: `批量转账使用说明.md`
- 项目地址: `/home/xiaodong/文档/stardust/stardust-gov-scripts`

## 十一、版本信息

- **版本**: 1.0.0
- **Node.js**: >= 16.0.0
- **@polkadot/api**: ^10.9.1
- **许可证**: MIT

---

**Happy Governing! 🎉**

