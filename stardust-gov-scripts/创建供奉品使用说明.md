# 创建供奉品脚本使用说明

**脚本文件**: `create-offerings.js`  
**功能**: 批量创建供奉品（Offerings）  
**状态**: ✅ 可用  

---

## 📋 功能说明

### 核心功能

1. **批量创建供奉品**: 一次性创建多个供奉品
2. **随机参数生成**: 自动生成多样化的供奉品参数
3. **两种类型支持**: Instant（即时）和 Timed（定时）
4. **交易跟踪**: 实时显示交易状态和结果
5. **结果保存**: 自动保存创建结果到JSON文件

---

## 🎯 供奉品类型

### Instant（即时型）

- **占比**: 70%（随机生成）
- **特点**: 立即生效，无时间限制
- **参数**: 
  - `kindFlag = 0`
  - 不需要 duration、canRenew、expireAction

### Timed（定时型）

- **占比**: 30%（随机生成）
- **特点**: 有时间限制，可续费
- **参数**:
  - `kindFlag = 1`
  - `minDuration`: 1-4周（随机）
  - `maxDuration`: min + 4~52周（随机）
  - `canRenew`: 80%概率为true
  - `expireAction`: 0~2（随机）
    - 0 = NoAction（无动作）
    - 1 = AutoArchive（自动归档）
    - 2 = AutoDelete（自动删除）

---

## ⚙️ 配置说明

### 管理员账户配置（ADMIN_CONFIG）

```javascript
const ADMIN_CONFIG = {
  mnemonic: 'gown lounge wolf cake hard sport napkin lock buddy interest session inside',
  expectedAddress: '5C7RjMrgfCJYyscR5Du1BLP99vFGgRDXjAt3ronftJZe39Qo',
};
```

**说明**:
- `mnemonic`: 管理员助记词（需要管理员权限）
- `expectedAddress`: 预期的账户地址（用于验证）

**⚠️ 安全提示**:
- 请勿将真实的生产环境助记词硬编码在脚本中
- 建议使用环境变量：`process.env.ADMIN_MNEMONIC`

---

### 创建配置（CREATE_CONFIG）

```javascript
const CREATE_CONFIG = {
  offeringCount: 50,              // 创建供奉品数量
  startKindCode: 1,               // 起始编码
  delayBetweenCreations: 500,     // 创建间延迟（毫秒）
  defaultEnabled: true,           // 默认启用状态
};
```

**参数说明**:
- `offeringCount`: 要创建的供奉品数量（默认50个）
- `startKindCode`: 起始编码号（默认从1开始）
- `delayBetweenCreations`: 每次创建之间的延迟时间（毫秒）
- `defaultEnabled`: 供奉品的默认启用状态

---

### 供奉品名称库（OFFERING_NAMES）

脚本包含50个预定义的供奉品名称，分为8大类：

1. **传统祭祀**: 鲜花祭祀、香烛供奉、水果贡品等
2. **鲜花类**: 菊花祭祀、百合献礼、玫瑰敬献等
3. **香类**: 檀香供奉、沉香祭祀、龙涎香献礼等
4. **灯烛类**: 烛台祭祀、油灯供奉、长明灯献礼等
5. **食品类**: 素斋供奉、斋饭祭祀、糕点献礼等
6. **纸品类**: 纸扎祭品、金元宝供奉、银元宝祭祀等
7. **经文类**: 经文诵读、佛经供奉、道经祭祀等
8. **音乐类**: 音乐祭祀、梵音供奉、钟声献礼等

**使用方式**: 脚本会随机从这50个名称中选择

---

### 媒体Schema CID库（MEDIA_SCHEMA_CIDS）

```javascript
const MEDIA_SCHEMA_CIDS = [
  'QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG',
  'QmPZ9gcCEpqKTo6aq61g2nXGUhM4iCL3ewB6LDXZCtioEB',
  // ... 更多CID
];
```

**说明**:
- 预定义了5个示例IPFS CID
- 脚本会随机选择其中一个
- **⚠️ 注意**: 这些是示例CID，实际使用时需要替换为真实的媒体Schema CID

---

## 🚀 使用方法

### 方法1: 使用npm scripts（推荐）

```bash
# 进入脚本目录
cd stardust-gov-scripts

# 安装依赖（首次使用）
npm install

# 运行脚本
npm run create-offerings

# 或使用简写
npm run offerings
```

---

### 方法2: 直接运行

```bash
# 进入脚本目录
cd stardust-gov-scripts

# 直接运行
node create-offerings.js

# 或使用可执行权限
chmod +x create-offerings.js
./create-offerings.js
```

---

### 方法3: 使用环境变量

```bash
# 设置节点地址（可选，默认 ws://127.0.0.1:9944）
export STARDUST_WS=ws://127.0.0.1:9944

# 运行脚本
npm run create-offerings
```

---

## 📊 执行流程

### 1. 初始化阶段

```
✅ 加密库准备完成
✅ 管理员账户地址验证通过
   地址: 5C7RjMrgfCJYyscR5Du1BLP99vFGgRDXjAt3ronftJZe39Qo
```

---

### 2. 连接链节点

```
🔌 正在连接节点: ws://127.0.0.1:9944
✅ 已连接 Stardust Testnet • Stardust Node v1.0.0
   代币: MEMO (精度: 12)
```

---

### 3. 余额检查

```
💰 检查账户余额...
   可用余额: 1000000.0 MEMO
```

---

### 4. 生成供奉品列表

```
📋 生成供奉品列表...
✅ 生成 50 个供奉品
   Instant类型: 35 个
   Timed类型: 15 个
```

---

### 5. 预估手续费

```
预估总手续费: 0.5 MEMO
单笔手续费: 0.01 MEMO
✅ 余额充足
```

---

### 6. 确认提示

```
⚠️  准备开始创建供奉品
   按 Ctrl+C 取消，或等待 3 秒自动开始...
```

---

### 7. 批量创建

```
🎯 开始创建供奉品...

[1/50] 创建供奉品 #1
   名称: 鲜花祭祀
   类型: Instant
   Media CID: QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG
   预估手续费: 0.01 MEMO
   📦 状态: Ready
   📡 已广播
   ✅ 包含区块: 0x1234...
   🎉 最终确认: 0x1234...
   ✅ 供奉品创建成功！

[2/50] 创建供奉品 #2
   名称: 香烛供奉
   类型: Timed
   时长范围: 2-20 周
   可续费: 是
   过期动作: 1
   ...
```

---

### 8. 最终结果

```
📊 创建完成
✅ 成功: 48 个
❌ 失败: 2 个
📝 总计: 50 个
📈 成功率: 96.00%

📊 类型统计:
   Instant: 34 个
   Timed: 14 个
```

---

### 9. 失败详情（如有）

```
❌ 失败的供奉品:
   - 编码 25: 纸扎祭品 (memoOfferings.AlreadyExists: Offering kind already exists)
   - 编码 40: 音乐祭祀 (system.ExtrinsicFailed: Transaction failed)
```

---

### 10. 余额统计

```
💰 最终余额查询...
   初始余额: 1000000.0 MEMO
   最终余额: 999999.52 MEMO
   实际花费: 0.48 MEMO
   平均手续费: 0.01 MEMO
```

---

### 11. 结果保存

```
💾 结果已保存到: /path/to/create-offerings-result.json
👋 脚本执行完成
```

---

## 📁 输出文件

### create-offerings-result.json

```json
{
  "timestamp": "2025-10-27T10:30:45.123Z",
  "summary": {
    "total": 50,
    "success": 48,
    "failed": 2,
    "successRate": "96.00%",
    "totalFees": "480000000000",
    "totalFeesFormatted": "0.48 MEMO"
  },
  "results": [
    {
      "kindCode": 1,
      "name": "鲜花祭祀",
      "mediaSchemaCid": "QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG",
      "kindFlag": 0,
      "minDuration": null,
      "maxDuration": null,
      "canRenew": false,
      "expireAction": 0,
      "enabled": true,
      "type": "Instant",
      "success": true,
      "blockHash": "0x1234567890abcdef...",
      "fee": "10000000000"
    },
    // ... 更多结果
  ]
}
```

---

## ⚠️ 常见错误

### 1. 地址验证失败

```
❌ 地址验证失败
   期望: 5C7RjMrgfCJYyscR5Du1BLP99vFGgRDXjAt3ronftJZe39Qo
   实际: 5GrwvaEF5zXb26Fz9rcQpDWS57CtERHpNehXCPcNoHGKutQY
```

**原因**: 助记词与预期地址不匹配  
**解决**: 检查 `ADMIN_CONFIG` 中的助记词和地址

---

### 2. 连接失败

```
❌ 发生错误: connect ECONNREFUSED 127.0.0.1:9944
```

**原因**: 链节点未启动或地址错误  
**解决**: 
1. 确认链节点已启动
2. 检查 WS 地址是否正确
3. 使用 `STARDUST_WS` 环境变量指定正确地址

---

### 3. 余额不足

```
❌ 余额不足！
   可用: 0.1 MEMO
   需要: 0.5 MEMO
```

**原因**: 账户余额不足以支付手续费  
**解决**: 向管理员账户转入足够的 MEMO

---

### 4. 权限不足

```
❌ 创建失败: system.BadOrigin: Bad origin
```

**原因**: 账户没有管理员权限  
**解决**: 使用具有管理员权限的账户

---

### 5. 编码已存在

```
❌ 创建失败: memoOfferings.AlreadyExists: Offering kind already exists
```

**原因**: 该 kindCode 已被使用  
**解决**: 修改 `startKindCode` 从未使用的编码开始

---

## 🔧 高级配置

### 自定义创建数量

```javascript
const CREATE_CONFIG = {
  offeringCount: 100,  // 改为100个
  // ...
};
```

---

### 自定义起始编码

```javascript
const CREATE_CONFIG = {
  startKindCode: 101,  // 从101开始
  // ...
};
```

---

### 调整创建延迟

```javascript
const CREATE_CONFIG = {
  delayBetweenCreations: 1000,  // 1秒延迟
  // ...
};
```

---

### 使用环境变量（推荐）

```bash
# 创建 .env 文件
cat > .env << EOF
STARDUST_WS=ws://127.0.0.1:9944
ADMIN_MNEMONIC=your mnemonic words here
EOF

# 在脚本中使用
require('dotenv').config();
const mnemonic = process.env.ADMIN_MNEMONIC || ADMIN_CONFIG.mnemonic;
```

---

## 📝 最佳实践

### 1. 测试环境先验证

```bash
# 先在测试环境运行
export STARDUST_WS=ws://testnet.example.com:9944
npm run create-offerings
```

---

### 2. 小批量创建

```javascript
const CREATE_CONFIG = {
  offeringCount: 10,  // 先创建10个测试
  // ...
};
```

---

### 3. 备份结果文件

```bash
# 每次运行后备份结果
cp create-offerings-result.json backups/result-$(date +%Y%m%d-%H%M%S).json
```

---

### 4. 监控链状态

```bash
# 使用另一个终端监控链
polkadot-js-api query.system.events
```

---

### 5. 批次创建

```bash
# 分批创建，避免一次性创建过多
# 第一批: 1-50
startKindCode: 1

# 第二批: 51-100
startKindCode: 51
```

---

## 🐛 调试模式

### 启用详细日志

在脚本中添加：

```javascript
// 在 main() 函数开始处
console.log('DEBUG: 配置详情');
console.log(JSON.stringify(CREATE_CONFIG, null, 2));
console.log(JSON.stringify(ADMIN_CONFIG, null, 2));
```

---

### 单个供奉品测试

```javascript
const CREATE_CONFIG = {
  offeringCount: 1,  // 只创建1个
  // ...
};
```

---

## 📊 性能建议

### 1. 并发控制

当前脚本是串行创建，如需提高速度可以考虑：

```javascript
// 并发创建（需要修改代码）
const CONCURRENT_COUNT = 5;  // 同时创建5个
```

---

### 2. 延迟优化

```javascript
const CREATE_CONFIG = {
  delayBetweenCreations: 100,  // 减少到100ms
  // ...
};
```

**⚠️ 注意**: 延迟太小可能导致链拥堵

---

## 🔐 安全建议

1. **不要硬编码助记词**: 使用环境变量
2. **使用测试账户**: 不要用生产环境的主账户
3. **定期备份**: 保存创建结果文件
4. **权限最小化**: 只给必要的管理员权限
5. **监控异常**: 设置告警机制

---

## ✅ 验证创建结果

### 方法1: 使用polkadot.js Apps

1. 访问 https://polkadot.js.org/apps
2. 连接到你的节点
3. 导航到 Developer → Chain state
4. 选择 `memoOfferings` → `specs(u8)`
5. 输入 kindCode 查询

---

### 方法2: 使用脚本查询

```javascript
// 查询供奉品
const offering = await api.query.memoOfferings.specs(kindCode);
console.log(offering.toHuman());
```

---

## 📞 技术支持

如遇到问题：

1. 检查本文档的"常见错误"章节
2. 查看 `create-offerings-result.json` 中的错误信息
3. 查看链节点日志
4. 联系技术团队

---

**最后更新**: 2025-10-27  
**脚本版本**: 1.0.0  
**状态**: ✅ 生产可用
